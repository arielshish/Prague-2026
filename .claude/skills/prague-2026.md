---
name: prague-2026
description: ידע מעמיק על פרויקט Prague 2026 — app.html, מבנה הנתונים, כל הפונקציות, באגים ידועים, וכללי עבודה. השתמש בסקיל זה לפני כל עריכה ב-app.html.
---

# Prague 2026 — מדריך מלא לעריכה

> עדכון אחרון: 2026-08-07 (גרסה 11 — OTP מספרי במקום magic link)

## 🚨 `DAYS` בקוד ≠ הלוז שהמשפחה רואה (12/08)

`getDaysState()` הוא `saved || DAYS`. **`prague_days_v1` תמיד גובר.** אצל המשפחה הוא קיים (מסונכרן מהענן), ולכן **עריכת `var DAYS` ב-app.html לא מגיעה אליהם בכלל**.

נמדד: מכשיר עם לוז שמור לא הראה את Pizza ביום 3, Manifesto ביום 5 ו-Koh-i-noor ביום 7 — כולם נפרסו בקוד.

**מגיע מהקוד**: `ALL_PLACES`, `PLACE_COORDS`, כרטיסי הבנק, `how`/`booking`/`tips` של מקומות, לוגיקה.
**לא מגיע**: כל מה שבתוך `DAYS` — תחנות, `details`/`tips` שלהן, `meals`.

⚠️ **לפני שמבטיחים למשתמש ששינוי לוז "נפרס"** — לוודא איך הוא אמור להגיע אליו. שינוי לוז אמיתי חייב לעבור דרך ה-UI או דרך מיגרציה שממזגת לפי גרסה.

## שיטת העבודה — אושרה ע"י המשתמש (12/08)

1. **למצוא את המנגנון בקוד**, לא לשער. `grep` על נתיב הנתונים עד לשורה עצמה.
2. **לאמת בהרצה ובמדידה**, לא בקריאת קוד. Playwright / סקריפט / כלי חיצוני.
3. **לומר במפורש מה לא אומת.** "לא בדקתי" עדיף על מספר שנשמע בטוח.
4. **לתקן שורש ולא סימפטום.**
5. **לחפש את אותה תקלה במקום נוסף** — `grep` על הנושא, לא רק על מה שהמשתמש הצביע עליו.
6. **להציג עלות נסתרת** (מרחק, החלפה, שעה סותרת) ולא להכריע בשקט.
7. **לא לחרוג ממה שהתבקש.**
8. סדר: tag → שינוי → אימות → commit/push → PR → CI → merge → **CLAUDE.md + סקיל** → אימות פריסה.
9. `BUILD_ID` בכל שינוי מהותי, עם assert על המחרוזת הישנה.
10. **העדפות נשמרות כאן**, לא בשיחה.

## ⚠️ לפני הכול — לשון זכר בלבד

**המשתמש (אריאל) הוא זכר.** כל פנייה אליו בגוף שני — **זכר**: `לחץ`, `תוודא`, `תגיד`, `תבחר`, `שים לב`. לא `לחצי`/`תוודאי`/`תגידי`.

נכשל פעמיים ב-11/08 והמשתמש תיקן במפורש. בעברית אין ברירת מחדל ניטרלית לגוף שני — כל פועל הוא הכרעה. **מוקד הסיכון: משפטי הסיום והוראות הפעולה.** לסרוק כל פועל בגוף שני לפני שליחה.

זה חל על השיחה עם אריאל, **לא** על טקסט ה-UI באפליקציה (שם קוראים גם בני משפחה אחרים).

## קובץ עיקרי
`/home/user/Prague-2026/app.html` — קובץ HTML יחיד, RTL עברי, ~7100 שורות.
GitHub Pages: `arielshish.github.io/Prague-2026/app.html`
Branch: `claude/unknown-session-xpa0pr` → PR → merge ל-`main`

---

## מבנה נתונים

### ALL_PLACES[] (~שורה 2675) — מקור יחיד לכל המקומות

```javascript
// פריטים עם שדה type:
type:'shop'       → {icon, name, stars, hours, metro, duration, brands, tip, mapUrl}
type:'restaurant' → {icon, name, level, sub, desc, price, badge, badgeClr, badgeTxt, google, mapUrl}
type:'dessert'    → {icon, name, level, sub, desc, price, rating, tag, tagClr, where, mapUrl}
type:'community'  → {icon, name, cat, fb, google, desc, booking, duration, tips, who, how, mapUrl}
type:'photo'      → {icon, name, fee, sub, desc, best, crowds, fee_txt, rating, tip, mapUrl}
type:'exchange'   → {icon, name, stars, hours, metro, duration, brands, tip, mapUrl} — נעצי "המרת כסף בטוחה" (2026-08-07, אותו schema כמו shop)
```

**`exchange`**: קטגוריה חדשה בטאב מיקום בלבד (אין computed view ייעודי, כמו `photo`). כל מקום שממפה type→ערך (`TYPE_GRAD`, `catColor` ב-`addMapMarkers`, `typeColor`/`typeLabel` ב-`renderNearbyList`, בלוק ה-if ב-`showMapNavDialog`) חייב ערך ל-`exchange` אחרת נופל ל-fallback אפור/גנרי. **רק מקומות עם אימות אמיתי (דירוג גוגל + ביקורות עקביות משני מקורות עצמאיים לפחות)** — לא להוסיף על סמך מקור בודד או "נשמע בסדר" (זו הייתה הסיבה שלא נוסף נעץ לאזור ואצלב — מקור אחד המליץ, אותו חיפוש בדיוק הזהיר על 10-20% עמלה נסתרת שם).

**3 המקומות הנוכחיים (2026-08-07)**: `eXchange s.r.o.` (Štefánikova, סמיחוב — הכתובת הישנה בקפרובה נסגרה 2/1/2024, אל תחזירו אותה!), `MI RYMAS EXCHANGE` (Václavské náměstí 19), `Praha Exchange` (Jindřišská 12). לכל אחד `tip` עם `\n\n` להפרדת פסקאות — לכן ה-div ב-`showMapNavDialog` לבלוק ה-`exchange` חייב `white-space:pre-line`, אחרת השורות מתמזגות.

**⚠️ לקח קריטי**: קבצי `.docx`/מסמכים "מאומתים" שמישהו מעלה (גם עם "דוח אימות" רשמי-נשמע) **הם claim, לא מקור**. בסשן הזה שני קבצים כאלה (נוצרו ב-ChatGPT) הכילו טעויות — כולל אחת שכבר תפסתי בעצמי (כתובת שסגורה מ-2024 שהקובץ טען שעדיין פעילה). תמיד לחפש כל שם+כתובת בעצמך לפני הוספה ל-ALL_PLACES, גם אם המקור "נשמע" מבוסס.

**⚠️ שדה `tips` ב-community**: תמיד **string** (לא array) — לעולם לא לקרוא `p.tips.forEach()` ישירות. תמיד `Array.isArray` guard.

**Computed views** (~שורה 3133, read-only):
```javascript
var COMMUNITY   = ALL_PLACES.filter(p => p.type==='community');  // 47 פריטים
var RESTAURANTS = ALL_PLACES.filter(p => p.type==='restaurant'); // 25 פריטים
var SHOPS       = ALL_PLACES.filter(p => p.type==='shop');       // 8 פריטים
var DESSERTS    = ALL_PLACES.filter(p => p.type==='dessert');    // 13 פריטים
var PHOTO_SPOTS = ALL_PLACES.filter(p => p.type==='photo');      // 12 פריטים
```

**⚠️ COMMUNITY כולל categories שונות** — לא רק אטרקציות:
- `cat:'🎟️ אטרקציות'` — 24 אטרקציות
- `cat:'🍽️ מסעדות'` — ביקורות קהילה על מסעדות
- `cat:'🍦 קינוחים'` — קינוחים קהילה
- `cat:'🛍️ קניות'` — קניות קהילה
כשמסננים לפי אטרקציות, חובה `(p.cat||'').indexOf('אטרקציות') !== -1` — לא `p.type === 'community'`!

**נפרדים** (לא ב-ALL_PLACES):
```javascript
REMINDERS[]  // (~שורה 1750) — משימות לפני הטיול
DAYS[]       // לוז בסיס, מוחלף ע"י DAYS_STATE מ-Firestore
```

**DAYS_STATE stops** — כל stop:
```javascript
{ emoji, name, time, desc, details, tips, booking, mapUrl, google, duration, who }
```

---

## מיפוי פונקציות עם שורות מדויקות

| פונקציה | שורה | תיאור |
|---------|------|--------|
| `esc(s)` | 3189 | HTML escape — מטפל ב-`&<>"'`. **חובה לכל ערך שמוכנס ל-innerHTML** |
| `timeOfDay(timeStr)` | 3243 | מחזיר `{label,emoji}` לפי שעה, או `null` אם לא HH:MM |
| `getDaysState()` | 3234 | מצב ימים נוכחי (Firestore/local) |
| `refreshScheduleBadges()` | 3272 | מרנדר מחדש טאב פעיל — נקראת אחרי כל שמירה |
| `openTab(id)` | 3211 | מחליף טאב + קורא ל-renderNearbyList אם location |
| `saveDaysState()` | 3254 | שומר DAYS_STATE + קורא refreshScheduleBadges |
| `initDaySwipe()` | 3336 | IIFE — Pointer Events API, סווייפ ימים, threshold 40px |
| `renderDays()` | 3372 | לוז יומי — stop cards עם DS tokens, numbered markers |
| `findItemInDays(name)` | 3551 | מחזיר `{dayNum, dayTitle, time}` או `null` |
| `showStopDetail(dayIdx,stopIdx)` | 3494 | popup פרטי תחנה + chips google/who |
| `renderReminders()` | 2154 | תזכורות עם schedInfo ירוק |
| `buildBankCards(items,...)` | 3674 | כרטיסי בנק עם תיאור/ציון/משך/תג כבר בלוז |
| `renderCommunity()` | 4062 | בנק אטרקציות לפי קטגוריה |
| `openAddCommunityStop(recIdx)` | 4113 | modal הוספת תחנה מ-community |
| `saveAddCommunityStop(recIdx)` | 4152 | שומר community → DAYS (כולל google/duration/who) |
| `renderShopping()` | 4193 | SHOPS עם תגי כבר בלוז |
| `renderRestaurants()` | 4223 | RESTAURANTS + DESSERTS + PHOTO_SPOTS |
| `renderDesserts()` | 4294 | DESSERTS |
| `renderPhotoSpots()` | 4339 | PHOTO_SPOTS |
| `haversine(lat1,lng1,lat2,lng2)` | 4567 | מרחק בק"מ |
| `initLeafletMap()` | 4583 | Leaflet retry loop עד 50×200ms |
| `addMapMarkers()` | 4623 | pins לכל ALL_PLACES + צבע לפי type + כתום אם בלוז |
| `showMapNavDialog(p, coords)` | 4644 | bottom sheet ניווט + מידע לפי type |
| `locateMe()` | 4770 | Geolocation → pin סגול "אתם כאן" |
| `filterNearby(cat)` | 4803 | מעדכן `_nearbyFilter` + מרנדר רשימה |
| `renderNearbyList()` | 4814 | רשימה ממוינת Haversine + פילטר |
| `_openNearbyDialog(apIdx,lat,lng)` | 4901 | wrapper עם try-catch → showMapNavDialog |
| `restaurantImgHtml(r)` | 4914 | header תמונה 150px עם gradient+Wikimedia fallback |
| `renderLocationTab()` | 4578 | מאתחל מפה + renderNearbyList |

---

## תשתית מפה ומיקום (~שורה 4396+)

### PLACE_COORDS (~שורה 4396)
```javascript
var PLACE_COORDS = {
  'שם מקום': [lat, lng],
  // ... כל 105 המקומות ב-ALL_PLACES
}
```
- **חובה לכל פריט שרוצים שיופיע במפה/רשימה** — אם `!PLACE_COORDS[p.name]` הפריט מדולג
- ⚠️ השם חייב להיות **זהה בדיוק** לשדה `name` ב-ALL_PLACES (Unicode, -, —, רווחים)

**נוהל הוספת קואורדינטה (אין יוצא מן הכלל):**
1. **שני מקורות בלתי-תלויים לפחות.** מסכימים בדיוק → קח את הערך. הפרש של עשרות מטרים → קח את זה ששניים מסכימים עליו, ורשום בהערה את השלישי ואת ההפרש (`OC Skalka`: tesca.cz ו-2GIS זהים, מקור שלישי במרחק ~35 מ׳).
2. **למדוד מרחק לכל שכן קרוב** לפני הדחיפה. `OC Skalka`: המלון 1367 מ׳, Fashion Arena 1952 מ׳ — עקבי עם המיקום על קו A. קואורדינטה שנוחתת חשודה של קרוב למקום קיים היא כמעט תמיד טעות.
3. **0 התנגשויות קואורדינטה** — ולוודא שהשם הקנוני של הנקודה החדשה הוא **עצמה** (`_canonicalName(n) === n`), אחרת היא נבלעה בקבוצה קיימת.

**⚠️ מקורות שחסומים ל-egress בסביבה הזו** (11/08): `mapy.com`, `2gis.cz`, `pepco.cz`, `firmy.cz`, `openhours.cz`, `cz.maptons.com`, ואף **Wikipedia ו-Nominatim**. `WebFetch` ישיר נכשל ב-`EGRESS_BLOCKED`; `WebSearch` עובד ומחזיר את הערכים בתוך ה-snippet. אל תסיק "אין מקור" מכשל fetch — נסה דרך חיפוש.

**מה עושים כשעובדה לא ניתנת לאימות מקוון**: לא ממציאים, ולא משמיטים בשקט. אם המשפחה הייתה שם, **עדות מהשטח היא מקור** — אבל מסמנים אותה ככזו. ב-`OC Skalka` נוכחות **Pepco** נשענת רק על עדות המשפחה (רשימת השוכרים לא פורסמה בשום מקור נגיש), ושעות טסקו **סותרות** בין מקורות (22:00 מול חצות) — שני הפערים נרשמו **בכרטיס עצמו** עם המלצה לוודא, ולא הוכרעו לטובת ניחוש.

**מקום שימושי שאי אפשר לאמת → טקסט, לא נעץ.** סניף `Koh-i-noor` ב-`Na Příkopě 860/24` יושב בדיוק על מסלול יום 7, אבל לא נמצאה לו קואורדינטה משני מקורות. הוא נכנס ל-`tips` של התחנה **ולא ל-`PLACE_COORDS`**, עם הערה בקוד שאוסרת לנחש. כשיש כמה סניפים לאותה רשת — **בוחרים את זה שאפשר לאמת**, ומזכירים את השאר בטקסט.

⚠️ **המרת DMS**: ויקיפדיה נותנת שניות שלמות, כלומר ±15 מ׳ מובנה. הפרש של ~27 מ׳ מול מקור עשרוני הוא **הסכמה**, לא סתירה. לא לפסול מקור בגלל זה, ולא להעמיד פנים שהדיוק גבוה מהמקור.

### PLACE_IMGS (~שורה 4494)
```javascript
var PLACE_IMGS = {
  'שם מקום': 'https://upload.wikimedia.org/...',
  // 35+ תמונות Wikimedia Commons
}
```
- `onerror="this.remove()"` — תמונה כושלת נעלמת, gradient+emoji נשאר
- **לא להוסיף URLs שאינם Wikimedia Commons** — CDN אחרים נחסמים

### TYPE_GRAD (~שורה 4558)
```javascript
var TYPE_GRAD = {
  restaurant: ['#2563EB','#1D4ED8'],
  community:  ['#16a34a','#15803d'],
  photo:      ['#9333ea','#7e22ce'],
  dessert:    ['#e879f9','#c026d3'],
  shop:       ['#f59e0b','#d97706'],
  schedule:   ['#F4634A','#e55039'],
}
```

---

## טאב מיקום — פירוט מלא

### פילטרים
```
'all'        → כל המקומות שיש להם PLACE_COORDS
'schedule'   → רק מה שב-DAYS_STATE
'restaurant' → type === 'restaurant'
'community'  → type === 'community' && cat.indexOf('אטרקציות') !== -1  ← חשוב!
'photo'      → type === 'photo'
```
⚠️ פילטר 'community' = **רק אטרקציות** — לא כל community. השתמש ב-indexOf לא ב-type בלבד.

### renderNearbyList() — זרימה
1. בונה `items[]` מכל ALL_PLACES שיש להם PLACE_COORDS
2. מוסיף: `{p, coords, dist, inDays, effType}`
3. מסנן לפי `_nearbyFilter`
4. ממיין: קרוב → רחוק (Haversine), ללא GPS → אלפביתי
5. מרנדר כרטיסים עם `onclick="_openNearbyDialog(apIdx, c0, c1)"`
   - `apIdx = ALL_PLACES.indexOf(p)` — אינדקס מספרי, בטוח לחלוטין בonclick

### showMapNavDialog(p, coords) — מנגנון בטוח
```javascript
// ✅ נכון — window._mapNavPlace מונע בעיות escaping
window._mapNavPlace = p;
// ...onclick uses: var mp = window._mapNavPlace || {};

// ✅ נכון — Array.isArray guard לפני forEach
if (p.tips && p.tips.length) {
  if (Array.isArray(p.tips)) {
    p.tips.forEach(t => ...);
  } else {
    infoHtml += '💡 ' + esc(p.tips);
  }
}

// ❌ אסור — community tips הם strings, forEach זורק TypeError:
p.tips.forEach(...)  // ← גורם לקריסה שקטה, dialog לא נפתח
```

---

## Design System

### CSS Tokens (~שורה 19)
```css
:root {
  --c-primary: #F4634A;
  --c-card: #fff;
  --r-card: 22px;
  --s-card: 0 2px 16px rgba(0,0,0,0.08);
  --t-fast: 0.15s; --t-base: 0.25s; --t-slow: 0.4s;
  --ease-spring: cubic-bezier(0.34,1.56,0.64,1);
  --c-surface: #F7F3EE;
}
```

### DS Classes
```
.ds-badge-{red,amber,green,blue,gray}
.ds-chip
.ds-fab
.ds-stat
.ds-section-hdr
.skeleton
.btn-primary  → gradient כתום + shadow
.btn-ghost    → transparent + border
```

### Light Theme v7
- body/html: `#F7F3EE` / `#FAF7F4`
- כרטיסים/מודאלים/nav: `#ffffff`
- header: כהה בכוונה (Prague skyline)
- גבולות: `rgba(0,0,0,0.10)` (לבנים לא נראים על רקע לבן!)

---

## Firebase / Firestore

| נתיב | תוכן |
|------|------|
| `appdata/main` | `days`, `total_budget`, `ts` |
| `appdata/pack` | `data` (JSON string של pack state) |
| `appdata/reminders` | `data` (JSON string) |
| `appdata/budget` | `total` (לתאימות אחורה) |

- `ensureFirebaseAuth()` → **לא עושה auth בעצמו** (מ-PR #61). **ממתין ל-`onAuthStateChanged`** (לא קורא `currentUser` ישירות!) ומחזיר Promise עם `true`/`false` לפי `user && !user.isAnonymous`. ה-auth האמיתי קורה פעם אחת ב-`verifyLoginCode()` דרך `signInWithCustomToken`. **פעם היה כאן `signInAnonymously()` — הוסר בכוונה, אל תחזירו** (ראה "session אנונימי ישן נתקע" למטה)
- `getFirestoreDb()` → Firestore instance
- `onSnapshot` על `appdata/main` → sync real-time לימים + תקציב

---

## מטמון iOS — למה תיקון "לא מגיע" למשפחה (2026-08-10)

האפליקציה היא HTML יחיד מ-GitHub Pages עם `apple-mobile-web-app-capable`. במסך הבית ב-iOS היא רצה **standalone: אין סרגל כתובות, אין כפתור רענון**, ו-Safari מגיש גרסה שמורה. פריסה מוצלחת ≠ המשתמש רואה אותה.

- `BUILD_ID` — **לעדכן ידנית בכל שינוי מהותי**. מוצג על כפתור "🔄 משוך גרסה עדכנית" דרך `_renderBuildStamp()` (נקראת מ-`_showApp()`). זו הדרך לוודא איזו גרסה טעונה בטלפון לפני שמתחילים לחפש באג.
- `forceUpdate()` — `?v=<timestamp>` + `location.replace`. **`location.reload(true)` לא עובד** — הפרמטר הוסר מהתקן.
- **כשמשתמש מדווח "לא רואה את X" אחרי פריסה מוצלחת — לבקש קודם את חותמת הגרסה**, לפני שמחפשים באג בקוד.
- ⚠️ **החותמת עצמה עלולה לשקר.** ב-11/08 היא פיגרה שלוש גרסאות: עדכנתי אותה בסקריפט עם `s.replace(ישן, חדש)` **בלי לאמת שהישן נמצא**, ואחרי מיזוג `main` המחרוזת השתנתה — ההחלפה נכשלה בשקט וכל שאר התוכן כן נכנס. **כל עדכון BUILD_ID בסקריפט חייב לזרוק אם המחרוזת הישנה לא נמצאה**, ואחרי כל מיזוג ל-`main` לוודא `grep "var BUILD_ID"` מול מה שציפינו.

## קורלציה תחנות ↔ מקומות ↔ מיקום (2026-08-10)

`_mappablePlaces()` = `_uniquePlacesForMap()` **+ תחנות לוז עם קואורדינטות שאינן ב-`ALL_PLACES`** (מסונתזות כ-`type:'stop'`). זו הרשימה שממנה מציירים `addMapMarkers()` ו-`renderNearbyList()`.

לפני: **29 תחנות עם מיקום, 7 בלבד על המפה.** המלון, טירת פראג, כיכר העיר העתיקה ואי קמפה היו בלוז, עם קואורדינטות, ובלתי נראים לגמרי.

**שלוש מלכודות שחייבים לזכור:**
1. **memoization חובה** (`_mappableCache`) — כרטיסי הרשימה מעבירים לדיאלוג **אינדקס מספרי**. מערך חדש בכל קריאה ⇒ `indexOf === -1` לאובייקט מסונתז ⇒ הדיאלוג לא נפתח. בטל דרך `_invalidateMappable()` בלבד (נקרא מ-`saveDaysState` ומה-listener של הלוז).
2. **de-dupe לפי קואורדינטה, לא רק לפי שם** — `"ארוחת ערב — Gran Fierro"` מול `"Gran Fierro"`, ו-**6** תחנות "מלון" על עוגן אחד. בלי זה: 120 נעצים על 98 נקודות.
3. **`_scheduledPlaceNames()`** — מקור יחיד ל"מה בלוז" למפה ולרשימה. מסמן גם מקום שתחנה היא כינוי שלו, אחרת הנעץ ששרד את ה-de-dupe מאבד את תג היום.

`type:'stop'` נוסף ל-`TYPE_GRAD` / `catColor` / `typeColor` / `typeLabel`. **כל type חדש חייב ערך בכל ארבעתם.**

**✅ נסגר 11/08**: `מוזיאון החושים` תוקן ל-`[50.0840, 14.4291]` (Jindřišská 939/20 — היה ממופה בטעות לעיר העתיקה), ו-`Westfield Chodov` ל-`[50.0311, 14.4905]` (סטייה של ~350 מ׳, אומת משלושה מקורות). `Albert Supermarket — Chodov` חולק את הנקודה של הקניון **במכוון** — ההיפרמרקט בתוכו; זו לא כפילות. נותרה נקודה מוערמת אחת, והיא נכונה.

**⚠️ סחיפת type במיזוג** — ב-`_uniquePlacesForMap`, כשמקום קיים גם כ-`restaurant`/`shop`/`dessert` וגם כ-`community`, **ה-type הספציפי חייב לגבור**. `merged.type = rich.type` גרם ל-`Manifesto Market — Anděl` להירשם כ-`community` ולהיעלם מפילטר "מסעדות". `community` = קטלוג גנרי; הסיווג הספציפי הוא זה שקובע פילטר.

**כשמוסיפים `type` חדש — לבדוק שיש לו צ׳יפ סינון** ב-`#lf-*` וב-`filterNearby`. `shop` ו-`dessert` היו על המפה אבל בלי כפתור סינון, כלומר נגישים רק דרך "הכל".

## שם קנוני לכל נקודה (2026-08-11)

`PLACE_COORDS` = 126 מפתחות אך ~98 נקודות. **17 קבוצות / 41 שמות** הן כינויים לאותה נקודה (`Výtopna` ↔ `Výtopna — מסעדת הרכבות`, `מצודת פראג` ↔ `טירת פראג וקתדרלת ויטוס`).

`_canonMap()` מקבץ לפי קואורדינטה ובוחר קנוני: **(1)** שם שקיים ב-`ALL_PLACES`, הקצר מביניהם; **(2)** אחרת הארוך ביותר — בקבוצת המלון זה `Comfort Hotel Prague City East` ולא `חזרה למלון ומנוחה`.

**כל מה שממופתח לפי שם חייב לעבור דרך `_canonicalName`.** כרגע: `isVisited`, `toggleVisited`, ו-`getVisitedState` (מיגרציה חד-פעמית, שומרת את ה-timestamp המוקדם בהתנגשות).

⚠️ **גם נתוני ענן**: ה-listener של `visited` הציב `VISITED_STATE` ישירות מ-`JSON.parse` ועקף את הנרמול — סימון ממכשיר אחר תחת כינוי פשוט לא נראה. חובה `_normalizeVisited()` על **כל** מקור. נתפס ברגרסיה כ-`remote applied: false`.

## ⚠️ סנכרון: מכשיר ריק לא דוחף לענן (12/08)

**הבאג שמחק את כל הסימונים של המשפחה.** `_resyncLocalDataToCloud()` קרא ל-`saveVisitedState()` ללא תנאי; מכשיר עם `localStorage` ריק דחף `"{}"` לענן ודרס הכל. ה-listener לא תפס כי המגן היה `if (d.visited)` ו-`"{}"` היא **truthy**.

**`_hasLocalData(key)` — resync מעלה למעלה בלבד.** אין נתונים מקומיים → אין מה לתרום → לא דוחפים. מזהה `{}`/`[]` ולא רק מפתח חסר.

**`_acceptRemote(json, localKey)` — ריק מהענן לא דורס מקומי (13/08).** המגן המשלים: `_hasLocalData` חוסם דחיפה למעלה, `_acceptRemote` חוסם קליטה למטה. **שניהם נחוצים** — מספיק מכשיר אחד עם גרסה ישנה כדי שריק יגיע לענן.

⚠️ **ה-listener אינו המקום היחיד.** אחרי שהגנתי על חמשת ה-`if (d.X)` בסנכרון החי, הבדיקה עדיין נפלה: `loadDaysFromSheets` הוא **נתיב קריאה שני** ל-`days_custom` שקבע `DAYS_STATE = []` ודרס את `localStorage`. לכל שדה יש לרוב **שלושה** מסלולים — `onSnapshot` חי · `.get()` בטעינה · מסלול GAS. `grep` על שם השדה, לא על הפונקציה.

**12 מגנים על 8 שדות.** מכשיר ריק מבצע **0 כתיבות** לענן.

⚠️ **`if (jsonString)` הוא מגן שגוי** — `"{}"`, `"[]"`, `"0"` כולן truthy. **לבדוק תוכן, לא קיום.**

⚠️ **וריאנט שני, מסוכן יותר — fallback שמייצר נתונים (13/08)**: `loadBudget()` נופל ל-`DEFAULT_BUDGET`, ולכן resync ממכשיר ריק דחף תקציב **שנראה לגיטימי** מעל 4,000 ₪ שהמשתמש הגדיר. **לחפש `||` בכל `load*`** — כל טעינה עם ברירת מחדל היא מועמדת. `saveTotalBudget` שרד רק כי היה `if (tb)`.

⚠️ **הכשל שלי**: הוספתי `_hasLocalData` לחמישה מפתחות ו**דילגתי על שתי שורות התקציב באותה פונקציה**. כשמתקנים מחלקת באג — **למנות את כל החברים אחד-אחד ולסמן ✅/❌**, לא "לעבור על הפונקציה" בעין.

## 🔴 עריכת משתמש חייבת לעבור דרך `save*`, לא דרך `setItem` (13/08)

`saveBudgetFromModal` כתב קטגוריות ל-`localStorage` בלבד ודחף רק `total_budget`. **הדחיפה הזו עצמה ייצרה snapshot**, ה-listener ראה קטגוריות ישנות בענן ≠ מקומי חדש, ודרס את המקומי. העריכה נמחקה תוך שברירי שנייה — **זה** היה השורש של "התקציב לא נשמר", לא ה-resync.

**הדפוס**: `grep "localStorage.setItem('prague_"` → לסנן נתיבי קליטה מהענן → מה שנשאר הן כתיבות משתמש, וכל אחת **חייבת** `save*` שדוחפת.

⚠️ **כתיבה מקומית בלי דחיפה היא גרועה מכלום** — היא נראית כמו הצלחה (`toast('✅ תקציב עודכן!')`) ואז מתבטלת בשקט מהענן.
## מפת החום: רמז ולא מילוי (13/08)

מילוי אטום בכל תא = קיר מלבנים. **רקע בשקיפות 5%–34%**, טקסט תמיד כהה, **היום היקר בכל שורה מודגש**. זברה + `thead`/`tfoot` + ריווח כפול.

⚠️ **מעל 3 ימים הטבלה נגללת** — לומר את זה במפורש, אחרת נראית חתוכה.

## כפתור "אזן" — האיזון גלוי, ההחלטה של המשתמש (13/08)

`computeBalance(budget, actualMap)` → `openBalanceDialog()` → `applyBalance()`. ⚖️ ליד ✏️ ערוך.

⚠️ **לא אוטומטי.** איזון אוטומטי משכתב בשקט מספרים שהמשתמש קבע — בדיוק סוג הכתיבה שמחקה השבוע "היינו" ותקציב. הוא רואה בדיוק מה יזוז ולוחץ.
⚠️ **הסה״כ לא משתנה** — העברה בין קטגוריות.
⚠️ **לוקחים ביחס לגודל העודף**, חסום ב-`Math.min(amt, sl.free)` — אף קטגוריה לא יורדת מתחת לבפועל שלה.
⚠️ **חוסר כיסוי נאמר במפורש** ולא מאוזן חלקית בשקט.

עובר דרך `saveBudget()` → נשמר מקומית, נדחף לענן, ומסנכרן את הסה״כ.

## 🔴 מספר בתוך שורה עברית חייב `<bdi>` (13/08)

`₪2,198 / ₪3,000` הוצג כ-`₪3,000 / ₪2,198`, ו-`3,281 CZK · ₪469` כ-`CZK · ₪469 3,281`. מספרים ולטינית הם LTR בתוך RTL, ואלגוריתם ה-bidi מסדר אותם מחדש. **אין שום סימן לזה בקוד — רק בצילום.**

**כל סכום עטוף `<bdi>`** עם `unicode-bidi:isolate;direction:ltr`. 49 מופעים בדוח, **וגם ב-`renderBudget` במסך עצמו**. כשמוצאים באג כיווניות — לחפש את אותה תבנית בכל מקום שמדפיס מספר בתוך עברית.

⚠️ **סדר נכון עדיין לא אומר מובן** — `₪2,198 / ₪3,000` לא מסביר מה מה. כל מספר קיבל **תווית**: בפועל · מתוך · נותר · נוצל.

⚠️ **`color:inherit` על `<b>` בתוך `<div>` מעומעם יורש את המעומעם**, לא את כלל ה-`b`.

⚠️ **מה שצריך להיראות כשסגור — חייב להיות בתוך ה-`<summary>`**, לא ב-`<details>`.

## מיקום ההוצאה — שדה נפרד (13/08)

`place` ברשומת ההוצאה. כפתור **📍 המיקום שלי** → `getCurrentPosition` → `_nearestPlace()` על `_mappablePlaces()`.

⚠️ **`haversine` מחזיר ק״מ** — כפול 1000 למטרים.
⚠️ **מעל 800 מ׳ → בלי מיקום.** עדיף ריק ממיקום שגוי.
⚠️ **המיקום לא נוגע בקטגוריה.** גלידה בגן החיות = אוכל, מקום = גן החיות.
⚠️ **לא חוסם שמירה** — אין הרשאה/timeout 8ש׳ → נשמר בלי מיקום. משתמש ב-`_userLat/_userLng` אם כבר אותרו.

מוצג בשורת ההוצאה, בעמודת `מיקום` ב-CSV (`COLS` עלה ל-9) ובדוח. רשומות ישנות → `—`.

## ⚠️ בחירה מפורשת של המשתמש גוברת תמיד (13/08)

`saveExpense` הוסיף את אימוג׳י הקטגוריה רק `if (!guessExpCat(name))` — כלומר הצ׳יפ נלקח בחשבון **רק כששום דבר אחר לא זוהה**. בחירת `🦁 גן החיות` מהרשימה + לחיצה על "אוכל" נשמרה כאטרקציה, בלי שום סימן.

**התיקון**: `guessExpCat(name) !== catKey`, ולהסיר אימוג׳י-קטגוריה פותח כדי שלא יישארו שני סימנים סותרים.

⚠️ **הלקח הכללי**: **הרחבת זיהוי שוברת מסלולים שהסתמכו על אי-זיהוי.** כשה-`guessExpCat` זיהה רק אימוג׳י פותח, `🦁` לא היה ברשימה והצ׳יפ עבד. אחרי ההרחבה — כמעט כל פריט מהרשימה חסם אותו. **אחרי כל הרחבת heuristic — לחפש `if (!heuristic(...))` בקוד.**

## הפרדת תחומים: מיקום · היינו · הוצאות (13/08)

רשימת ההצעות בהוצאות נבנתה גם מ-`DAYS` והציעה `😴 מנוחה במלון`, `🚶 הליכה`, `🌉 גשר קארל`, `🚖 צ'ק-אאוט`. **תחנת לוז עונה על "איפה היינו", לא על "על מה שילמנו".** הקבוצה הוסרה מההוצאות; מיקום ו"היינו" ממשיכים להציג את כל 35 התחנות.

⚠️ **לפני הסרה — לבדוק מה נאבד.** טירת פראג קיימת כ-`מצודת פראג`, השייט כ-`שייט בוולטאווה`. שניים היו רק תחנה ונוספו ל-`COMMUNITY` עם התוכן verbatim: `ממלכת הרכבות` (350/250 CZK) ו-`כיכר העיר העתיקה` (מגדל ~200). `Čertovka` **לא** נוסף — `booking` אומר "אזור ציבורי", כלומר חינם.

⚠️ **כלל שנפסל**: זיהוי "משלמים כאן" לפי `\d CZK|כרטיס` בטקסט התחנה — נמדד ששגוי בשני הכיוונים (השאיר את גשר קארל, הפיל את Hamleys). **הפרדה לפי מקור הנתונים עדיפה על ניחוש מטקסט.**

⚠️ **שם חדש זהה לשם התחנה** → ה-de-dupe ממזג ולא מכפיל נעץ. אומת: 105 נעצים, 0 שמות כפולים.

**154 → 121 פריטים · 34% → 2% בכללי** (נשארו רק `🏨 מלון`, `💊 בריאות`, `🔀 שונות`) · **0 מ-42 ההוצאות זזו.**

## סיווג הוצאות נגזר מהמאגר (13/08)

טבלה כתובה ביד לא מדביקה מאגר שגדל: **53 מתוך 154 ההצעות (34%) נפלו ל"כללי"**. `_expDerivedMaps()` בונה מ-`ALL_PLACES` שלוש מפות (התאמה מדויקת `icon+' '+name` · שמות מהארוך לקצר · אימוג׳י), ממוקאש.

**סדר ההכרעה הוא הלוגיקה, לא פרט טכני** — מהוודאי אל הניחוש:
1. התאמה מדויקת לפריט במאגר · 2. הטבלה הידנית (בחירת המשתמש) · 3. שם מקום בטקסט · 4. אימוג׳י נגזר · 5. `null` → קטגוריה אחרונה + "ללא סיווג"

⚠️ **2 לפני 3 בכוונה** — אחרת `🍽️ KFC קניון פלדיום` נחשב קניות רק כי "פלדיום" בשם.
⚠️ **1 לפני 2 בכוונה** — אחרת `🌉 Mlýnec` (מסעדה) ו-`🚂 ממלכת הרכבות` (אטרקציה) מסווגים לפי האימוג׳י ולא לפי מה שהם.
⚠️ **אימוג׳י דו-משמעי במאגר נפסל** (🌉 ✡️ 🏛️ 🍔 🍫) — עדיף "לא יודע" מתשובה בטוחה ושגויה.

**אחרי: 8/154 (5%) בכללי** — מלון ×3, מנוחה, בריאות, שונות, ו-🌅 ×2 (שייט ערב מול ארוחת בוקר — לא הוכרע במכוון). **0 מ-42 ההוצאות זזו.**

## ⚠️ שדה חסר הוא באג שקט (13/08)

`SHOPS.map(r => r.e+' '+r.n)` — סכימה ישנה. באיחוד ל-`ALL_PLACES` (18/07) השדות הפכו ל-`icon`/`name`, ואתר הקריאה הזה פוספס: **9 שורות "undefined undefined"** במודל "הוסף הוצאה" במשך חודש.

⚠️ **גישה לשדה שלא קיים לא זורקת.** כל בדיקות "0 שגיאות JS" עברו בהצלחה כשהמסך הראה ג׳יבריש. **לחפש בפלט, לא ביומן.**

`undefsweep.mjs` סורק את כל הטאבים והמודלים אחרי `undefined`/`NaN`/`[object Object]` בטקסט הנראה. להריץ אחרי כל שינוי סכימה.

**כששם שדה משתנה — `grep` על שם המערך (`SHOPS.`), לא על שם השדה.** אתר קריאה עם שמות קצרים (`r.e`) לא ניתן לחיפוש.

## דוח HTML + סיווג מאוחד (13/08)

`exportHtmlReport()` — כפתור 📄. קובץ עצמאי אחד, RTL, בהיר/כהה, `@media print`. מספרי כותרת · תקציב מול בפועל **לפי הקטגוריות של המשתמש** · מפת חום קטגוריה × יום · הוצאה יומית · פירוט ב-`<details>`.

⚠️ **`_h()` על כל טקסט מהמשתמש** — שמות הוצאות מכילים גרשיים ומרכאות. תו לא-מוברח שובר את המסמך מהנקודה והלאה.

⚠️ **בטבלה רחבה מהמסך, העמודה האחרונה נחתכת** — הסה״כ הוא לכן העמודה **השנייה**, צמוד לשם. נתפס בצילום (`,382` במקום `15,382`), לא בחישוב. **תמיד לרנדר ולהסתכל.**

⚠️ **צבע סטטוס לא לבד** — תווית מילולית לצד כל פס.

**`EXP_CAT_EMOJI` + `expBudgetCat(name, budget)` = נקודת הסיווג היחידה.** `catActualMap` ו-`_expCat` שניהם קוראים לה. לפני: 22 אימוג׳י סווגו אחרת בין המסכים; אחרי: **0**. ההתאמה בכל מקום בשם ולא רק ב-`^`. `💡` הוסר מאטרקציות (הוא של `כללי ושונות 💡`). הוצאה בלי אימוג׳י → הקטגוריה האחרונה, **מסומנת "ללא סיווג"**.

## ⚠️ הפער ההיסטורי: שתי טבלאות סיווג (תוקן 13/08)

`guessExpCat` (פסי התקציב) מול `_expCat` (ייצוא CSV) — **22 אימוג׳י מסווגים אחרת**. `guessExpCat` מתאים **רק בתחילת השם** ונופל ל`null` → הקטגוריה **האחרונה** בתקציב; `_expCat` מתאים בכל מקום ונופל ל-`❓ אחר`. `💡` = אטרקציות בייצוא, והוא האימוג׳י של `כללי ושונות 💡` בתקציב.

**הייצוא לא קורא את קטגוריות התקציב של המשתמש** — הוא מייצר חמש משלו (`🍽️ אוכל` מול `אוכל ומסעדות 🍽️`). שינוי שם קטגוריה בתקציב לא משתקף בדוח.

**מטריצת קטגוריה × יום** בייצוא: `COLS = Math.max(8, dayOrder.length + 2)` נקבע **אחרי** האגרגציה — `line()` קורא את `COLS` בזמן הקריאה. תא ריק ולא `0` (גרף באקסל מדלג על ריק). שתי מטריצות נפרדות CZK/ILS כדי שכל תא יישאר מספר גולמי.

## תקציב: הסה״כ נגזר מהקטגוריות (13/08)

היו **שני מספרים בלתי-תלויים** — `manualTotal || catTotal`, והידני גבר. העלאת קטגוריה לא הזיזה את הסה״כ, והסה״כ לא התחלק לקטגוריות.

**עכשיו**: `budgetCatTotal()` הוא מקור האמת. `prague_total_budget` **לא נמחק** — `_syncDerivedTotal()` מעדכן אותו כמראה, בשביל גיליון ה-GAS וקוראים ותיקים.

| פונקציה | תפקיד |
|----------|-------|
| `budgetCatTotal(budget)` | סכום הקטגוריות — המספר שמוצג |
| `_syncDerivedTotal(budget)` | מראה ל-`prague_total_budget` + ענן. **נקרא מתוך `saveBudget()`** — נקודת הכתיבה היחידה לקטגוריות |
| `_modalCatTotal()` / `_refreshModalTotal()` | סה״כ חי במודל תוך כדי הקלדה (`oninput`) |

⚠️ **`_syncDerivedTotal` פותח ב-`_hasLocalData('prague_budget_v1')`** — בלעדיו זו אותה תקלה בפעם השלישית: מכשיר ריק היה דוחף את סכום `DEFAULT_BUDGET` (11,500) מעל התקציב האמיתי.

⚠️ **מתכנס, לא מהדהד** — דוחפים רק כש-`t !== loadTotalBudget()`; שני מכשירים מחשבים אותו סכום ולכן זה נעצר אחרי דחיפה אחת.

**הרשימה המלאה של הקריאות המוגנות ב-`_resyncLocalDataToCloud`:** `prague_exp_v10` · `prague_days_v1` · `prague_pack_v2` · `prague_visited_v1` · `prague_remindersDone` · `prague_budget_v1` · `saveTotalBudget` (מוגן ב-`if (tb)`) · `saveSchedules` (לא נדרש — כותב `remindersSchedule` בלבד).

⚠️ **`getDaysState()` נופל ל-`DAYS` הבסיסי** כשאין שמירה מקומית — כלומר בלי המגן, מכשיר חדש דוחף את המסלול המקורי מעל עריכות המשפחה. אותה פצצה, רק שקטה יותר.

**לפני שמוסיפים sync חדש** — לשאול: *מה קורה כשמכשיר בלי נתונים נכנס?* זה המקרה שהורס.

## כפתור "היינו" — מצב ביקורים (2026-08-10)

`VISITED_STATE` = `{שם מקום: timestamp}`. **ממופתח לפי שם, לא לפי אינדקס** — אינדקס ב-`ALL_PLACES` זז בכל הוספת מקום והסימון היה עובר למקום אחר.

| פונקציה | תפקיד |
|----------|-------|
| `getVisitedState()` | טעינה עצלה מ-`localStorage['prague_visited_v1']` |
| `isVisited(name)` / `visitedCount()` | קריאה |
| `toggleVisited(name)` | סימון/ביטול + שמירה + רענון UI |
| `saveVisitedState()` | localStorage → `appdata/main.visited` (דפוס `saveDaysState`) |
| `visitedBtnHtml(name, variant)` | `'icon'` = כפתור צר לכרטיסי קהילה · `'block'` = שורת פעולות במסעדות |
| `refreshVisitedUI()` | מרנדר את הטאב הפעיל, **כולל `days`** — `refreshScheduleBadges` מדלגת עליו בכוונה |

⚠️ **ה-listener רשום ב-capture phase** (`addEventListener('click', fn, true)`). כרטיס התחנה בטאב ימים נושא `onclick="showStopDetail(...)"` כ-attribute; ב-bubble ה-listener ב-`document` רץ **אחרי** ה-onclick של הכרטיס, ו-`stopPropagation` מאחר — הסימון עבד אבל הפופ-אפ נפתח יחד איתו. **אל תחזירו ל-bubble.**

⚠️ **event delegation ולא `onclick` inline** — listener אחד על `document` עם `.closest('.visitedBtn')` + `data-vn`. שמות מקומות מכילים גרשים ומרכאות (`מגדל פטז׳ין`, `מ"ר`, `בסופ"ש`); זו אותה מלכודת שכבר שברה `onclick` פעמיים בפרויקט הזה (ראה `_mapNavPlace`).

**כיסוי בפועל — 110/110 כרטיסים + 3 משטחים נוספים**: אטרקציות 56 · מסעדות 26 · קינוחים 11 · צילום 10 · קניות 7, וכן `showStopDetail` (הפופ-אפ של תחנה), כרטיס התחנה בטאב ימים (תג ✅), ודיאלוג הנעץ במפה.

⚠️ **מזהי הקונטיינרים לא אחידים** — `#commList`, `#restaurantsList`, `#dessertsList`, `#photoSpotsList`, אבל קניות הוא **`#shoppingGrid`** (לא `shoppingList`). בדיקה שסופרת לפי מזהה שגוי תדווח 0 ותיראה כמו באג.

`refreshVisitedUI()` חייב לעשות **ארבעה** דברים, אחרת הסימון לא אחיד: (1) לעדכן כל `.visitedBtn` ב-DOM דרך `outerHTML` — כך נתפסים גם פופ-אפ/דיאלוג פתוחים שלא נבנים מחדש ע"י רינדור טאב; (2) `refreshScheduleBadges()`; (3) `renderDays()` אם טאב ימים פעיל; (4) לצייר מחדש את נעצי המפה.

## באגים ידועים שתוקנו (אל תחזיר אותם!)

| באג | תיקון |
|-----|-------|
| `p.tips.forEach` על community item → TypeError, dialog לא נפתח | `Array.isArray` guard |
| `p.name`/`p.desc` עם `"` בתוך onclick attribute → שבירת HTML | `window._mapNavPlace` בmapNavDialog |
| פילטר אטרקציות מציג מסעדות/קינוחים community | `indexOf('אטרקציות')` על `cat` |
| `renderNearbyList` — שגיאה ב-item אחד שוברת כל הרשימה | `try-catch` per item |
| **`signInAnonymously()` כ-fallback ב-`ensureFirebaseAuth()`** — Firestore דוחה anonymous, אז זה ייצר "התחברת בהצלחה" מדומה שנכשלת בשקט בכל כתיבה (`Missing or insufficient permissions`) | הוסר לגמרי. auth אמיתי רק דרך OTP → `signInWithCustomToken`. `ensureFirebaseAuth()` רק בודק `!isAnonymous` |
| **קריאה סינכרונית ל-`currentUser` ב-`ensureFirebaseAuth()`** — Firebase משחזר session אסינכרונית, אז בטעינת עמוד `currentUser===null` ו-`false` נִמְמָש לצמיתות; כל הטעינות ב-`DOMContentLoaded` נדחו ב-permission-denied → תג "⚠️ מקומי" | להמתין ל-`onAuthStateChanged` (עם unsubscribe + guard נגד resolve כפול + timeout 8ש׳) |
| **`initRealtimeSync()` בלי ניתוק listeners קודמים** — `_realtimeUnsubs` היה קיים אבל מעולם לא שימש לניתוק, אז קריאה שנייה (אחרי כניסה) הכפילה listeners | לנתק את כל `_realtimeUnsubs` ולאפס את המערך בתחילת הפונקציה |
| **`showDayTimePicker` שמר תחנה רזה** — `{emoji,name,time,desc,mapUrl:''}` בלבד, ואיבד `details`/`tips`/`booking`/`google`/`duration`/`who`/`mapUrl`. כל מקום שנוסף מבנק האטרקציות נפתח אח"כ ב"ימים" ככותרת ריקה. (`saveAddCommunityStop` כן שמר מלא — שתי דרכי הוספה, שתי סכימות) | `_placeToStopFields` / `_stopFallback` / `_withPlaceDetails` — מנרמלות את סכימות ה-type ומשלימות שדות **ריקים בלבד**. מוחל ב-`showStopDetail`, `renderDays` ובשמירה. **ההשלמה בזמן תצוגה מתקנת גם תחנות שכבר נשמרו רזות בענן** — אין צורך במיגרציה. כשמוסיפים דרך הוספה חדשה — לעבור דרך `_stopFallback`, לא לבנות אובייקט תחנה ביד |
| **`showDayTimePicker(emoji,name,desc)` קיבל `desc` ומעולם לא הציג אותו** — פרמטר מת; הפופ-אפ הראה רק שם מקוצר ל-26 תווים | התיאור + צ׳יפים (⭐/משך/מתאים ל/הזמנה) מוצגים מתוך `_stopFallback(name)` |
| **fallback חלקי ב-`showStopDetail`/`renderDays`** — השלימו רק `google`/`duration`/`who`, ורק מול `COMMUNITY`/`RESTAURANTS` ולא מול `ALL_PLACES` המלא (כלומר shop/dessert/photo/exchange לא כוסו כלל) | `_withPlaceDetails` על `_uniquePlacesForMap()` — כיסוי מלא, ואותו מידע בכרטיס ובפופ-אפ |
| **bottom sheet בלי `max-height`+`overflow-y`** — `#addStopModal` היה הגיליון **היחיד** באפליקציה בלי שניהם. תוכן הגיליון ~730px, כלומר גבוה ממסך של רוב הטלפונים; עם `align-items:flex-end` החלק העליון (כותרת + טאבי הבנק) נחתך **מעל** גבול המסך, ומכיוון שהקונטיינר `position:fixed` — אי אפשר לגלול אליו בכלל. נמדד: 62px חתוכים ב-375×667, 65px ב-390×664, 14px ב-414×715 | `max-height:88vh;overflow-y:auto;overscroll-behavior:contain;box-sizing:border-box` על הפאנל. **כל bottom sheet חדש חייב את זה** — יש 7 גיליונות אחרים בקובץ, כולם כבר עם `max-height:8Xvh;overflow-y:auto`; העתיקו מהם |
| **`element.style.cssText = '...'` על קונטיינר שיש לו style ב-HTML** — `buildBankCards` דרסה את כל ה-style של `#attrBankRow` בכל החלפת טאב ומחקה בשקט את `-webkit-overflow-scrolling` ו-`margin-bottom` שהיו ב-HTML. `buildBankRemCards` בכלל לא נגעה בסגנון, אז ירשה את מה שהטאב הקודם השאיר | קבוע יחיד `BANK_ROW_STYLE`, שתי הפונקציות מציבות אותו, וחייב להישאר זהה ל-`style` של `#attrBankRow` ב-HTML |
| **`addEventListener('DOMContentLoaded', _showApp)` מתוך callback אסינכרוני** — `onAuthStateChanged` יורה לרוב *אחרי* שהאירוע כבר קרה, ומאזין לאירוע שעבר לא יורה לעולם. תוצאה: session תקף לגמרי אבל מסך הכניסה נשאר תקוע והמשתמש חושב שהתנתק | `_showAppWhenReady()` — בודק `document.readyState`, קורא `_showApp()` ישירות אם ה-DOM מוכן. **כל קוד אסינכרוני שמציג UI חייב את הבדיקה הזו** |

---

## כללי עבודה קריטיים

### לפני כל שינוי — ללא יוצא מן הכלל:
```bash
git tag backup-<תיאור>-$(date +%Y%m%d-%H%M)
git push origin --tags
```

### סדר קבוע:
1. `git tag` לפני הנגיעה הראשונה
2. בצע שינוי
3. `git commit` + `git push`
4. עדכן CLAUDE.md + עדכן skill זה

### branch וdeploy:
- פיתוח תמיד על: `claude/unknown-session-xpa0pr`
- PR → merge ל-`main` → GitHub Actions פורס ל-GitHub Pages (~1-2 דקות)
- **לעולם לא לדחוף ישירות ל-main**

### ציוני Google:
- לאמת רק מ: `top-rated.online` או TripAdvisor
- **לא להמציא ציונים**

### המרת מחירים לשקלים (12/08):
`_annotateIls(root)` מוסיף `(₪…)` ליד כל סכום CZK **בזמן תצוגה**, דרך `RATE`. נקרא מ-`openTab` (כל הטאבים חוץ מהוצאות), `renderDays`, `showStopDetail`, `showMapNavDialog` ו-`showDayTimePicker`.

⚠️ **צמתי טקסט בלבד** — `createTreeWalker` + `SHOW_TEXT`. אסור לגעת ב-HTML או ב-attributes: זו בדיוק הדרך לשבור `onclick` ו-`data-vn` בשמות עם מרכאות.

⚠️ **ה-regex חייב lookahead** `(?!\s*\(₪)` — בלעדיו רינדור חוזר על אותו צומת מוסיף את ההמרה פעמיים.

⚠️ **טאב הוצאות מוחרג במפורש** — שם כבר מוצגות שתי המטבעות.

⚠️ **`_priceTxt(v)`** — שדות `price` כבר מכילים "CZK"; להוסיף עוד אחד יוצר `"400–800 CZK למנה CZK"`.

**לקח על הזרקת קוד בסקריפט**: אל תספור רווחי הזחה מפלט של `awk`/`grep` שמוסיף תווים משלו (`print NR": "$0`). עוגן שגוי בהזחה אחת נחת אצלי בפונקציה אחרת לגמרי בסוף הקובץ, ועבר את בדיקת ה-`count==1`. **לאמת שההוק נחת בפונקציה הנכונה** (`grep -n` על שם הפונקציה + מספר שורה), לא רק שההחלפה בוצעה.

### ייצוא CSV (`exportCsv`, 11/08):
⚠️ **אל תסתמכו על סדר `EXPENSES`** — הוא נבנה ב-`unshift` ומתמזג מ-Sheets ומ-Firebase, כלומר הסדר הוא "מי נוסף אחרון". כל ייצוא/דוח חייב למיין בעצמו.

⚠️ **`e.date` הוא מחרוזת he-IL** (`"11.8.2026, 22:03:53"`), לא `Date`. `Date.parse` מחזיר `NaN`, ו-`sort` על `NaN` **נכשל בשקט ונראה כאילו עבד**. להשתמש ב-`_expTs(e)` — regex מפורש עם נפילה ל-`id` (שהוא `Date.now()`).

⚠️ **ה-BOM חייב להישאר** — בלעדיו אקסל מציג עברית כג׳יבריש. בקוד הוא כתוב כ-escape `﻿` ולא כתו עצמו; `Edit` עם התו האמיתי ב-`old_string` **לא יתאים**. לערוך דרך סקריפט Python/Node שמאמת את העוגן.

מבנה הקובץ: טבלת פירוט נקייה (ניתנת למיון/סינון) → בלוק סיכום בתחתית (סה״כ, לפי יום, לפי קטגוריה). לא שורות סיכום ביניים, שהורסות את הטבלה.

`_expCat()` גוזרת קטגוריה **מהאימוג׳י שהמשתמש עצמו הקליד** בשם ההוצאה — לא סיווג מומצא. בלי אימוג׳י מוכר → `❓ אחר` במפורש.

**לבדוק ייצוא ע"י הרצת הכפתור בפועל** (`page.waitForEvent('download')` + `saveAs`) והשוואת הסכומים לחישוב עצמאי בכלי אחר. כך נמצא שהסכומים תקינים מול 35 ההוצאות האמיתיות.

**מבנה הקובץ (מ-12/08) הוא דוח ולא טבלה שטוחה**: כותרת עם תקופה/כמות/**שער חליפין** → סה״כ כללי בראש → סיכום לפי קטגוריה → סיכום לפי יום → **פירוט מחולק לקטגוריות**, לכל אחת שורת `סה״כ <קטגוריה>` → סה״כ כללי בסוף. כל סכום מופיע ב-CZK **וב-ILS**.

⚠️ **מספרים גולמיים בלבד** בעמודות הכספיות — `27853` ולא `"27,853"`. פסיקים הופכים אותם לטקסט באקסל ואי אפשר לסכם.

⚠️ **שער החליפין חייב להופיע בדוח** — בלעדיו אי אפשר לשחזר איך הומרו המספרים.

⚠️ **התאמת מין ומספר** — `cnt(n)` מחזיר `'הוצאה אחת'` ליחיד. קטגוריה עם פריט אחד היא מקרה נפוץ.

**האימות הוא צולב, לא רק "רץ בלי שגיאה"**: לקרוא את הקובץ שנוצר בכלי אחר (Python) ולוודא ששורות הפירוט מסתכמות למקור, **ושסכום הקטגוריות שווה לסה״כ הכללי**. סכום אחוזים 99.9% הוא עיגול תקין ולא באג.

### עובדה שמופיעה בכמה מקומות — לבדוק את כולם (12/08):
שאלה על קו אוטובוס אחד חשפה **שלוש טענות סותרות** על הגעה ל-Aquapalace: תחנת הלוז אמרה "אין תחבורה ציבורית", כרטיס הקהילה אמר "363 ישר לפתח הפארק", וכרטיס הטיפים אמר "אפשר בנוחות". כולן שרדו חודשים כי אף אחת לא נבדקה מול השנייה.

**לפני שעונים על שאלת עובדה** (תחבורה, שעות, מחיר) — `grep` על שם המקום בכל הקובץ ולראות **כל** המקומות שבהם הוא מוזכר: `DAYS` · `ALL_PLACES` (`how`/`booking`/`tips`) · כרטיסי טיפים סטטיים ב-HTML. לתקן את כולם באותו קומיט, אחרת נשארת סתירה.

### פסק דין מהשטח = תיעוד, לא פריט לשיבוץ (11/08):
כשהמשפחה מספרת שאכלה/ביקרה במקום **בתאריך מסוים** — לשבץ אותו **ליום של אותו תאריך**, לא ליום הפנוי הבא. שיבצתי את `Pizza & Pasta Factory` (נאכלה 10/8) כארוחת ערב עתידית ביום 5, והמשתמש תפס. התוצאה הייתה לוז שגוי בשני הכיוונים: יום 3 לא שיקף את הערב האמיתי, ויום 5 שלח אותם לאכול שוב באותו מקום.

תחנה ביום שכבר עבר היא **רשומה לגיטימית ומועילה** — היא מזינה את לוח הסיכום, את `findItemInDays` ואת "היינו".

⚠️ **לבדוק שכל מקום מופיע פעם אחת בלבד בכל `DAYS`.** `findItemInDays` מחזיר את ההתאמה הראשונה, ולכן שיבוץ כפול לא זורק שגיאה — הוא רק מציג תג יום שגוי. סריקה: לעבור על כל הימים ולספור מופעים לפי שם.

### עלות נסתרת — להציג, לא להכריע בשקט (11/08):
כשממליצים על מקום עם עלות שאינה גלויה בשם שלו — מרחק, החלפת קו מטרו, שעת סגירה — **לרשום אותה בכרטיס**. `Manifesto Market` נכנס ליום 5 עם `⚠️ 6.7 ק״מ מהמלון ובכיוון ההפוך... ~30–35 דק׳` בטיפים, כדי שההחלטה תישאר של המשתמש. אותו דפוס כמו שעות טסקו הסותרות ב-`OC Skalka`.

### CI — כשל רשת ≠ קישור שבור (11/08):
`check-links.yml` מבחין בין שני סוגי כשל, ו**אסור לטשטש את ההבחנה**:
- **`404`/`410`/`500`** — השרת ענה, המשאב לא קיים → **מפיל build**. זו כל מטרת הבדיקה.
- **`000`/ERR** — אין תשובת HTTP כלל (timeout/DNS) → ניסיון אחרון ארוך (45 שנ׳), ואז **אזהרה בלבד**.

⚠️ **אל תוסיף דומיינים ל-soft list בשביל timeout.** ניסיתי את זה ב-11/08 והחזרתי: שלוש ריצות אדומות ברצף נפלו כל פעם על אתר **אחר** (`prague2go`, ואז `jewishmuseum.cz` שעבר שתי דקות קודם) בזמן ש-`main` היה ירוק. soft list לפי דומיין = משחק חתול-ועכבר שמנפח את הרשימה עד שהבדיקה לא בודקת כלום. ה-soft list שמורה לחסימת-בוטים **עקבית** (`hrad.cz` 403, wikimedia thumbnails 400) — לא לרעש רשת.

**לפני שדוחפים שינוי בלוגיקת הסיווג — לבדוק אותה מקומית** על 8 המקרים: `200`/`302` עוברים · `404`/`500` מפילים · `ERR`/`000` מזהירים · `ERR`→`200` עובר · `ERR`→`404` מפיל.

**לפני שמכריזים "ה-CI נשבר בגללי" — לבדוק אם `main` ירוק.** אם כן, והדיף נוגע רק לנתונים, זו כמעט תמיד זמינות של צד שלישי.

### HTML safety:
- כל ערך ב-innerHTML → `esc(value)` בלי יוצא מן הכלל
- onclick עם data → `window._mapNavPlace` pattern, לא inline strings
- `"` בשדה JS string ('בסופ"ש', 'מ"ר') — תקין ב-JS, אבל מסוכן ב-HTML attribute

---

## פיצ'רים קיימים (גרסה 10 — 2026-07-18)

### תגי "כבר בלוז" — כיסוי מלא
- כל טאב (community, restaurants, shopping, reminders, photo, desserts) — תג ירוק + כפתור "✏️ עדכן"
- טאב מיקום — תג ✅ ברשימת הקרובים + ב-showMapNavDialog
- מבוסס על `findItemInDays(name)` — live מ-Firestore

### Swipe ימינה/שמאלה
- `initDaySwipe()` IIFE — Pointer Events API (חסין מ-iOS Safari draggable)
- listeners על `document`, מסנן `section#days`, מוציא `#dayButtons`
- threshold: `|dx| >= 40` ו-`|dx| > |dy| * 1.2`
- `pointercancel` מבצע סווייפ

### חצי ניווט ‹ › בין ימים
- מתעמם opacity ביום ראשון/אחרון
- משלים את הsswipe (לא מחליף)

### Fallback לנתוני Firestore ישנים
```javascript
var _fb = COMMUNITY.find(c=>c.name===s.name) || RESTAURANTS.find(r=>r.name===s.name) || {};
```
- `renderDays()` + `showStopDetail()` — fallback ל-google/duration/who אם stop ישן

### תיקון תקציב
- `saveTotalBudget()` כותב ל-`appdata/main.total_budget` (+ `appdata/budget.total` legacy)
- `saveBudgetFromModal()` קורא תמיד ל-`saveTotalBudget()` גם בweb
- realtime listener מקשיב ל-`total_budget`

### כניסה — OTP מספרי (2026-08-07, כולל תיקון Firestore custom token)
**לא magic link.** קוד 6 ספרות שהמשתמש מקליד — אין תלות בלחיצה על קישור/בדפדפן ששלח את הבקשה.
- `app.html`: `sendLoginCode()` → `_gasJsonp('sendOtpCode',[email],cb)` → GAS שולח מייל עם קוד ומחזיר `{ok}` → מציג `#loginOtpForm`
- `verifyLoginCode()` → `_gasJsonp('verifyOtpCode',[email,code],cb)` → אם `ok && token` → `firebase.auth(_fbApp).signInWithCustomToken(token)` → `_setDeviceRemembered()` → `_resyncLocalDataToCloud()` → `_showApp()`
- `_gasJsonp()` — helper JSONP כללי (script tag + callback global + timeout 20s), משתמש ב-`GAS_URL` המוגדר ליד `triggerReminderEmail` (מאותחל לפני שהמשתמש יכול ללחוץ כפתור — סדר טעינת `<script>` tags)
- **בדיקת המיילים המורשים היא בצד שרת בלבד** (`FAMILY_EMAILS` ב-`Code.gs`) — הקליינט לא מכיל רשימת מיילים (הוסרה ב-2026-08-07 מטעמי אבטחה, ראה "אבטחה" למטה); בצד קליינט נבדק רק פורמט מייל
- שרת: `sendOtpCode`/`verifyOtpCode` ב-`Prague-2026-backend/gas_project/Code.gs` — קוד + תפוגה (10 דק') ב-`PropertiesService`, חד-פעמי (נמחק אחרי אימות מוצלח), נחשף דרך `dispatch_()` הקיים
- GAS mode (`isGas()`) לא מושפע — מדלג על כל מסך הכניסה כרגיל, ולא נוגע ב-Firestore בכלל (`initRealtimeSync()` מתחיל ב-`if (isGas()) return;`)
- **הוחלף**: הגישה הקודמת (Firebase `sendSignInLinkToEmail`/`signInWithEmailLink`, magic link) הוסרה לגמרי — הייתה שבירה כשהקישור נפתח בדפדפן/מכשיר אחר (למשל in-app browser של Gmail בנייד, ללא גישה ל-localStorage של הבקשה המקורית)

#### באג קריטי שהתגלה + תוקן (2026-08-07): OTP "עבד" אבל שום דבר לא נשמר
`verifyOtpCode` המקורי החזיר רק `{ok:true}` בלי ליצור session אמיתי ב-Firebase Auth. `ensureFirebaseAuth()`
(שכל פונקציית שמירה/טעינה ב-app.html עוברת דרכה) נופלת ל-`signInAnonymously()` כשאין `currentUser` —
וה-Firestore Rules דוחות anonymous auth (**אומת ישירות מול הפרויקט**: `signInAnonymously` + ניסיון קריאה
מ-`appdata/main` → `403 PERMISSION_DENIED`). בזרימת ה-magic-link הישנה זה עבד כי `signInWithEmailLink`
יצר `currentUser` אמיתי לפני שכל שאר האפליקציה הגיעה ל-`ensureFirebaseAuth()`. ה-OTP מעולם לא נגע
ב-Firebase Auth SDK כלל — כניסה "הצליחה" (מסך התחלף) אבל כל כתיבה ל-DB נכשלה בשקט.

**התיקון**: `verifyOtpCode` המוצלח יוצר עכשיו Firebase custom token (JWT חתום RS256 עם
`Utilities.computeRsaSha256Signature`, פונקציה `createFirebaseCustomToken_()` ב-`Code.gs`) ומחזיר
`{ok:true, token}`. הקליינט קורא `signInWithCustomToken(token)` לפני `_showApp()` — כניסה אמיתית,
לא anonymous. דורש Script Property `FIREBASE_SERVICE_ACCOUNT_JSON` ב-GAS (ראה
`Prague-2026-backend/CLAUDE.md` → "Firebase custom token").

#### המשך הבאג: session אנונימי ישן נתקע (PR #61, 2026-08-07)
גם אחרי תיקון ה-custom token, מכשירים שכבר היו להם דגל session ישן **דילגו על מסך הכניסה**
והמשיכו עם ה-session האנונימי השבור — צילום מסך מהמשתמש הראה `Auth user: kMEne5AoXeRny...`
(UID אנונימי, לא מייל) + `Missing or insufficient permissions`. שני תיקונים:
1. **`ensureFirebaseAuth()` כבר לא קורא ל-`signInAnonymously()` בכלל.** הוא רק בודק
   `currentUser && !currentUser.isAnonymous`, אחרת מחזיר `false`. הנפילה ל-anonymous רק ייצרה
   "הצלחה למראה" שנכשלת בשקט בכל כתיבה. **אל תחזירו את ה-fallback הזה.**
2. ה-IIFE בטעינת העמוד מוודא עם `onAuthStateChanged` ש-`user && !user.isAnonymous` לפני
   `_showApp()`. אם anonymous/אין user → מנקה את הדגל + `signOut()` → מסך כניסה מחדש.

#### `_resyncLocalDataToCloud()` (PR #61)
נקראת מ-`verifyLoginCode()` אחרי `signInWithCustomToken` מוצלח. דוחפת לענן את **כל** מה שנצבר
ב-localStorage בזמן שה-session היה שבור (כל כתיבה נכשלה בשקט): קוראת לפונקציות השמירה הקיימות
עצמן — `saveLocal` (הוצאות), `saveDaysState`, `savePackLocal`, `saveRemindersDoneLocal`,
`saveSchedules`, `saveTotalBudget`, `saveBudget` — כל אחת ב-`try/catch` נפרד.
**⚠️ מגבלה ידועה**: חלק מהכתיבות הן `set()` בלי `merge` — אם כמה מכשירים צברו נתונים מקומיים
שונים, מי שמתחבר אחרון דורס. לא נוצר כאן, אבל שווה לדעת.

### הרובע היהודי (Josefov) — 5 אטרקציות (2026-08-07)
`בית הכנסת הגדול — Staronová synagoga` (היה קיים) + 4 שנוספו: `בית העלמין היהודי העתיק — Josefov`, `בית הכנסת הספרדי — Španělská`, `בית הכנסת פינקס — אנדרטת השואה`, `בית הכנסת מייזל — Maiselova`. כולם `type:'community'`, `cat:'🎟️ אטרקציות'`, עם קואורדינטות ב-`PLACE_COORDS`.

**לקח לחיפוש עתידי**: המשתמש חשב שאין תוכן על הרובע — בפועל היה, אבל **השם לא הכיל "רובע יהודי"** (הוא נקרא ע"ש בית הכנסת). לפני שמסיקים "אין X באפליקציה", להריץ את לוגיקת הפילטר בפועל (`type==='community' && cat.indexOf('אטרקציות')` + בדיקת `PLACE_COORDS[name]`) ולא לחפש לפי שם בלבד — פריט בלי קואורדינטות נעלם מהמפה ומהרשימה בשקט.

**עוד ברובע (נוספו 2026-08-07)**: `בית העירייה היהודי — השעון שהולך אחורה` (שעון עברי נגד כיוון השעון, 1764 — **חינם**, צפייה מבחוץ), `פסל פרנץ קפקא — Vězeňská` (רונה 2003 — **חינם**; אבנים שחורות במדרכה = צורת חרק, רמז ל"גלגול"), ושתי מסעדות כשרות שחסרו: `King Solomon — כשר` (הוותיקה בצ׳כיה, מרתף, ליל שבת) ו-`Dinitz — כשר ישראלי` (קליל וזול יותר).

### שווקים (2026-08-10)
3 שווקים, **הימים קריטיים** כי הטיול 8–15/8/2026 מתחיל ונגמר בשבת:
- `שוק האיכרים Náplavka — שבת בלבד` (Rašínovo nábřeží) — 08:00–14:00, **שבת בלבד**. בטיול הזה: רק 8/8 (נחיתה) או 15/8 (חזרה) — תלוי בשעות טיסה
- `שוק האיכרים Jiřák — רביעי–שבת` (nám. Jiřího z Poděbrad) — ד׳–ו׳ 08:00–18:00, ש׳ 08:00–14:00. **החלופה המעשית** — פתוח 12–14/8
- `Pražská tržnice — השוק הגדול (הולשוביצה)` — המתחם הגדול בעיר. Hala 22 (שוק הירקות): ב׳–ו׳ 07:30–17:00, ש׳ עד 14:00, **א׳ סגור**. 🎬 קולנוע קיץ יולי–ספטמבר (חופף לטיול). תחבורה: מטרו C→Vltavská +5 דק׳, או טראם 6/12/14/17/25 לתחנה "Holešovická tržnice"
- `Havelské tržiště — השוק העתיק בפראג` (Havelská) — פתוח כל יום, גיבוי בטוח. `cat:'🛍️ קניות'` (האחרים `'🎟️ אטרקציות'`)

**⚠️ מחירי תחבורה ציבורית — עודכנו 1/1/2026** (היו מיושנים באפליקציה, פער ~30%): 30 דק׳ **39** CZK (36 באפליקציית PID Lítačka), 90 דק׳ **50** (46), יומי 24ש׳ **150** (140), ילד עד 15 חינם. SMS הכי יקר. **לבדוק שוב אם עובדים על התקציב** — התעריף עלה משמעותית ב-2026.

**לקח**: כשמוסיפים מקום עם שעות/ימים מוגבלים — להצליב מול תאריכי הטיול בפועל (`python3 -c "import datetime; ..."`) ולכתוב את המסקנה ב-`tips`. שוק שפתוח רק בשבת הוא חסר ערך אם לא אומרים שהשבתות היחידות הן ימי הטיסה.

### מעקב מיקום חי (2026-08-10)
`locateMe()` הוא **toggle** על `watchPosition` (לא `getCurrentPosition` חד-פעמי). `stopLocationWatch()` עוצר.
- **נעץ יחיד** `_userMarker` שממוקם מחדש + `_userCircle` (רדיוס דיוק). **הבאג הישן**: כל לחיצה הוסיפה נעץ חדש בלי להסיר → נעצים נערמו.
- **throttle לרשימה**: `renderNearbyList()` בונה `innerHTML` מלא, אז רצה רק אם עברו 3 שניות **או** תזוזה >15 מ׳. הדיאלוג זול (שורה אחת) אז מתעדכן בכל טיק דרך `_refreshOpenDialogDistance()` → דורש `id="mapNavDistRow"` + `window._mapNavCoords`.
- **עצירה אוטומטית** (סוללה): ביציאה מטאב מיקום (`openTab`) וב-`visibilitychange`.
- **⚠️ ב-`openTab` הבדיקה היא `_geoWatchId != null` ולא `!==`** — var hoisting עושה אותו `undefined` אם `openTab` ירוץ לפני שורת ההשמה, ו-`undefined !== null` היה נכנס לשווא.
- נבדק בסימולציה: 20 טיקים בהליכת ~100 מ׳ → 7 רינדורים (לא 20); 30 טיקים בעמידה → 0.

### ⚠️ כלל: פיצ'ר תלוי-GPS לא נעלם בשקט (2026-08-10)
בדיאלוג הנעץ שורת המרחק הייתה `if (_userLat !== null)` בלבד — בלי מיקום היא פשוט לא רונדרה, והמשתמש בשטח לא הבין למה אין מרחק. **תמיד להוסיף ענף `else` עם כפתור שמפעיל את הפיצ'ר**, לא להשאיר ריק.
`_locateForDialog(btn)` — מבקש מיקום פעם אחת, ממלא את `#mapNavDistRow` **במקום** (בלי לסגור/לפתוח דיאלוג) + מרענן את רשימת הקרובים ברקע. בכישלון: משחזר את הכפתור (לא נשאר "⌛ מאתר…") ומציג הודעה.
`_refreshOpenDialogDistance()` מחילה גם `display:flex` — ה-div של ה-fallback הוא רק `margin`, ובלי זה התגים נדבקים.

### כפתור מפה בכרטיסי תחנות (טאב ימים, 2026-08-10)
`openStopMapDialog(name)` — פותח את **הדיאלוג המלא** (`showMapNavDialog`), לא toast, כי הוא כבר כולל מרחק+כיוון+ניווט.
- מטפל בשני מקרים: תחנה שקיימת ב-`ALL_PLACES` (מידע עשיר), ו**תחנה מותאמת אישית** שהמשתמש הוסיף (בונה אובייקט מינימלי מה-stop עצמו — אחרת הדיאלוג לא היה נפתח בכלל).
- `event.stopPropagation()` חובה — לכרטיס עצמו יש `onclick="showStopDetail(...)"`.

**⚠️ אי-התאמת שמות בין `DAYS` ל-`ALL_PLACES`**: `PLACE_COORDS` ממופתח לפי **שם מדויק**, ושמות התחנות ב-`DAYS` שונים לפעמים ("ארוחת ערב — Gran Fierro" מול "Gran Fierro"). התוצאה: רק 7/29 תחנות קיבלו כפתור. נוספו 6 **כינויים** ב-`PLACE_COORDS` (אותן קואורדינטות של הרשומה המקורית) → 13/29.
**כשמוסיפים תחנה חדשה ל-`DAYS` — לוודא שהשם קיים ב-`PLACE_COORDS`, או להוסיף כינוי.**

### השלמת קואורדינטות — 29/29 תחנות (2026-08-10)
נסגר הפער שנשאר: **כל 29 שמות התחנות ב-`DAYS` נמצאים ב-`PLACE_COORDS`** (וגם 98/98 ב-`ALL_PLACES`; סה"כ 126 מפתחות, בלי כפילויות).

**איך אומתו** (כלל הזהב: לא ממציאים — ראה "לקח מ-2026-08-07"):
| תחנה | קואורדינטות | מקור |
|------|-------------|-------|
| Comfort Hotel Prague City East | `[50.0763, 14.4937]` | דף "צור קשר" הרשמי: 50°4'34.829"N 14°29'37.250"E, **מאומת שנית** מול Wikidata `Q98398369` (50°4'34.61"N 14°29'36.92"E) — פער ~7 מ׳ |
| כיכר העיר העתיקה והשעון | `[50.0870, 14.4209]` | ויקיפדיה 50°05′13.23″N 14°25′15.30″E |
| טירת פראג וקתדרלת ויטוס | `[50.0908, 14.4003]` | ויקיפדיה 50°05′27″N 14°24′02″E (זהה ל-`מצודת פראג` הקיים) |
| אי קמפה | `[50.0850, 14.4083]` | ויקיפדיה 50°05′06″N 14°24′30″E |
| Čertovka — ונציה של פראג | `[50.0863, 14.4081]` | נקודת גלגל המים, ~130 מ׳ צפונית למרכז קמפה |
| ממלכת הרכבות — Království železnic | `[50.0713, 14.4025]` | 50°4'16.765"N 14°24'9.124"E, Stroupežnického 23 |

**תחנות "פעולה" שאין להן כתובת משלהן** קיבלו את **עוגן** המיקום ההגיוני שלהן, כך שגם להן יש כפתור "📍 ממני":
- מלון (סיבוב באזור / חזרה ומנוחה ×2 / ארוחת בוקר אחרונה / צ'ק-אאוט) → קואורדינטות המלון
- "סמטאות העיר העתיקה" → כיכר העיר העתיקה · "Mala Strana — שכונת הברוק" → `רחובות מלה סטרנה`
- "קפה אחרון וארוחת סיום" → Gran Fierro (**לפי ה-`mapUrl` של התחנה עצמה**, לא ניחוש)
- "יציאה ל-Aquapalace בבוקר" → `Aquapalace Prague` · "הליכה דרך Na Příkopě" → `שדרת Na Příkopě`

⚠️ מפתח עם גרש (`"צ'ק-אאוט ויציאה לשדה"`) חייב מרכאות כפולות — אחרת שגיאת syntax שמשביתה את כל הסקריפט.
⚠️ הוספת מפתחות DAYS ל-`PLACE_COORDS` **לא** מוסיפה נעצים למפה — `addMapMarkers()`/`renderNearbyList()` רצות על `_uniquePlacesForMap()` (כלומר `ALL_PLACES`), לא על מפתחות `PLACE_COORDS`.

**שיטת האימות כשה-egress חסום**: ב-session הזה `nominatim`, `api.openstreetmap.org`, `wikidata.org`, `mapcarta`, `photon.komoot.io` וגם אתר המלון — **כולם חסומים ל-WebFetch**. מה שכן עבד: `WebSearch` (מסכם את תוכן הדפים). שאילתה שמכילה את פורמט הקואורדינטות עצמו (`"50°4" "14°29" GPS`) מוציאה את המספרים מה-snippet גם כשהדף עצמו לא נגיש.

### תיקון עובדתי — מיקום המלון (2026-08-10)
`details` של תחנת המלון טען **"רובע Žižkov"** ו-"רחוב Seifertova ליד המלון" — **שגוי**. הכתובת האמיתית: `Bečvářova 2081/14, Strašnice, Praha 10`; חשמלית **Solidarita** צמודה למלון (קווים 7/11), מטרו **Strašnická** (קו A) במרחק הליכה. תוקן בשתי התחנות.
כמו כן הוסר הטיפ "הבריכה פתוחה 07:00–22:00" — **אין בריכה במלון**; יש חדר כושר 24/7, חדר משחקים ומועדון ילדים.

### כפתור "📍 ממני" בכרטיסים (2026-08-10)
`showDistanceTo(placeName)` — toast עם כיוון+מרחק+זמן הליכה, זמין מ**כל טאב** (לא רק מיקום).
- מוצג בכרטיסי `renderCommunity` וגם `renderRestaurants`, **רק אם `PLACE_COORDS[name]` קיים** (בפועל כיסוי מלא: 56/56 קהילה, 26/26 מסעדות).
- משתמש ב-`getCurrentPosition` (לא `watchPosition`) — בדיקה נקודתית, לא מעקב. התוצאה נשמרת ב-`_userLat/_userLng` אז לחיצות הבאות מיידיות; `maximumAge:60000` מאפשר קריאה אחרונה במקום fix חדש.
- **שם המקום עובר ב-`data-pn` ולא inline string** — לפי הדפוס הקיים נגד שבירת `onclick` על מרכאות בשם (אותה מלכודת של `_mapNavPlace`).

### כיוון יחסי למיקום (2026-08-10)
`bearingTo(lat1,lng1,lat2,lng2)` — forward azimuth על ספירה, מחזיר 0–360° (0=צפון, 90=מזרח).
`compassFor(deg)` → `_COMPASS[Math.round(deg/45)%8]` → `[חץ, שם בעברית]`.
מוצג ב-**שני** מקומות (לשמור על עקביות אם משנים): כרטיס ב-`renderNearbyList` (תג `dirHtml` ליד המרחק), ובראש `showMapNavDialog` (שורת כיוון+מרחק+זמן הליכה).
**מותנה ב-GPS**: רק אם `_userLat !== null && _userLng !== null` — אחרת לא מוצג כלל.
**מגבלה מכוונת**: החץ מצביע לכיוון גאוגרפי (⬆️=צפון), לא לכיוון שאליו המכשיר מופנה. בוסולה אמיתית תדרוש `deviceorientation` + הרשאה נפרדת ב-iOS.
נבדק מול 5 כיווני יסוד + מקרה אמיתי (מלון→רובע יהודי = 292.8° צפון-מערבה ✓).

**✅ תוקן (2026-08-07) — נעצים כפולים במפה**: 15 מקומות מופיעים פעמיים ב-`ALL_PLACES` (פעם `type:'restaurant'`/`'shop'` ופעם `type:'community'` עם `cat:'🍽️ מסעדות'`/`'🛍️ קניות'`), שניהם עם אותו `name` → שני נעצים זהים זה על זה + כרטיס כפול ברשימת הקרובים.

**הפתרון — `_uniquePlacesForMap()`** (~שורה 4876), de-dupe **בשכבת התצוגה בלבד**:
- **⚠️ אסור להסיר את הכפילויות מ-`ALL_PLACES`** — `renderCommunity()` מרנדר את רשומות ה-community לפי `cat`, והן ייעלמו מטאב "קהילה" (52 פריטים). זו הסיבה שה-de-dupe לא נעשה במקור הנתונים.
- **ממזג שדות** משתי הרשומות במקום לבחור אחת: `price`/`badge`/`level` מ-restaurant, `how`/`booking` מ-community. ה-`type` של הרשומה עם יותר שדות מלאים קובע את הרינדור ב-`showMapNavDialog`.
- נוסף **fallback גנרי** ב-`showMapNavDialog` שמציג `how`/`booking` כש-`type !== 'community'` — אחרת המידע הממוזג לא היה מוצג כלל.
- **`renderNearbyList` ו-`_openNearbyDialog` מאנדקסים לתוך הרשימה המאוחדת, לא `ALL_PLACES`** — `ALL_PLACES.indexOf(p)` על אובייקט ממוזג מחזיר `-1` ומשמיט כל כרטיס. יש מטמון (`_uniquePlacesCache`) כדי שהאינדקסים יהיו יציבים בין קריאות. **אם מוסיפים מקומות דינמית — לאפס את המטמון.**
- אומת: 109 → 94 (בדיוק 15), אפס כפילויות, אפס שמות אבדו, טאב קהילה ללא שינוי.

**מחיר Jewish Museum (מאומת 2026-08-07)**: 600 CZK מבוגר · 400 סטודנט עד 26 · 200 ילד 6–15 · חינם עד 6. תקף 3 ימים רצופים, כל אתר פעם אחת. **סגור בשבתות ובחגי ישראל.** (בקוד היה 500 CZK מיושן — תוקן גם בתזכורת `r2` וגם ב-`booking` של בית הכנסת.)

#### אימות אבטחה — האם ה-DB פתוח לכולם? (2026-08-07, נבדק אמפירית)
נבדק בפועל מול הפרויקט החי, לא בהנחה:
1. הרשמה עם `accounts:signUp` (email/password, מייל אקראי לא-משפחתי) עם ה-`apiKey` הפומבי → **הצליחה**. כלומר ספק email/password פעיל וכל אחד יכול ליצור חשבון Auth.
2. קריאה מ-`appdata/main` עם הטוקן הזה → **`403 PERMISSION_DENIED`**
3. כתיבה ל-`appdata/sectest_probe` עם הטוקן הזה → **`403 PERMISSION_DENIED`**
4. חשבון הבדיקה נמחק (`accounts:delete`) ואומת שנמחק.

**מסקנה**: ה-Rules לא בודקות `request.auth != null` (זה היה מתיר כל חשבון) אלא משהו צר יותר שרק ה-custom token של ה-GAS עומד בו. **אל תשנו את ה-Rules ל-`if request.auth != null`** — זה יפתח את כל ה-DB לכל מי שמכיר את ה-apiKey (שהוא פומבי ב-`app.html`).
**שיפור אפשרי (לא בוצע)**: לכבות את ספק email/password ב-Firebase Console — הוא לא בשימוש בכלל (האפליקציה משתמשת רק ב-custom tokens), וכיבויו ימנע יצירת חשבונות זבל בפרויקט.

#### כפתור התנתקות (2026-08-07)
`doLogout()` — כפתור "🚪 התנתק מהמכשיר" בטאב הראשי. מוחק `localStorage[SESSION_KEY]` + `firebase.auth().signOut()` + `location.reload()`. **לא מוחק נתונים** (לא מקומית ולא בענן) — רק מנתק גישה. עם `confirm()` נגד לחיצה בטעות.

#### זכירת מכשיר ל-7 ימים (PR #62)
`SESSION_KEY` עבר מ-`sessionStorage` (נמחק בכל סגירת טאב → OTP חדש כמעט בכל פתיחה)
ל-`localStorage` עם **timestamp של תפוגה** (`Date.now() + SESSION_TTL_MS`, שבוע):
`_setDeviceRemembered()` / `_isDeviceRemembered()`. הדגל לבדו אף פעם לא מספיק — תמיד מאומת
מול Firebase Auth (`onAuthStateChanged`) לפני דילוג על מסך הכניסה.

### PLACE_IMGS — 35+ תמונות
גשר קארל, לטנה, ויישהראד, מנזר סטרהוב, מגדל פטז'ין, גן ולנשטיין, חומת לנון, גן חיות, Aquapalace, Westfield Chodov, Café Savoy, U Fleků, ועוד.
