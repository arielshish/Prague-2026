# Prague 2026 — משפחת שיש (repo ציבורי — GitHub Pages בלבד)

> עדכון אחרון: 2026-08-07 (גרסה 11 — OTP מספרי במקום magic link)

## ⚠️ פנייה למשתמש — לשון זכר בלבד

**המשתמש (אריאל) הוא זכר. כל פנייה אליו — לשון זכר, בלי יוצא מן הכלל.**

זה נכשל **פעמיים** ב-11/08 (`לחצי`, `ותוודאי`), והמשתמש תיקן במפורש. בעברית המין דקדוקי וכפוי על כל פועל בגוף שני, ולכן אין "ברירת מחדל ניטרלית" — כל משפט הוא הכרעה, ו-slip אחד בולט מיד.

| ❌ לא | ✅ כן |
|------|------|
| לחצי · תוודאי · תגידי · תבחרי · שימי לב | לחץ · תוודא · תגיד · תבחר · שים לב |
| רוצה שאוסיף? (זהה בשני המינים — בסדר) | — |

**מוקדי סיכון**: משפטי סיום והוראות פעולה ("לחץ על משוך גרסה עדכנית"), שם נוטה להיכנס נקבה. **לסרוק כל פועל בגוף שני לפני שליחה.**

⚠️ **זה נפרד מטקסט ה-UI באפליקציה** — שם קוראים גם בני משפחה אחרים, ולכן טקסט האפליקציה יכול להיות ניטרלי/רבים. הכלל הזה חל על **השיחה עם אריאל**.

זהו repo **ציבורי בכוונה** (חייב להישאר public כדי ש-GitHub Pages יעבוד בחשבון חינמי).

## 🚨 פער ידוע: עריכות `DAYS` בקוד **לא מגיעות** למכשיר עם לוז שמור (2026-08-12)

```js
DAYS_STATE = saved || JSON.parse(JSON.stringify(DAYS));   // getDaysState()
```

**`saved` תמיד גובר.** ברגע ש-`prague_days_v1` קיים במכשיר — וזה המצב אצל המשפחה, כי הלוז מסונכרן מהענן — **כל שינוי ב-`var DAYS` שבקוד הוא קוד מת עבורם.**

נמדד ב-12/08 בהשוואת שני מכשירים:

| | מכשיר נקי (בלי לוז שמור) | מכשיר עם לוז שמור |
|---|---|---|
| Pizza & Pasta ביום 3 | ✅ מופיע | ❌ **לא מופיע** |
| Manifesto ביום 5 | ✅ מופיע | ❌ **לא מופיע** |
| Koh-i-noor ביום 7 | ✅ מופיע | ❌ **לא מופיע** |

**מה כן מגיע**: `ALL_PLACES`, `PLACE_COORDS`, כרטיסי קהילה/מסעדות/קניות, `how`/`booking`/`tips` של רשומות מקום, ולוגיקה. אלה נקראים מהקוד ישירות ולא ממצב שמור.

**מה לא מגיע**: כל מה שבתוך `DAYS` — הוספת תחנה, הסרה, שינוי `details`/`tips` של תחנה קיימת, ו-`meals`.

⚠️ **המשמעות המעשית**: שינוי לוז חייב להתבצע **דרך ה-UI של האפליקציה** (הוסף/הזז/ערוך תחנה), או שצריך מנגנון מיגרציה שמזהה גרסת לוז וממזג. עריכה ב-`app.html` לבדה **לא מספיקה** ותיראה כאילו "לא נפרסה".

## שיטת העבודה (אושרה ע"י המשתמש 2026-08-12)

הדפוס שהוכיח את עצמו בתקלת מחיקת הסימונים, ושיש להחיל על כל משימה:

**1. לא לנחש — למצוא את המנגנון בקוד.** "כנראה בגלל סנכרון" הוא לא אבחון. `grep` על נתיב הנתונים עד שרואים את השורה שגורמת לזה. בתקלה הזו זה היה `if (d.visited)` על מחרוזת `"{}"`.

**2. לאמת אמפירית, לא להצהיר.** להריץ את הקוד ולמדוד — Playwright, סקריפט, כלי חיצוני. "אמור לעבוד" אינו אימות. ייצוא CSV נבדק ע"י הרצת הכפתור והשוואה לחישוב עצמאי ב-Python, לא ע"י קריאת הקוד.

**3. להגיד במפורש מה לא אומת.** אזור תעריפי 1 או 2, מחיר מדויק, נוכחות Pepco — כשלא אומת, זה נכתב ככה בכרטיס ובתשובה. **עדיף "לא בדקתי" מאשר מספר שנשמע בטוח.**

**4. לתקן את השורש, לא את הסימפטום.** להוסיף דומיין ל-soft list היה סימפטום; ההבחנה בין כשל רשת לקישור שבור הייתה השורש.

**5. לחפש את אותה תקלה במקום נוסף.** אם `visited` נדרס ע"י מכשיר ריק — גם `days` נדרס (`getDaysState` נופל ל-`DAYS`). אם יש טענה על תחבורה בתחנה — יש עוד שתיים בקהילה ובטיפים. **`grep` על הנושא, לא רק על המקום שהמשתמש הצביע עליו.**

**6. להציג עלות נסתרת ולא להכריע בשקט.** 6.7 ק״מ בכיוון ההפוך, שעות סותרות בין מקורות, מרחק מהמסלול — נרשמים בכרטיס עצמו כדי שההחלטה תישאר של המשתמש.

**7. לא לחרוג ממה שהתבקש.** כשנאמר "רק שחזור" — לא נגעתי בקוד בכלל.

**8. סדר קבוע:** `git tag` גיבוי → שינוי → אימות אמפירי → commit + push → PR → CI ירוק → merge → **עדכון CLAUDE.md והסקיל** → אימות פריסה.

**9. `BUILD_ID` בכל שינוי מהותי**, עם assert שהמחרוזת הישנה נמצאה.

**10. לשמור העדפות במקום קבוע.** אם המשתמש תיקן משהו — זה נכנס לכאן, לא נשאר בשיחה.

## מה יש כאן, ומה לא

הריפו הזה מכיל **רק** את מה שצריך להיות מוגש דרך GitHub Pages:
- `app.html` — האפליקציה (login screen + Firebase, לגלישה ב-`arielshish.github.io/Prague-2026/app.html`)
- `translator.html` — מתרגם קולי עצמאי
- `robots.txt` — חוסם אינדוקס במנועי חיפוש
- `.github/workflows/pages.yml` — בונה תיקיית `_site` זמנית עם רק שלושת הקבצים האלה ופורס אותה (**לא** את כל הריפו)

**כל השאר עבר ל-repo פרטי:** `arielshish/Prague-2026-backend` — כולל `gas_project/Code.gs` (הבאקאנד של GAS), `sync_gas.py`, `_archive/` (גרסאות ישנות), ותיעוד מלא (מיילים משפחתיים, Spreadsheet ID, Deployment ID). זה עבר כי הריפו הזה חייב להישאר public.

## מפת הפיצ'רים הקיימים ב-app.html

> עדכון אחרון: 2026-08-07 (גרסה 11 — OTP מספרי במקום magic link)

### מבנה נתונים מרכזי

#### ALL_PLACES[] — מקור יחיד לכל האטרקציות (2026-07-18)
```
ALL_PLACES[] — פריטים עם שדה type:
  type:'shop'       → {icon, name, stars, hours, metro, duration, brands, tip, mapUrl}
  type:'restaurant' → {icon, name, level, sub, desc, price, badge, badgeClr, badgeTxt, google, mapUrl}
  type:'dessert'    → {icon, name, level, sub, desc, price, rating, tag, tagClr, where, mapUrl}
  type:'community'  → {icon, name, cat, fb, google, desc, booking, duration, tips, who, how, mapUrl}
  type:'photo'      → {icon, name, fee, sub, desc, best, crowds, fee_txt, rating, tip, mapUrl}
  type:'exchange'   → {icon, name, stars, hours, metro, duration, brands, tip, mapUrl} (2026-08-07, אותו schema כמו shop)
```
`type:'exchange'` — נעצי "המרת כסף בטוחה" בטאב מיקום בלבד (אין לו computed view/טאב ייעודי, כמו photo). כל מקום שרוץ על ALL_PLACES/TYPE_GRAD/catColor/typeColor/typeLabel חייב לכלול ערך ל-`exchange` (ראה `renderNearbyList`, `addMapMarkers`, `showMapNavDialog` ב-app.html) — **רק מקומות שאומתו בפועל (דירוג גוגל/ביקורות עקביות משני מקורות לפחות)** — לא להוסיף דוכן רק כי הוא "נשמע בסדר" במקור בודד.

**⚠️ לקח מ-2026-08-07**: המשתמש העלה שני קבצי `.docx` שנוצרו ב-ChatGPT עם "רשימות מאומתות" של דוכני חליפין (כולל "דוח אימות" עם מתודולוגיה נשמעת-רצינית). **שני הקבצים הכילו טעויות עובדתיות** — כולל אחת שכבר תפסתי בעצמי בחיפוש עצמאי (כתובת שנסגרה ב-2024, שהקובץ טען שעדיין פעילה). מסמך שנוצר ע"י LLM אחר, גם עם "מתודולוגיית אימות" מפורטת, **הוא לא מקור — הוא claim שצריך לאמת בעצמך** בדיוק כמו כל מקור בודד אחר. לעולם לא להטמיע רשימות "מאומתות" כאלה בלי לחפש כל שם+כתובת בעצמך.
**Computed views** (נגז��ות מ-ALL_PLACES, read-only):
```javascript
var COMMUNITY   = ALL_PLACES.filter(p => p.type==='community');
var RESTAURANTS = ALL_PLACES.filter(p => p.type==='restaurant');
var SHOPS       = ALL_PLACES.filter(p => p.type==='shop');
var DESSERTS    = ALL_PLACES.filter(p => p.type==='dessert');
var PHOTO_SPOTS = ALL_PLACES.filter(p => p.type==='photo');
```
**נשארים נפרדים** (לא חלק מ-ALL_PLACES):
- `REMINDERS[]` — משימות לפני הטיול לסימון ✅
- `DAYS[]` — לוז בסיס ראשוני (מחליפו `DAYS_STATE` מ-Firestore)

#### שדות תזמון
- `getDaysState()` — מח��יר את מצב הימים הנוכחי (Firestore / local)
- `findItemInDays(name)` — מחזיר `{dayNum, dayTitle, time}` אם תחנה נמצאת ביום כלשהו, אחרת `null`

### טאבים ראשיים
| טאב | פונקציית render | תיאור |
|-----|----------------|--------|
| ימים (`days`) | `renderDays()` ~3001 | ציר זמן של תחנות יומיות |
| קהילה (`community`) | `renderCommunity()` ~3649 | בנק אטרקציות לפי קטגוריה |
| מסעדות (`restaurants`) | `renderRestaurants()` ~3794 | מסעדות, קינוחים, נקודות צילום |

### פיצ'רים מיושמים

#### ציוני Google + משך + מתאים ל (2026-07-18)
- כל תחנה ב-DAYS מציגה ⭐ ציון Google, משך ביקור, ו"מתאים ל" (who)
- `showStopDetail()` popup מציג chips של ציון + who
- `saveAddCommunityStop()` מעביר אוטומטית google/duration/who מ-COMMUNITY ל-DAYS

#### תג "כבר בלוז" (2026-07-18)
- `renderCommunity()`: אם פריט כבר ב-DAYS → תג ירוק **"✅ יום X · שעה"** + כפתור משתנה ל-**"✏️ עדכן"**
- `renderRestaurants()`: תג זהה אם מסעדה קיימת ב-DAYS
- `renderReminders()`: schedInfo ירוק "✅ יום X · שעה" אם ב-DAYS
- `renderPhotoSpots()` / `renderDesserts()`: כפתור ➕ מוחלף בתג ✅
- `buildBankCards()`: כרטיסי בנק מורחבים עם תיאור, ⭐, משך, תג הזמנה, ותג "כבר בלוז"
- מבוסס על `findItemInDays()` — תמיד live מ-Firestore

#### תג "כבר בלוז" — קניות (2026-07-18)
- `renderShopping()`: תג ירוק "✅ יום X · שעה" + כפתור "✏️ עדכן" כשחנות כבר ב-DAYS
- `openTab('shopping')` + `refreshScheduleBadges()` מכסים גם shopping

#### סינכרון תגים בזמן אמת (2026-07-18)
- `saveDaysState()` קוראת ל-`refreshScheduleBadges()` — מרנדרת מחדש את הטאב הפעיל מיד עם כל שמירה
- `openTab('community')` מרנדר `renderCommunity()` — תגים תמיד עדכניים בכניסה לטאב
- כיסוי מלא: הוסף → תג מופיע מיד; מחק/הזז → תג נעלם מיד בכל טאב פעיל

#### Swipe ימינה/שמאלה בין ימים (2026-07-18)
- `initDaySwipe()` IIFE — listeners על `document` (לא `#dayCard`) לעקיפת `draggable` ב-iOS Safari
- `touchmove` מעדכן מיקום אחרון; `touchcancel` מבצע סווייפ (לא מבטל)
- threshold: `|dx| >= 40` ו-`|dx| > |dy| * 1.2`

#### Fallback לנתוני Firestore ישנים (2026-07-18)
- `renderDays()` ו-`showStopDetail()` — fallback ל-COMMUNITY/RESTAURANTS אם stop חסר google/duration/who

#### חצי ניווט ימים (2026-07-18)
- ‹ ו-› משני צידי שורת `#dayButtons` — ניווט מהיר בין ימים
- `renderDays()` מעדכן opacity/pointerEvents — מתעמם ביום ראשון/אחרון
- משלים את ה-swipe (לא מחליף)

#### תיקון סנכרון תקציב (2026-07-18)
- `saveTotalBudget()` כותב גם ל-`appdata/main.total_budget` (+ `appdata/budget.total` לתאימות)
- `saveBudgetFromModal()` קורא ל-`saveTotalBudget()` גם בסביבת web (קודם רק GAS)
- realtime listener (`appdata/main` onSnapshot) מקשיב ל-`total_budget` ומעדכן `renderBudget()` מיידית
- הוסרה קריאה מתה ל-`appdata/budget.categories` ב-`syncBudgetFromFirebase()`

#### דיאלוג מפה עשיר — Rich Map Dialog (גרסה 10)
- `showMapNavDialog(p, coords)` (~שורה 4644) — scrollable bottom sheet בלחיצה על כל pin/כרטיס
- header: icon + שם + תג ✅ "כבר בלוז · יום X" אם ב-DAYS
- מידע לפי type: community (desc/duration/google/who/how/booking/tips) | restaurant (badge/sub/desc/price/google) | dessert (sub/desc/tag/rating/price/where) | shop (stars/hours/metro/duration/brands/tip) | photo (sub/desc/rating/best/crowds/fee/tip)
- **תיקון קריטי (גרסה 10)**: `tips` community items הוא **string** לא array — שמירה מלאה עם `Array.isArray` guard. בלי זה: `p.tips.forEach` זורק TypeError ושום דיאלוג לא נפתח.
- **תיקון קריטי (גרסה 10)**: כפתור "הוסף ללוז" בדיאלוג משתמש ב-`window._mapNavPlace` — לא inline data — מונע שבירת onclick בגלל `"` בתוך desc/name (כמו `מ"ר`, `בסופ"ש`).
- `_openNearbyDialog(apIdx, lat, lng)` (~שורה 4901) — wrapper עם try-catch; `apIdx = ALL_PLACES.indexOf(p)` — אינדקס מספרי, ללא בעיות escaping
- כפתורי ניווט נעוצים בתחתית: Google Maps | Waze | 📍 מפה מקומית | סגור

#### כפתור "משוך גרסה עדכנית" + חותמת גרסה (2026-08-10)
- **הבעיה**: האפליקציה היא קובץ HTML יחיד מ-GitHub Pages עם `apple-mobile-web-app-capable`. כשמוסיפים אותה למסך הבית ב-iOS היא רצה standalone — **בלי סרגל כתובות ובלי כפתור רענון** — ו-Safari מגיש את הגרסה השמורה. תיקון נפרס בהצלחה והמשפחה ממשיכה לראות ישן, בלי שום דרך לדעת שהם על גרסה ישנה
- `BUILD_ID` — מחרוזת שמתעדכנת **ידנית בכל שינוי מהותי**, מוצגת על הכפתור (`_renderBuildStamp()` נקראת מ-`_showApp()`). מאפשרת לדעת במבט אחד איזו גרסה טעונה בטלפון
- `forceUpdate()` — מוסיף `?v=<timestamp>` ל-URL ומנווט מחדש. **`location.reload(true)` לא מספיק** — הפרמטר הוסר מהתקן ורוב הדפדפנים מתעלמים ממנו; URL שונה הוא הדרך היחידה לאלץ הבאה מהרשת
- אין service worker באפליקציה — הבעיה היא מטמון HTTP של Safari, לא cache של SW

#### קורלציה תחנות ↔ מקומות ↔ מיקום (2026-08-10)
- **הבאג**: `addMapMarkers()` ו-`renderNearbyList()` רצו על `_uniquePlacesForMap()` = `ALL_PLACES` בלבד. תחנה שקיימת בלוז אבל לא ב-`ALL_PLACES` **לא צוירה כלל** — גם כשהיו לה קואורדינטות. נמדד: **29 תחנות עם מיקום, 7 בלבד על המפה**. המלון, טירת פראג, כיכר העיר העתיקה, אי קמפה — נעלמו מהמפה ומרשימת "קרוב אליי"
- **`_mappablePlaces()`** — האיחוד: כל `ALL_PLACES` + כל תחנת לוז עם קואורדינטות שאינה כבר ברשימה, מסונתזת כ-`type:'stop'` ומועשרת ב-`_withPlaceDetails`
- ⚠️ **חייב להיות memoized** (`_mappableCache`): כרטיסי הרשימה מעבירים לדיאלוג **אינדקס מספרי** לתוך הרשימה. מערך חדש בכל קריאה היה מחזיר `indexOf === -1` לאובייקט מסונתז והדיאלוג לא היה נפתח. המטמון מבוטל ב-`_invalidateMappable()` מ-`saveDaysState` ומה-listener של הלוז
- ⚠️ **de-dupe לפי קואורדינטה ולא רק לפי שם**: `"ארוחת ערב — Gran Fierro"` ו-`"Gran Fierro"` הם שמות שונים באותה נקודה, וכך גם **6** תחנות "מלון" הממופות לעוגן אחד. בלי זה: 120 נעצים על 98 נקודות. אחרי: 103 נעצים, ו-15 נקודות מוערמות ירדו ל-5 (כולן קיימות מלפני כן ב-`ALL_PLACES`)
- **`_scheduledPlaceNames()`** — מקור יחיד ל"מה בלוז", משותף למפה ולרשימה. מסמן גם מקום ב-`ALL_PLACES` שתחנה היא כינוי שלו (אותן קואורדינטות), אחרת הנעץ ששרד את ה-de-dupe לא היה מקבל תג יום
- `type:'stop'` נוסף ל-`TYPE_GRAD`/`catColor`/`typeColor`/`typeLabel` — כל מפת type חדשה חייבת ערך, אחרת נפילה ל-fallback אפור
- **"היינו" מקורלל לשלושתם**: נעץ ירוק עם ✓ במפה (`makeVisitedPinIcon`), תג ברשימת הקרובים, וכפתור toggle בדיאלוג הנעץ. `refreshVisitedUI()` מעדכן כפתורים **במקום** (`outerHTML`) כדי לתפוס גם דיאלוג פתוח

**⚠️ באג נתונים שנותר (לא תוקן — דורש אימות)**: `Trdelník (טרדלניק)` ו-`מוזיאון החושים` חולקים קואורדינטה זהה `[50.0875, 14.4213]`, וכך גם `Westfield Chodov` ו-`Albert Supermarket — Chodov`. אחת מכל זוג שגויה. **לא נוחשה קואורדינטה חדשה** — צריך אימות משני מקורות כמו כל השאר.

#### תיקון קואורדינטות שגויות + קורלציה מלאה בטאב מיקום (2026-08-11)

**סוג C נסגר** — שתי נקודות שבהן שני מקומות חלקו קואורדינטה:
| מקום | היה | הפך ל | אימות |
|------|-----|-------|--------|
| `מוזיאון החושים` | `[50.0875, 14.4213]` (העיר העתיקה — **שגוי**) | `[50.0840, 14.4291]` | Jindřišská 939/20; Google Maps place `50.0839944,14.4290621`, ותואם למרחק מ-`Praha Exchange — Jindřišská` שכבר במאגר |
| `Westfield Chodov` | `[50.0277, 14.4892]` (~350 מ׳ סטייה) | `[50.0311, 14.4905]` | ויקיפדיה `50°01′50″N 14°29′26″E` + Apple Maps `50.031557,14.490308` + מקור שלישי `50.03137,14.49234` |
| `Albert Supermarket — Chodov` | `[50.0277, 14.4892]` | `[50.0311, 14.4905]` | **אותה נקודה כמו הקניון במכוון** — ההיפרמרקט יושב בתוכו (`albert.cz` + `westfield.com`). זו לא כפילות לתיקון |

תוצאה: נקודות מוערמות 5 → **1**, וזו שנשארה נכונה.

**כפילות שנמצאה רק ב-audit מטושטש (11/08)**: `קריוטוש כשר` ו-`קריוטוש כשר — רובע יהודי` — אותו מקום, **21 מ׳** הפרש. חמקה מכל הבדיקות הקודמות כי גם השם וגם הקואורדינטה היו שונים *במעט*, ולכן לא נתפסה לא ע"י de-dupe לפי שם ולא ע"י התנגשות קואורדינטה. אוחדה. **בדיקת כפילויות חייבת להיות מטושטשת** — הכלה של שם אחד בשני + קרבה גאוגרפית, לא רק שוויון מדויק. אחרי: 99 נעצים על 98 נקודות.

**⚠️ ארבעה זוגות שנראים כפולים ואינם** — לא לאחד אותם: `Westfield Chodov`/`Albert Chodov` (היפרמרקט בתוך הקניון) · `מוזיאון האשליות`/`מוזיאון הלגו` (112 מ׳, חולקים רק את המילה "מוזיאון") · `בית הכנסת פינקס`/`בית הכנסת מייזל` (110 מ׳, שני בתי כנסת אמיתיים ברובע היהודי) · `מוזיאון הלגו`/`Escape Room` (חולקים רק "Prague"). וכן **שני פסלי קפקא שונים**: `פסל קפקא המסתובב` (דוד צ׳רני, ראש מסתובב 11 מ׳, `[50.0892, 14.4228]`) מול `פסל פרנץ קפקא — Vězeňská` (ירוסלב רונה, 2003, 3.75 מ׳, `[50.0906, 14.4200]`) — 250 מ׳ זה מזה.

**פילטרים חסרים בטאב מיקום** — כל המקומות היו על המפה (0 חסרים מכל הטאבים), אבל **לא היה כפתור סינון** ל-`shop` ול-`dessert`, אז 7 חנויות ו-11 קינוחים היו נגישים רק דרך "הכל". נוספו שלושה צ׳יפים: 🍦 קינוחים · 🛍 קניות · ✅ היינו.

**⚠️ סחיפת type במיזוג** — `_uniquePlacesForMap` קבע `merged.type = rich.type`, ולכן מקום שה-community שלו היה עשיר יותר נרשם כ-`community` ונעלם מהפילטר הייעודי שלו (`Manifesto Market — Anděl`: 25 מסעדות מתוך 26). התיקון: **ה-type הספציפי גובר על `community`** — `community` הוא הקטלוג הגנרי, `restaurant`/`shop`/`dessert`/`photo` הם הסיווג האמיתי שקובע פילטר.

**מדידה סופית**: מסעדות 26/26 · אטרקציות 32 · קינוחים 11/11 · קניות 7/7 · צילום 10/10 · המרת כסף 3/3 · בלוז 18 · היינו — לפי הסימון. 0 סחיפת type.

#### איחוד שמות כפולים (2026-08-10)
ביקורת מדדה **שלושה** סוגי כפילויות נפרדים:

| סוג | כמות | נראה למשתמש |
|-----|------|--------------|
| A — אותו **שם** פעמיים ב-`ALL_PLACES` (`shop`/`restaurant`/`dessert` + `community`) | 15 | לא במפה (`_uniquePlacesForMap` ממזג), **כן בין טאבים** |
| B — אותו מקום בשני **שמות שונים** | 3 | **כן** — שני נעצים ושני כרטיסים |
| C — שני מקומות **שונים** באותה קואורדינטה | 2 | כן — נעצים מוערמים |

**B תוקן** — ארבע רשומות אוחדו לשם קנוני אחד, כך שה-de-dupe הקיים לפי שם קולט אותן:
- `Primark Wenceslas Square` + `Primark — וצסלב` → **`Primark — כיכר וצסלב`** (שם התחנה ב-`DAYS`)
- `מאפה פרג — מאקובי זאוין` → **`מאפה פרג`**
- `Little Chimney` → **`Little Chimney — קריוטוש דובאי`**

תוצאה: 103 נעצים → **100**, התנגשויות קואורדינטה 5 → **2**, שמות ייחודיים 98 → 95.

⚠️ **בעת שינוי שם של מקום — לבדוק את `PLACE_IMGS`**: המפתח `Primark Wenceslas Square` התייתם והכרטיס איבד את התמונה. נוסף מפתח לשם הקנוני. `PLACE_COORDS` לא דרש שינוי (הכינויים הישנים נשארו, לא מזיקים).
⚠️ `VISITED_STATE` ממופתח לפי שם — שינוי שם מאבד סימון "היינו" קיים על אותו מקום.

**A ו-C לא טופלו** — A דורש שינוי מבני ב-`COMMUNITY` (הטאב מרנדר ישירות מ-`type:'community'`, ראה האזהרה ב-`_uniquePlacesForMap`), ו-C הוא שגיאת נתונים שדורשת אימות קואורדינטה משני מקורות.

#### סט פעולות אחיד בכל המשטחים (2026-08-10)
ביקורת מדדה 6 פערים; כולם נסגרו. **8 משטחים × 4 קבוצות פעולה — הכל ✅**:

| משטח | נווט | ממני | היינו | לוז |
|-------|:----:|:----:|:-----:|:---:|
| אטרקציות · מסעדות · קינוחים · צילום · קניות | ✅ | ✅ | ✅ | ✅ |
| כרטיס תחנה (ימים) | ✅ | ✅ | ✅ | ✅ |
| פופ-אפ פרטי תחנה | ✅ | ✅ | ✅ | ⏰ שעה · ↔ העבר יום |
| דיאלוג נעץ במפה | ✅ | ✅ | ✅ | ✅ |

- נוסף `📍 ממני` לקינוחים (11), צילום (10) וקניות (7)
- כרטיס התחנה: התג הפסיבי `✅ היינו` הוחלף ב**כפתור toggle אמיתי** (variant `'chip'` — קומפקטי לשורה צפופה)
- פופ-אפ התחנה קיבל `📍 ממני`, `⏰ שעה` ו-`↔ העבר יום` — קודם היה צריך לסגור ולחזור לכרטיס בשביל כל פעולה
- **⚠️ ה-listener של `.visitedBtn` עבר ל-capture phase (`addEventListener(..., true)`)**: כרטיס התחנה נושא `onclick="showStopDetail(...)"` כ-attribute. ב-bubble ה-listener ב-`document` רץ **אחרי** ה-onclick של הכרטיס, ולכן `stopPropagation` איחר והפופ-אפ נפתח יחד עם הסימון. נתפס בבדיקה (`popupOpened: true`), תוקן, ואומת (`popupOpened: false`)

#### פסקי דין של המשפחה מהשטח (2026-08-11)
המשפחה דיווחה חוויות בפועל, ואלה נרשמו כדי שהמאגר ישקף ניסיון ראשון ולא רק מדריכים.

| מקום | מה נרשם |
|------|----------|
| **Pizza & Pasta Factory** | **מקום חדש** — לא היה במאגר. 28. října 372/5, ⭐4.9. קואורדינטה אומתה משני מקורות: 2GIS `50.083515,14.422403` ו-Firmy.cz `50.0834997,14.4222442` (הפרש ~11 מ׳) → `[50.0835, 14.4223]` |
| **Café Savoy** | האוכל מעולה, **והאפל שטרודל במיוחד** — נוסף גם ל-`desc` וגם ל-`tips` של התחנה ביום 4 |
| **Výtopna** | ⚠️ ה-`desc` טען **"האוכל עצמו בינוני"**. המשפחה חוותה "יפה וטעים". הטענה הגנרית הוסרה והוחלפה בפסק הדין מהשטח, עם ציון מפורש שהוא בניגוד לביקורות |

**הדפוס**: פסק דין מהשטח נרשם עם קידומת `👨‍👩‍👧 משפחת שיש, <תאריך>:` כדי שיהיה מובחן מטקסט מדריכים. הוא **גובר** על טענה גנרית סותרת — ניסיון ראשון של מי שהיה שם עדיף על ביקורת מצטברת, אבל מציינים את הפער ולא מוחקים אותו בשקט.

**מדידה**: מסעדות 27 → **28**, ALL_PLACES 117, 0 התנגשויות קואורדינטה, 0 זוגות כפילות חדשים.

#### OC Skalka — הקניון שלא היה במאגר (2026-08-11)
המשפחה ביקרה בחנות הסטוק **Action** ובקניון שסביבה, וחיפשה אותו באפליקציה — **לא היה שם בכלל**.

זוהה כ-**OC Skalka**, `Přetlucká 3295/50, Praha 10 — Strašnice`: אותה כתובת מדויקת מחזירה גם את סניף Action וגם את Tesco Extra (היפרמרקט 8,000 מ״ר). זה גם הקניון **הקרוב ביותר למלון** — תחנת מטרו אחת מ-Strašnická בקו A, או ~1.4 ק״מ הליכה.

| מקור | קואורדינטה |
|------|-------------|
| tesca.cz | `50.0702854, 14.5103746` |
| 2GIS | `50.0702854, 14.5103746` |
| מקור שלישי | `50.0703, 14.5099` (הפרש ~35 מ׳) |

נלקח הערך ששני מקורות מסכימים עליו **בדיוק** → `[50.0703, 14.5104]`. 0 התנגשויות; הקרוב ביותר הוא המלון ב-1367 מ׳ ו-Fashion Arena ב-1952 מ׳ — עקבי עם המיקום על ציר קו A מזרחה.

⚠️ **שני פערים נרשמו בכרטיס במקום להיסגר בשקט**:
- **נוכחות Pepco נשענת על עדות המשפחה בלבד** — אף מקור מקוון נגיש לא מפרט את רשימת השוכרים המלאה, ו-`pepco.cz` חסום ל-egress. JYSK, dm drogerie ו-Stoklasa כן אומתו מקוונת.
- **שעות טסקו סותרות בין מקורות** (22:00 מול חצות) — נרשמה המלצה לוודא לפני נסיעה מאוחרת.

**מדידה**: קניות 7/7 → **8/8**, canonical name = עצמו (לא נבלע בקבוצת המלון), דיאלוג הנעץ נפתח, 0 שגיאות JS.

#### שווי בשקלים ליד כל מחיר CZK (2026-08-12)
המחירים בלוז ובכרטיסים הוצגו ב-**CZK בלבד** — מה שמציק כשעומדים בקופה ומנסים להעריך את הסכום. עכשיו מוצג לצידם השווי בשקלים: `690–890 CZK (₪99–127) לאדם`.

חל על כרטיסי היום, פופ-אפ התחנה, דיאלוג הנעץ במפה, פופ-אפ בחירת היום, וכל הטאבים. **טאב ההוצאות לא נגוע** — שם ממילא מוצגות שתי המטבעות, והמרה הייתה יוצרת כפילות.

**שתי החלטות תכנוניות:**
- **ההמרה בזמן תצוגה ולא בנתונים.** המחירים נשארים ב-CZK במקור, וההמרה משתמשת ב-`RATE` — אותו שער שנמשך חי מ-`open.er-api.com` וניתן לעריכה ידנית. שינוי שער משתקף מיד בכל המסכים, בלי מיגרציה ובלי מספרים שהתיישנו בתוך הטקסט.
- **`_annotateIls` עובר על צמתי טקסט בלבד** (`createTreeWalker` + `SHOW_TEXT`) ולא נוגע ב-HTML או ב-attributes, ולכן **לא יכול לשבור `onclick`** או מרכאות בתוך שמות מקומות — המלכודת שכבר שברה דברים כאן פעמיים.

**באג קיים שנחשף**: שדות `price` ב-`ALL_PLACES` כבר מכילים `"CZK"`, והתבניות הוסיפו עוד אחד — `400–800 CZK למנה CZK`. נוסף `_priceTxt()` שמוסיף `CZK` רק אם הוא עוד לא שם.

⚠️ **שני באגים שנתפסו בהזרקה עצמה**: (1) עוגן ההזרקה שלי התאים לפונקציה **אחרת** בסוף הקובץ, כי ספרתי רווחי הזחה מפלט `awk` שהוסיף תו משלו — **לא לספור הזחה מפלט מעוצב**; (2) ה-regex חייב `lookahead` שמונע הכפלה, אחרת רינדור חוזר על אותו צומת מוסיף `(₪…)` פעמיים.

אומת על 8 מקרים: סכום בודד · טווח · פסיקים באלפים · `Kč` · שני מחירים באותו משפט · הרצה כפולה · מספר טלפון שלא הומר · `0 CZK` שנשאר.

#### ייצוא CSV — סה״כ, מיון וסיכומים (2026-08-11)
המשתמש ייצא את ההוצאות והצביע על שתי בעיות: **אין סה״כ** ו**אין סדר מסוים**.

**הסדר**: `exportCsv` הסתמך על סדר המערך של `EXPENSES`, שנבנה ב-`unshift` — כלומר "מי נוסף אחרון" ולא סדר כרונולוגי מובטח, ובמיוחד כשמתמזגות רשומות מ-Sheets ומ-Firebase. הייצוא ממיין עכשיו בעצמו, **בסדר עולה**, כך שהקובץ נקרא כמו יומן טיול מיום 1 ליום האחרון.

⚠️ **המלכודת**: `e.date` הוא מחרוזת he-IL (`"11.8.2026, 22:03:53"`) ולא `Date`. `Date.parse` מחזיר עליה `NaN`, ו**מיון על `NaN` לא עושה כלום בשקט** — כלומר `sort` נאיבי היה נראה כאילו עבד. `_expTs()` מפרסר במפורש עם regex, ונופל ל-`id` (שהוא `Date.now()` ביצירה).

**מה נוסף**: שורת סה״כ · סיכום לפי יום · סיכום לפי קטגוריה ממוין יורד עם אחוזים · עמודות תאריך/שעה נפרדות ב-ISO (אקסל לא מפרסר `"11.8.2026, 22:03:53"`) · עמודת "יום בטיול" דרך `tripDayNumber()` · שם קובץ עם תאריך.

**הקטגוריה נגזרת מהאימוג׳י שהמשתמש עצמו בחר** בשם ההוצאה — לא סיווג שהמצאנו. רשומה בלי אימוג׳י מוכר נופלת ל-`❓ אחר` במפורש ולא משויכת בכוח.

בלוק הסיכום **בתחתית ולא כשורות ביניים**, כדי שאזור הפירוט יישאר טבלה נקייה שאפשר למיין ולסנן.

⚠️ **ה-BOM (`﻿`) חייב להישאר** — בלעדיו אקסל מציג את העברית כג׳יבריש. בקוד הוא כתוב כ-escape ולא כתו; עריכה שמחליפה אותו בתו עצמו תיכשל בהתאמה.

**אומת מול 35 ההוצאות האמיתיות** של המשתמש, ע"י הרצת כפתור הייצוא בפועל והשוואה לחישוב עצמאי ב-Python: 27,853 CZK / ₪3,981, ופירוט יומי 3281/8528/9554/6490 — זהה.

**סבב שני — הקובץ מחולק לקטגוריות (2026-08-12)**: המשתמש הבהיר שהכוונה הייתה ש**הפירוט עצמו** יחולק, לא רק שיהיה סיכום קטגוריות בסוף. הקובץ נבנה מחדש כ**דוח**:

1. כותרת — תקופה, מספר הוצאות, ו**שער החליפין ששימש** (בלעדיו הדוח לא ניתן לשחזור)
2. **סה״כ כללי מיד בראש**, ב-CZK וב-ILS
3. סיכום לפי קטגוריה, ממוין יורד, עם אחוזים
4. סיכום לפי יום, עם מספר היום בטיול
5. **פירוט מחולק לקטגוריות** — לכל אחת כותרת, שורות כרונולוגיות, ושורת `סה״כ <קטגוריה>` בשתי המטבעות
6. סה״כ כללי חוזר בסוף

⚠️ **הסכומים נשארים מספרים גולמיים** ולא מחרוזות עם פסיקים — אחרת אקסל מתייחס אליהם כטקסט ולא ניתן לסכם. הפורמוט הוא עניין של תצוגה באקסל, לא של הקובץ.

⚠️ **התאמת מין ומספר**: `'1 הוצאות'` → `cnt()` שמחזיר `'הוצאה אחת'` ליחיד. קטגוריה עם פריט אחד היא מקרה נפוץ ולא קצה.

**אימות צולב** (לא רק הרצה): קריאת הקובץ שנוצר ב-Python וּוידוא ש-35 שורות הפירוט מסתכמות למקור, **ושסכום כל הקטגוריות שווה לסה״כ הכללי**. סכום אחוזים 99.9% הוא עיגול תקין.

#### תיעוד ≠ תכנון — הבאג בשיבוץ Pizza & Pasta (2026-08-11)
המשפחה סיפרה שאכלה ב-Pizza & Pasta Factory ב-10/8 והיה מעולה. שיבצתי אותה כארוחת ערב **עתידית** ביום 5, כי הוא היה היום היחיד עם dinner מסוג placeholder. המשתמש תפס: *"אבל כבר היינו בפיצה פסטה פקטורי ב10/8 בערב"*.

**הכשל הוא מושגי ולא טכני**: המלצה מהשטח היא **תיעוד של מה שקרה**, ולא פריט פנוי לשיבוץ. שיבוץ שלה קדימה יצר לוז לא-נכון **בשני הכיוונים** — יום 3 לא שיקף את הערב שבו באמת אכלו, ויום 5 שלח אותם לאכול שוב באותו מקום.

**הכלל**: כשמגיע פסק דין מהשטח עם תאריך — **לשבץ אותו ליום של התאריך הזה**, לא ליום הפנוי הבא. `time:'ערב'` ביום שכבר עבר הוא רשומת תיעוד לגיטימית ומועילה (היא מזינה את לוח הסיכום ואת "היינו"), ולא "בזבוז" של תחנה.

⚠️ **הבדיקה שהחמצתי**: לוודא שמקום מופיע **פעם אחת בלבד** בכל הלוז. `findItemInDays` מחזיר את ההתאמה הראשונה, ולכן שיבוץ כפול לא צועק — הוא פשוט מציג תג יום שגוי. הבדיקה נוספה: סריקת כל `DAYS` וספירת מופעים לפי שם.

#### ⚠️ resync ממכשיר ריק מחק את כל סימוני "היינו" (2026-08-12)
המשפחה איבדה את **כל** הסימונים. השרשרת:

1. `_resyncLocalDataToCloud()` רץ אחרי כל כניסה אמיתית וקרא ל-`saveVisitedState()` **ללא תנאי**.
2. במכשיר עם `localStorage` ריק — בן משפחה שנכנס בפעם הראשונה — `getVisitedState()` מחזיר `{}`.
3. `saveVisitedState()` דחף `"{}"` ל-`appdata/main.visited` ודרס את הסימונים של כולם.
4. ה-listener מוגן ב-`if (d.visited)`, אבל **`"{}"` היא מחרוזת truthy** — המגן לא תפס, וכל מכשיר דרס את העותק שלו.

**אותה פצצה קיימת בלוז**: `getDaysState()` נופל ל-`DAYS` הבסיסי, כלומר מכשיר חדש היה דוחף את המסלול המקורי מעל כל עריכות המשפחה.

**שני מגנים:**
- **`_hasLocalData(key)`** — resync מעלה למעלה **בלבד**. אם אין נתונים מקומיים אין מה לתרום, ודחיפה היא רק הרס. מזהה גם `{}`/`[]` ולא רק מפתח חסר. חל על visited, ימים, הוצאות, ציוד, תזכורות ו**תקציב** (הוסף ב-13/08 — ראה הסעיף הבא).
- **ה-listener מסרב לדרוס סימונים קיימים בענן ריק**, ובמקום זה דוחף את המצב המקומי בחזרה — כלומר מכשיר ששרד **משחזר אוטומטית** את כל השאר.

⚠️ **הכלל הכללי**: כל מגן מסוג `if (value)` על JSON מסונכרן הוא **שגוי** — `"{}"`, `"[]"` ו-`"0"` כולן truthy. לבדוק את התוכן, לא את קיום המחרוזת.

⚠️ **ולפני שמוסיפים sync חדש**: לשאול "מה קורה כשמכשיר בלי נתונים נכנס?" — זה המקרה שהורס, לא המקרה עם הנתונים.

#### ⚠️ אותו באג, חבר שפספסתי — קטגוריות התקציב (2026-08-13)
המשתמש הגדיל את תקציב האוכל ל-**4,000 ₪**, והשינוי לא נשמר — הוא נאלץ להגדיל **שוב** למחרת.

זו **בדיוק** השרשרת של מחיקת "היינו", רק על מפתח אחר:

```js
// _resyncLocalDataToCloud — ללא תנאי:
saveBudget(loadBudget());

// loadBudget נופל לברירת המחדל כשאין נתונים מקומיים:
return JSON.parse(localStorage.getItem('prague_budget_v1') || 'null')
       || window._memBudget || DEFAULT_BUDGET;   // ← אוכל = 3,000
```

כל כניסה מהקשר שה-`localStorage` שלו ריק דחפה את **ברירת המחדל** מעל העריכה שלו, וה-listener הפיץ אותה חזרה לכל המכשירים. הפעם המגן לא היה `"{}"` truthy אלא **fallback שמייצר נתונים יש מאין** — מסוכן יותר, כי הערך שנדחף *נראה* לגיטימי.

⚠️ **הכשל שלי**: כשהוספתי את `_hasLocalData` בתיקון של "היינו" הגנתי על visited, ימים, הוצאות, ציוד ותזכורות — ו**דילגתי על שתי שורות התקציב** באותה פונקציה עצמה. הבאג נשאר חי עוד יום, ונתפס רק כי המשתמש הבחין שהמספר חזר.

**הכלל**: כשמתקנים **מחלקת** באג — לא מספיק לתקן את המופע שהמשתמש הצביע עליו, ולא מספיק "לעבור על הפונקציה". צריך **למנות את כל החברים במפורש** — כאן: כל קריאת שמירה ב-`_resyncLocalDataToCloud`, אחת-אחת, ולסמן ✅/❌ לכל אחת. `grep` על שם הפונקציה, לא סריקה בעין.

⚠️ **ולחפש `||` בכל `load*`**: כל פונקציית טעינה שנופלת לברירת מחדל היא מועמדת לאותו באג. `saveTotalBudget` שרד כי היה מוגן ב-`if (tb)`; `loadBudget` לא, כי ה-fallback שלו תמיד מחזיר ערך.

אומת בהרצה: מכשיר ריק **לא דוחף** (לפני: דחף 3,000) · אחרי עריכה ל-4,000 → resync רץ → **נשאר 4,000**.

#### 🔴 השורש האמיתי: המודל לא דחף קטגוריות לענן (2026-08-13)
המשתמש דיווח שוב: *"התקציב לא מתעדכן בקטגוריות בעריכה — נותן לערוך ולשנות אבל לא נשמר ולא משתקף במסך הראשי."*

השחזור הראה שזה **חמור מהמשוער** — העריכה לא שרדה אפילו מקומית:

| שלב | לפני התיקון |
|---|---|
| אוכל בהתחלה | 3,000 |
| ערך ל-4,000 ולחץ שמור | **חזר ל-3,000 מיד** |
| מה נדחף לענן | `total_budget` בלבד |

```js
// saveBudgetFromModal — הענף הלא-GAS, כלומר GitHub Pages:
try { localStorage.setItem('prague_budget_v1', JSON.stringify(budget)); } catch(e) {}
...
} else {
  saveTotalBudget(totalVal);      // ← רק הסה״כ. הקטגוריות לא נדחפות בכלל.
}
```

**השרשרת**: המודל כתב קטגוריות ל-`localStorage` בלבד → דחף `total_budget` → **הדחיפה הזו עצמה ייצרה snapshot** → ה-listener ראה `budget_categories` ישן בענן ≠ מקומי חדש → **דרס את המקומי בחזרה**. העריכה נמחקה בתוך שברירי שנייה, לפני שהמסך בכלל התרענן.

**התיקון**: `saveBudget(budget)` במקום `setItem` גולמי — כותב מקומית **וגם** דוחף לענן (ומסנכרן את הסה״כ הנגזר), עם fallback ל-`setItem` אם הוא זורק.

⚠️ **תיקון האבחנה שלי מהבוקר**: ייחסתי את היעלמות ה-4,000 ל-`_resyncLocalDataToCloud` שדחף `DEFAULT_BUDGET` (תוקן ב-#100). זה היה באג אמיתי — **אבל הוא לא היה הסיבה**. הסיבה היא הנתיב הזה, שפוגע ב**כל עריכה, מיד**, ולא רק בכניסה מהקשר ריק. שני הבאגים חיו במקביל.

⚠️ **הדפוס לזיהוי**: `grep` על `localStorage.setItem('prague_<key>'` וסינון נתיבי הקליטה מהענן. מה שנשאר הוא כתיבות של המשתמש — וכל אחת **חייבת** לעבור דרך פונקציית `save*` שדוחפת, ולא לכתוב גולמי. נמדד: זו הייתה **הכתיבה היחידה** מסוג זה שנשארה.
#### מטריצת קטגוריה × יום בייצוא (2026-08-13)
המשתמש ביקש "סה״כ מסודר לכל קטגוריה לפי ימים". הדוח ידע לתת סיכום **לפי קטגוריה** וסיכום **לפי יום** — אבל לא את ההצלבה, ולכן "כמה הוצאנו על אוכל ביום 3" חייב סריקה ידנית של הפירוט.

נוספו שתי מטריצות, CZK ואז ILS: שורה לכל קטגוריה, עמודה לכל יום (`יום 3 · 08-10`), סה״כ בשני הכיוונים ושורת `סה״כ יומי`.

⚠️ **`COLS` נקבע דינמית** — `COLS = Math.max(8, dayOrder.length + 2)`, **אחרי** האגרגציה ולפני בניית השורות. `line()` קורא את `COLS` בזמן הקריאה, ולכן ההרחבה תקפה לכל השורות. בלי זה, טיול בן 8 ימים היה נחתך ל-8 עמודות.

⚠️ **תא ריק ולא `0`** — "לא הוצאנו כאן" שונה מ"הוצאנו אפס", וגרף באקסל מדלג על ריק במקום למתוח קו לרצפה.

⚠️ **שתי מטריצות נפרדות ולא מטבע כפול בתא** — כל תא נשאר מספר גולמי אחד שאקסל יכול לסכם.

**אומת מול 42 ההוצאות האמיתיות** של המשתמש: כל שורה מסתכמת לעמודת הסה״כ, כל עמודה לשורת `סה״כ יומי`, ושתי המטריצות ל-35,000 CZK / 5,001 ILS — זהה לדוח שהוא ייצא בעצמו (3281/8528/9554/6490/7147).

#### K12 BBQ & Sushi — פסק דין מהשטח, יום 7 (2026-08-14)
המשפחה אכלה שם היום. נוסף כ-`type:'restaurant'` (מסעדה 29).

**מה זה**: בופה אסייתי אכול-כפי-יכולתך עם **גריל על השולחן** — בוחרים נתחים ומצליבים בעצמכם. סושי, ווק, הוט-פוט, סשימי, דים סאם ופירות ים, 1.5 שעות. **בבניין `Dům módy`, Václavské náměstí 804/58, קומה 1.** פתוח עד חצות.

⚠️ **השם הוא `K12` ולא `12K`** — המשתמש כתב "12k bbq", והחיפוש העלה שההיפוך הוא הנכון.

⚠️ **המחיר לא אומת** — נרשם `אכול כפי יכולתך · ⚠️ המחיר לא אומת` במקום סכום מומצא. גם דירוג גוגל לא אומת ולכן `google:''`.

⚠️ **הקואורדינטה ממקור אחד** — ויקיפדיה הצ׳כית של `Dům módy`: `50°4'47.17"N 14°25'44.26"E` → `[50.0798, 14.4290]`. **האימות המשלים פנימי**, וכל המרחקים קוהרנטיים: 98 מ׳ מ-George Prime Steak · 114 מ׳ מ-Kantýna · **160 מ׳ מ-Primark שבכיכר וצסלב** · 141 מ׳ מפסל ואצלב ו-157 מ׳ מהמוזיאון הלאומי — בדיוק "החלק העליון של הכיכר ליד הפסל" שבתיאור המקור. **הנקודה היא של הבניין ולא של הקומה או הכניסה.**

**ההוצאה כבר נרשמה ע"י המשתמש בעצמו** — לא נגעתי בה.

⚠️ **התחנה בלוז נשארה למשתמש** — עריכת `DAYS` בקוד לא מגיעה למכשיר עם לוז שמור (ראה האזהרה בראש הקובץ). הוא מוסיף אותה מטאב מסעדות ליום 7.

#### Taiyaki Bar ו-IKEA Zličín — שתי רשומות **בלי קואורדינטה** (2026-08-14)
המשתמש שלח ריל של `Taiyaki Bar` וביקש להוסיף, וגם את IKEA באזור Zličín.

**`Taiyaki Bar`** — `type:'dessert'`, `Maiselova 14/1, Josefov`, כל יום 11:00–21:00, ⭐**4.9 מ-1,990 ביקורות**. בר יפני שאופה טאיאקי (כיסון בצורת דג) במקום וממלא בגלידה. מפורסם בשוקו החם — רפאלו, אוראו, סמורס. הכתובת אומתה מארבעה מקורות בלתי-תלויים; הדירוג מ-Wanderlog/RestaurantGuru.

**`IKEA Praha — Zličín`** — `type:'shop'`, `Skandinávská 15a`, 10:00–20:00. **מתחנת המטרו Zličín יוצאים אוטובוסי מעבורת חינם** להיפרמרקטים (איקאה, גלובוס, טסקו) — מקור: ויקיפדיה של תחנת Zličín. באותו אזור גם `Homepark Zličín` עם Tesco Extra 06:00–24:00.

✅ **עדכון (אותו יום)**: **המשתמש שלח את הנקודה של Taiyaki Bar מ-Google Maps כשעמד במקום** — `50°05'18.4"N 14°25'07.4"E` → `[50.0884, 14.4187]`. דיוק ה-DMS הוא 0.1 שנייה ≈ **3 מ׳**, כלומר טוב יותר מכל מקור רשת. שלוש בדיקות שפיות פנימיות מתכנסות: **20 מ׳** מבית הכנסת מייזל (Maiselova 10), **111 מ׳** מבית העירייה היהודי (Maiselova 18) — עקבי עם רחוב שרץ צפונה — ו-**223 מ׳** מכיכר העיר העתיקה, שתואם ל"3 דקות הליכה" שבמקורות. **IKEA נשאר בלי קואורדינטה.**

⚠️ **הדפוס שעבד**: כשכל מקורות הרשת חסומים ואי אפשר לאמת — **לבקש מהמשתמש שעומד שם**. זו עדות ראשונית, טובה יותר משני מקורות משניים. הבקשה חייבת להיות קונקרטית: *"לחיצה ארוכה על הנעץ, תעתיק את המספרים"* — לא "תשלח לי מיקום", כי קישור מקוצר של Google (`maps.app.goo.gl`) חסום גם הוא, וגם `nominatim`, `mapy.cz` ו-`geocode.maps.co`.

⚠️ **לשתיהן לא הייתה קואורדינטה מאומתת בעת ההוספה, ולכן לא נוספה להן רשומה ב-`PLACE_COORDS`.** ניסיתי: כל מקורות המפה (mapy, polomap, wolt, wanderlog, kurzy) חסומים ל-egress, והמקורות הזמינים נתנו רק כתובת. **הפיתוי היה לגזור מהשכנים** — בית הכנסת מייזל ברחוב Maiselova 10 יושב ב-`50.08861, 14.41861`, ובית העירייה היהודי במספר 18 ב-`50.08944, 14.41861`, כלומר הרחוב כמעט אנכי ואפשר היה לשערך את מספר 1. **זו אקסטרפולציה ולא מקור — ולכן לא נעשתה.**

**המשמעות המעשית**: הכרטיסים מופיעים בטאבים ובחיפוש עם כל התוכן, וכפתור המפה מנווט נכון דרך Google Maps. **מה שאין: נעץ במפה המקומית ומרחק ב"קרוב אליי".** נכתב במפורש בשדה `where`/`tip` של כל אחד.

זהו אותו דפוס כמו סניף `Koh-i-noor Na Příkopě` — מקום שימושי שלא ניתן לאמת נכנס כטקסט, לא כנעץ.

⚠️ **`renderDesserts` הוא lazy** — קינוח חדש לא מופיע עד שנכנסים לתת-הטאב "קינוחים". נבדק שגם `Angelato` הקיים מתנהג כך, כלומר זו התנהגות רגילה ולא רגרסיה.

#### Metropole Zličín — Primark השני בפראג (2026-08-14)
המשתמש ביקש להוסיף אותו כתחנה עם מיקום. נוסף כ-`type:'shop'` (סניף 10 ברשימת הקניות).

**מה שהופך אותו לרלוונטי**: **Primark** — הסניף השני בפראג, נפתח **נובמבר 2024**. אומת מדף החנות **באתר Primark עצמו** (`primark.com/cs-cz/stores/praha-5/metropole-zlicin-shopping-center`) ומדף החנות באתר הקניון (`westfield.com/.../metropole/obchody/primark`). זהו **המקום היחיד מלבד כיכר וצסלב** — ⚠️ **ב-Westfield Chodov אין Primark**, למרות שרשימות אגרגטיביות טוענות אחרת; ההתרחבות הבאה בפראג היא ל-Černý Most.

עוד ברשת: `H&M` (דף ייעודי ב-hm.com), `Albert` היפרמרקט (דף ב-albert.cz), C&A, Reserved, New Yorker, Lindex, M&S, Sportisimo, Decathlon, Datart, Cinema City.

⚠️ **הקואורדינטה נשענת על מקור כתובת אחד בלבד** — `50.054136, 14.287900` — ולא שניים כמו בשאר המאגר. האימות המשלים **עקיף**: תחנת המטרו הסופית Zličín ב-`50.054, 14.291`, 215 מ׳ ממזרח, והקניון צמוד לה בגישה רגלית ישירה. הקומפלקס הוא 63–70 אלף מ״ר, כלומר שתי הנקודות בתוך המתחם. **הדיוק ברמת המבנה ולא ברמת הדלת — נרשם בקוד ולא הוסתר.**

⚠️ **המרחק נרשם בכרטיס ולא הוסתר**: **15.3 ק״מ מהמלון** ו-10.2 ק״מ מהמרכז — הקצה המערבי של העיר, סוף קו B. מטרו A → Můstek → B, כ-50–60 דק׳ לכיוון. זו הנסיעה הארוכה ביותר לכל מקום במאגר.

⚠️ **הדירוג בגוגל לא אומת** — שדה `stars` נושא `~200 חנויות` במקום כוכבים מומצאים, וזה נאמר גם בטיפ.

**מדידה**: קניות 9 → **10**, נעצים 105 → **106**, 0 שמות כפולים, 0 שכנים בטווח 300 מ׳, סיווג הוצאה → `קניות 🛍️`, 0 `undefined`.

#### מפת החום — טבלה קריאה במקום קיר מלבנים (2026-08-13)
המשתמש סימן את הטבלה "לפי קטגוריה ויום" וביקש שתהיה ברורה יותר.

**הבעיה**: כל תא היה מלא בצבע אטום. התוצאה — קיר של מלבנים כחולים שקשה לסרוק, ובתאים הכהים הטקסט לבן ובבהירים כהה, כלומר **שני סוגי קריאה באותה טבלה**.

**עכשיו**: הרקע הוא **רמז עדין בלבד** — שקיפות 5%–34% של אותו כחול — הטקסט תמיד כהה וקריא, ו**היום היקר בכל שורה מודגש**. אפשר לסרוק בלי לקרוא כל תא.

בנוסף: ריווח כפול, פסי זברה, `thead`/`tfoot` אמיתיים עם קווי הפרדה, עמודת סה״כ תחומה משני צדדים, מקפים מעומעמים לתאים ריקים, וכל מספר ב-`<bdi>`.

⚠️ **עם יותר מ-3 ימים הטבלה רחבה ממסך טלפון ונגללת.** נוספה הערה מפורשת — `↔ הטבלה נגללת לצדדים — יש בה N ימים` — אחרת רואים טבלה חתוכה וחושבים שחסרים ימים.

#### כפתור "אזן" — האיזון גלוי, ההחלטה של המשתמש (2026-08-13)
המשתמש שאל אם דיברנו על כפתור איזון. **הצעתי אותו ולא בניתי** — לא היה אישור. עכשיו נבנה.

**⚖️ אזן** ליד ✏️ ערוך בכרטיס התקציב. לחיצה מציגה **בדיוק מה יזוז**, ורק אחרי אישור זה קורה:

```
מעביר ₪419 מקטגוריות עם עודף אל אלה שחרגו. הסה״כ לא משתנה.
  אוכל ומסעדות    −₪157
  אטרקציות        −₪117
  תחבורה           −₪57
  כללי ושונות      −₪88
  קניות           +₪419
```

⚠️ **ההכרעה המרכזית: לא אוטומטי.** איזון אוטומטי משכתב בשקט מספרים שהמשתמש קבע בכוונה — **בדיוק סוג הכתיבה שמחקה השבוע סימוני "היינו" ואת התקציב**. כאן הוא רואה ולוחץ בעצמו.

⚠️ **הסה״כ לא משתנה** — זו העברה בין קטגוריות ולא הגדלה. נמדד: 8,300 → 8,300.

⚠️ **לוקחים ביחס לגודל העודף** ולא בחלוקה שווה, וכל לקיחה חסומה ב-`Math.min(amt, sl.free)` — **אף קטגוריה לא יורדת מתחת למה שכבר הוצא בה**.

⚠️ **חריגה שאין ממה לכסות נאמרת במפורש** — "חסרים עוד ₪X שאין מהיכן לקחת" — במקום לאזן חלקית בשקט.

עובר דרך `saveBudget()`, כלומר נשמר מקומית **וגם** נדחף לענן ומסנכרן את הסה״כ הנגזר.

**אומת בהרצה**: קניות חרגו ב-419 → אוזן בדיוק ל-1,219 (=בפועל) · סה״כ ללא שינוי · הרצה שנייה מחזירה "אין חריגה".

#### 🔴 כיווניות: `₪2,198 / ₪3,000` נקרא הפוך (2026-08-13)
המשתמש: *"התצוגה בדוח של הוצאות לפי קטגוריה וסה״כ מבולגנת"*.

זו לא הייתה רק צפיפות. **המספרים הוצגו בסדר הפוך:**

| בקוד | על המסך |
|---|---|
| `₪2,198 / ₪3,000` (בפועל / תקציב) | `₪3,000 / ₪2,198` |
| `15,382 CZK · ₪2,198 · 20 הוצאות` | `15,382 20 · ₪2,198 · CZK הוצאות` |
| `3,281 CZK · ₪469` | `CZK · ₪469 3,281` |

⚠️ **הסיבה**: מספרים וטקסט לטיני הם **LTR בתוך שורה עברית RTL**. בלי בידוד, אלגוריתם ה-bidi מסדר את הרצף מחדש — ולא היה שום סימן לכך בקוד. **המשמעות המעשית**: אי אפשר היה לדעת איזה מספר הוא "בפועל" ואיזה "תקציב".

**התיקון**: `<bdi>` עם `unicode-bidi:isolate; direction:ltr` על **כל** סכום — 49 מופעים בדוח. כל סכום נשאר יחידה אחת במקום שנכתב.

⚠️ **אותו באג היה גם במסך עצמו**, לא רק בדוח — `renderBudget` הציג `₪2,198 / ₪3,000` באותה היפוך. תוקן שם באותו אופן, עם התוויות `בפועל` / `מתוך`. **כשמוצאים באג כיווניות — לחפש את אותה תבנית בכל מקום שמדפיס מספר בתוך עברית.**

**ובנוסף — הפריסה נבנתה מחדש**, כי גם אחרי תיקון הסדר `₪2,198 / ₪3,000` לא אומר מה כל מספר:

```
🍽️ אוכל ומסעדות                    ✓ בתקציב
בפועל      מתוך      נותר      נוצל
₪2,198    ₪3,000    ₪802      73%
[████████████████░░░░░]
15,382 CZK
```

כל מספר עם **תווית מפורשת**, שורות נפרדות, קו מפריד בין קטגוריות, והסכום ב-CZK מתחת לפס.

⚠️ **`color:inherit` על `<b>` בתוך `<div>` מעומעם יורש את המעומעם** ולא את כלל ה-`b` — "נותר" יצא אפור בעוד השאר כהים. נתפס בצילום. `var(--ink)` מפורש.

⚠️ **הסכומים חייבים להישאר בתוך ה-`<summary>`** — הזזתי אותם ל-`<div>` בתוך ה-`<details>` והם נעלמו כשהוא סגור, כלומר בדיוק במצב שבו רוצים לראות את כל הקטגוריות במבט אחד. הוחזרו לכותרת בשורה שנייה.

**אומת**: כל 14 המספרים המרכזיים מופיעים נכון בדוח · 0 שגיאות · כל הרגרסיות עוברות.

#### מיקום ההוצאה — שדה נפרד מהקטגוריה (2026-08-13)
המשתמש: *"אני רוצה שזה יישמר תחת המיקום שאני נמצא בו"*. גלידה בגן החיות היא **אוכל** שנקנה **בגן החיות** — שני שדות, לא אחד.

**`place`** נוסף לרשומת ההוצאה. במודל: כפתור **📍 המיקום שלי** → `getCurrentPosition` → `_nearestPlace()` מוצא את המקום הקרוב מתוך `_mappablePlaces()` ומציג שם + מרחק. ניתן לנקות ב-✕.

⚠️ **`haversine` מחזיר קילומטרים** — כפול 1000 למטרים. בלי זה כל מקום היה "0 מ׳".

⚠️ **מעל 800 מ׳ לא נשמר מיקום כלל** — זה כבר לא "המקום שאני בו". עדיף בלי מיקום מאשר מיקום שגוי.

⚠️ **המיקום אינו נוגע בקטגוריה** — זו כל הנקודה. אומת: `🍽️ גלידות ושתיה` עם `place: גן החיות של פראג` נשאר **אוכל**.

**לא חוסם שמירה**: אין הרשאה / אין GPS / timeout של 8 שניות → הודעה, וההוצאה נשמרת בלי מיקום. משתמש ב-`_userLat/_userLng` אם כבר אותרו בטאב מיקום, כדי לא לבקש שוב.

**מוצג ב**: שורת ההוצאה (תג ירוק), עמודת `מיקום` ב-CSV ובדוח ה-HTML. ⚠️ **`COLS` בייצוא עלה ל-9** בגלל העמודה החדשה.

**תאימות לאחור**: רשומות ישנות בלי `place` מוצגות `—` ולא נשברות.

**אומת בהרצה** (GPS מדומה בגן החיות, 50.1166/14.4055): אותר `גן החיות של פראג · 162 מ׳` · נשמר · מוצג ברשימה · נטען חזרה בעריכה · הקטגוריה נשארה אוכל · 0 שגיאות JS.

#### ⚠️ הצ׳יפ הראה קטגוריה אחת וההוצאה נשמרה באחרת (2026-08-13)
המשתמש היה בגן החיות, קנה גלידות ושתיה, ורצה לרשום כאוכל — *"הוא לא נותן"*.

**שוחזר**: בחירת `🦁 גן החיות של פראג` מהרשימה → לחיצה על הצ׳יפ **"אוכל"** → הצ׳יפ מראה "אוכל" → **ההוצאה נשמרת כאטרקציה**. בלי הודעה, בלי סימן.

```js
// saveExpense — התנאי המקורי:
if (catKey && !guessExpCat(name)) { name = catEmoji[catKey] + ' ' + name; }
```

הצ׳יפ נלקח בחשבון **רק כששום דבר אחר לא זוהה**. ברגע שהשם מזוהה — הבחירה המפורשת של המשתמש נזרקת.

⚠️ **הפגם היה שם מהתחלה, אבל ההרחבה של הסיווג היום החמירה אותו דרמטית**: כשה-`guessExpCat` זיהה רק אימוג׳י בתחילת השם, `🦁` לא היה ברשימה והצ׳יפ כן עבד. אחרי שהסיווג התחיל לזהות גם שמות מקומות והרחיב את טבלת האימוג׳י — כמעט כל פריט שנבחר מהרשימה חוסם את הצ׳יפ. **הרחבת זיהוי יכולה לשבור מסלול שהסתמך על אי-זיהוי.**

**התיקון**: `guessExpCat(name) !== catKey` — בחירה מפורשת גוברת תמיד. בנוסף מוסר אימוג׳י-קטגוריה **פותח**, אחרת נשארים שני סימנים סותרים בשם ו-`guessExpCat` עלול להעדיף את הישן לפי סדר המפתחות.

⚠️ **מוסר רק אימוג׳י שנמצא בטבלה** — `🍣` נשאר ב-`🛍️ 🍣 Yammi Sushi House`, וזה תקין: אימוג׳י שאינו בטבלה אינו יכול לנצח בשלב 2 ממילא.

**אומת על 8 צירופים**: בחירה מהרשימה + שינוי צ׳יפ · השארת הצ׳יפ המקורי (בלי אימוג׳י כפול) · טקסט חופשי · שם שכבר נושא את האימוג׳י הנכון · כללי. **0 כשלים.**

#### הפרדת התחומים: מיקום · היינו · הוצאות (2026-08-13)
המשתמש: *"כל מה שלא קשור להוצאות לא צריך להיות בבחירה בהוצאות. צריך להפריד בין מיקום להיינו להוצאות."*

רשימת ההצעות במודל ההוצאה נבנתה גם מ-`DAYS`, ולכן הציעה `😴 חזרה ומנוחה במלון`, `🚶 הליכה דרך Na Příkopě`, `🌉 גשר קארל`, `🏝️ אי קמפה` ו-`🚖 צ'ק-אאוט` — **אף אחד מהם אינו הוצאה.** תחנת לוז עונה על "איפה היינו", לא על "על מה שילמנו".

**קבוצת תחנות הלוז הוסרה מרשימת ההוצאות.** מיקום ו"היינו" ממשיכים להציג את **כל** 35 התחנות — אומת: `stopsMissingFromMap: []`.

⚠️ **לפני ההסרה נבדק מה היה נאבד**, ולא הונח שכלום: טירת פראג קיימת במאגר כ-`מצודת פראג`, השייט כ-`שייט בוולטאווה`, ו-Hamleys כ-`Hamleys + LEGO Store`. **שניים היו קיימים רק כתחנה** ונוספו ל-`COMMUNITY` עם התוכן שלהם verbatim מהתחנה (`desc`/`booking`/`tips`/`duration`), בלי לכתוב מחדש:
- `ממלכת הרכבות — Království železnic` — `~350 CZK מבוגר, 250 ילד`
- `כיכר העיר העתיקה והשעון האסטרונומי` — הכיכר חינם, המגדל ~200 CZK

**`Čertovka` לא נוסף במכוון** — ה-`booking` שלו אומר `ללא צורך — אזור ציבורי`, כלומר חינם. הוא שייך למיקום, לא להוצאות. אותו שיקול לגשר קארל, אי קמפה ומלה סטרנה.

⚠️ **הכלל שנפסל בדרך**: ניסיתי לזהות "משלמים כאן" לפי אות מחיר (`\d CZK|כרטיס`) בטקסט התחנה. **המדידה הראתה שהוא שגוי בשני הכיוונים** — השאיר את `🚶 סיבוב קצר` ו-`🌉 גשר קארל` (כי הטיפים מזכירים מחירים של דברים סמוכים) והפיל את `🧸 Hamleys` (חנות). הוא נזרק ולא נשלח. **הפרדה לפי מקור הנתונים עדיפה על ניחוש מטקסט.**

⚠️ **השמות שנוספו זהים בדיוק לשמות התחנות**, ולכן ה-de-dupe לפי שם ממזג ולא מכפיל נעץ. אומת: 105 נעצים, **0 שמות כפולים**, התנגשות קואורדינטה אחת (Westfield/Albert — מכוונת).

**המדידה**:

| | לפני | אחרי |
|---|---|---|
| פריטים ברשימת ההוצאות | 154 | **121** |
| נופלים ל"כללי" | 53 (34%) | **3 (2%)** |
| שלושת הנותרים | — | `🏨 מלון` · `💊 בריאות` · `🔀 שונות` — **בדיוק מה שכללי אמור להיות** |
| 42 ההוצאות של המשתמש | — | **0 שינו קטגוריה** |
| תחנות במפה | 35 | **35** |

בנוסף אוחדה כפילות: הקבוצה `👥 קהילה — אטרקציות` הייתה זהה לקבוצת האטרקציות החדשה.

#### הסיווג נגזר מהמאגר, לא מרשימה כתובה ביד (2026-08-13)
המשתמש הסתכל על פסי התקציב ושאל *"אני רואה יותר מדי כללי — זה הגיוני?"*. **זה לא היה הגיוני**: נמדד ש-**53 מתוך 154 ההצעות במודל ההוצאה (34%) נפלו ל"כללי"** — סושי, טאקוס, בית העלמין היהודי, מוזיאון הלגו, OC Skalka, רחוב פריז.

**השורש**: הטבלה נכתבה ביד (68 אימוג׳י) בזמן שהאפליקציה עצמה מציעה עשרות אחרים. אבל האפליקציה **כבר יודעת** מה כל פריט — `Yammi Sushi House` הוא `type:'restaurant'`, `OC Skalka` הוא `type:'shop'`. אין סיבה לנחש מאימוג׳י כשהסיווג האמיתי כתוב ליד.

**`_expDerivedMaps()`** בונה שלוש מפות מ-`ALL_PLACES` בזמן ריצה (ממוקאש): התאמה מדויקת `icon + ' ' + name`, רשימת שמות ממוינת מהארוך לקצר, ומפת אימוג׳י.

**סדר ההכרעה הוא הלוגיקה כאן, לא פרט טכני** — מהוודאי אל הניחוש:
1. **התאמה מדויקת לפריט במאגר** → הסיווג האמיתי שלו. `🚂 ממלכת הרכבות` אטרקציה, `🌉 Mlýnec` מסעדה — למרות האימוג׳י.
2. **הטבלה הידנית** — האימוג׳י שהמשתמש בחר בכוונה. ⚠️ **גובר על התאמת-שם רופפת**, אחרת `🍽️ KFC קניון פלדיום` היה נחשב קניות רק כי "פלדיום" מופיע בשם.
3. **שם מקום בתוך הטקסט** (הארוך ביותר קודם) — לתחנות שנערכו ידנית.
4. **אימוג׳י חד-משמעי שנגזר מהמאגר** — לשמות חופשיים.
5. `null` → הקטגוריה האחרונה, **מסומן "ללא סיווג"**.

⚠️ **אימוג׳י שמופיע בשתי קטגוריות במאגר נפסל ואינו נגזר** — 🌉 (Mlýnec / גשר קארל), ✡️ (מסעדה כשרה / הרובע היהודי), 🏛️, 🍔, 🍫. **עדיף "לא יודע" מתשובה בטוחה ושגויה**; ההתאמה המדויקת ממילא פותרת את שניהם.

**שתי הכרעות ידניות שנרשמו**: `🧸` עבר **אטרקציות → קניות** (Hamleys הוא חנות; מוזיאון הלגו הוא 🧱 ונפתר לפי שם) · `🥂` נוסף ל**אוכל**.

**המדידה**:

| | לפני | אחרי |
|---|---|---|
| הצעות שנופלות ל"כללי" | **53 / 154 (34%)** | **8 / 154 (5%)** |
| 42 ההוצאות של המשתמש | — | **0 שינו קטגוריה** |
| סכומים לכל קטגוריה | 2198/1250/1219/178/156 | **זהה** |

**שמונה שנשארו — וכולם נכונים**: 🏨 מלון ×3 · 😴 מנוחה · 💊 בריאות · 🔀 שונות · 🌅 ×2. ⚠️ `🌅` משמש גם ל"שייט ערב" (אטרקציה) וגם ל"ארוחת בוקר אחרונה" (אוכל) — **לא הוכרע במכוון**, ואלה שתי תחנות `DAYS` שאינן ב-`ALL_PLACES`.

**תיקון לוואי שנתפס במדידה**: `✡️ בית הכנסת הגדול` ו-`✡️ בית הכנסת הספרדי` היו מסווגים **כאוכל** — כי `✡️` ברשימת האוכל הידנית (למסעדות כשרות). ההתאמה המדויקת מעבירה אותם לאטרקציות.

#### ⚠️ "undefined undefined" ברשימת הצעות ההוצאה — באג שקט מ-15/07 (2026-08-13)
המשתמש צילם את מודל "הוסף הוצאה": תחת 🛍️ קניות הופיעו **9 שורות `undefined undefined`**.

```js
{ label:'🛍️ קניות', items: SHOPS.map(function(r){ return r.e+' '+r.n; }) ... }
```

`r.e`/`r.n` הייתה הסכימה הישנה של החנויות. ב**איחוד ל-`ALL_PLACES` (18/07)** הן הפכו ל-`icon`/`name`, והשורה הזו פוספסה. הבאג חי **חודש**.

⚠️ **למה שום בדיקה לא תפסה אותו**: גישה לשדה שלא קיים מחזירה `undefined` ו**לא זורקת**. כל בדיקות ה-"0 שגיאות JS" עברו בהצלחה כשהמסך הראה ג׳יבריש. **שדה חסר הוא באג שקט** — צריך לחפש אותו בפלט, לא ביומן השגיאות.

**הבדיקה שנוספה** (`undefsweep.mjs`): סורקת את **כל** הטאבים והמודלים ומחפשת `undefined` / `NaN` / `[object Object]` בטקסט הנראה. נמדד לפני: 18 מופעים במודל ההוצאה (9 פריטים × 2 מילים). אחרי: **0 בכל האפליקציה**, 154 פריטים ברשימה.

**הדפוס לפעם הבאה**: כששמות שדות משתנים — `grep` על **שם המערך** (`SHOPS.`) ולא רק על שם השדה, כי אתר הקריאה עשוי להיות רחוק מההגדרה ולהשתמש בשמות קצרים שקשה לחפש (`r.e`).

#### דוח HTML + איחוד הסיווג לטבלה אחת (2026-08-13)

**`exportHtmlReport()`** — כפתור 📄 ליד ה-CSV. קובץ HTML **אחד עצמאי** (18KB), RTL, בהיר/כהה, עם `@media print` לשמירה כ-PDF. אין בו פונטים, סקריפטים או תמונות חיצוניים — הוא ייפתח גם בעוד שנה ובלי רשת.

מה יש בו: מספרי כותרת (בפועל · נותר/חריגה) · **תקציב מול בפועל לפי הקטגוריות של המשתמש** · מפת חום קטגוריה × יום · הוצאה יומית · פירוט מקופל ב-`<details>`.

⚠️ **כל טקסט מהמשתמש עובר `_h()`** — שמות הוצאות מכילים גרשיים ומרכאות (`שטויות של עדי`, `מ"ר`), בדיוק המלכודת ששברה `onclick` פעמיים. כאן זה קובץ סטטי, אבל תו לא-מוברח אחד שובר את המסמך מהנקודה ההיא והלאה.

⚠️ **עמודת הסה״כ היא השנייה ולא האחרונה.** בצילום הראשון היא יצאה **חתוכה** (`,382` במקום `15,382`) — טבלה רחבה מהמסך גוללת, והעמודה האחרונה היא זו שנחתכת. הצמדה לשם הקטגוריה משאירה את המספר החשוב גלוי, והימים נגללים. **נתפס רק כי צילמתי והסתכלתי** — ולא כי חישבתי נכון.

⚠️ **צבע סטטוס אף פעם לא לבד** — לצד כל פס יש תווית מילולית (`✓ בתקציב` · `● קרוב לתקרה` · `⚠️ חריגה`), כי שניים מגווני הסטטוס יושבים מתחת ל-3:1 על רקע בהיר.

**מפת החום היא גוון אחד בהיר→כהה** (12 מדרגות כחול) ולא קשת. תא ריק = `—` ולא 0.

**איחוד הסיווג**: `EXP_CAT_EMOJI` היא עכשיו **הטבלה היחידה**, ו-`expBudgetCat(name, budget)` היא נקודת הסיווג היחידה — `catActualMap` (פסי התקציב) ו-`_expCat` (הייצוא) שניהם קוראים לה. נמדד לפני: **22 אימוג׳י סווגו אחרת** בין המסכים. אחרי: **0**.

- ההתאמה עברה מ-`^` (תחילת השם בלבד) ל-`indexOf` בכל מקום בשם — `"ארוחה 🍽️"` נתפס עכשיו גם בפסים.
- הדוח וה-CSV משתמשים ב**שמות הקטגוריות של המשתמש** (`אוכל ומסעדות 🍽️`) במקום בחמש שהמציאו לעצמם.
- `💡` **הוסר** מרשימת האטרקציות — הוא האימוג׳י של קטגוריית התקציב `כללי ושונות 💡`, ותיוג "כללי" היה נוחת תחת אטרקציות.
- 🧸 הופיע בטעות בשתי רשימות; הוסר מ`קניות` ונשאר ב`אטרקציות` כמו בגרסה הישנה. הבדיקה מוודאת **0 כפילויות** על 68 אימוג׳י.
- הוצאה בלי אימוג׳י מוכר נופלת לקטגוריה **האחרונה** בתקציב — התנהגות הפסים מאז ומתמיד — אבל מסומנת בדוח כ**"ללא סיווג"** ולא נבלעת בשקט.

**אומת מול 42 ההוצאות**: 20 אוכל · 6 אטרקציות · 11 קניות · 2 תחבורה · 3 כללי — זהה לספירה בדוח שהמשתמש ייצא. סכומי הדוח 35,000 CZK / 5,001 ILS, וכל שורה מסתכמת לעמודתה.

#### ⚠️ שתי טבלאות סיווג שונות — התקציב והייצוא לא מדברים אותה שפה (2026-08-13)
קיימות **שתי** פונקציות סיווג בלתי-תלויות, ולשתיהן טבלאות אימוג׳י שונות:

| | `guessExpCat` | `_expCat` |
|---|---|---|
| משמש את | פסי התקציב (`catActualMap`) | ייצוא ה-CSV |
| התאמה | **רק בתחילת השם** (`^`) | בכל מקום בשם |
| מחזיר | `'אוכל'` (מותאם ל-`b.cat` ב-`indexOf`) | `'🍽️ אוכל'` |
| ברירת מחדל | `null` → נופל ל**קטגוריה האחרונה** בתקציב | `'❓ אחר'` מפורש |

**נמדד: 22 אימוג׳י מסווגים אחרת בין שני המסכים.** דוגמאות: `🚌` תחבורה בתקציב ו-`❓ אחר` בייצוא · `🥐` אוכל בתקציב ו-`אחר` בייצוא · `🕍`/`🎭`/`🖼` אטרקציות בייצוא ולא מזוהים בתקציב.

⚠️ **`💡` מסווג בייצוא כאטרקציות** — והוא האימוג׳י של קטגוריית התקציב `כללי ושונות 💡`. תיוג "כללי" יופיע בדוח תחת אטרקציות.

⚠️ **שמות הקטגוריות עצמם שונים**: התקציב מכיר `אוכל ומסעדות 🍽️` והייצוא מייצר `🍽️ אוכל`. הייצוא **לא קורא** את קטגוריות התקציב של המשתמש, ולכן שינוי שם או הוספת קטגוריה בתקציב לא משתקפים בדוח.

**ב-42 ההוצאות בפועל השניים מסכימים** — פרט לשלוש רשומות בלי אימוג׳י (`eSIM`, `מס עירוני`, `חולצות`), ששתי הפונקציות לא מזהות. `חולצות` הוא סיווג שגוי אמיתי (קניות). **לא הומצא ניחוש לפי מילים** — הסיווג נשאר נגזר מהאימוג׳י שהמשתמש בחר, וזו החלטה מ-11/08 שנשמרת.

#### ⚠️ ביקורת שלמות: ארבעה מפתחות נמחקו מענן ריק (2026-08-13)
המשתמש ביקש לוודא ש"שום דאטה לא יעלם ושום פונקציה לא תחרב". הביקורת הריצה את הקוד ומדדה — ומצאה שהתיקון של "היינו" **הגן רק על "היינו"**.

הזרקת snapshot ריק ל-`appdata/main` מ"מכשיר אחר" מחקה בפועל:

| שדה | לפני | אחרי |
|------|------|------|
| `visited` | ✅ שרד (מגן #98) | ✅ |
| `expenses` (listener) | ✅ שרד | ✅ |
| `days_custom` | ❌ **נמחק ל-`[]`** | ✅ |
| `packing_list` | ❌ **נמחק ל-`[]`** | ✅ |
| `budget_categories` | ❌ **נמחק ל-`[]`** | ✅ |
| `remindersDone` | ❌ **נמחק ל-`{}`** | ✅ |

**וזה היה נגיש בפועל**: `saveBudget([])` — מחיקת כל הקטגוריות במודל — דוחפת `"[]"` לענן, ומשם זה מוחק את התקציב בכל מכשיר.

**`_acceptRemote(json, localKey)`** — המגן המשלים ל-`_hasLocalData`. `_hasLocalData` חוסם דחיפת ריק **למעלה**; `_acceptRemote` חוסם קליטת ריק **למטה**. שניהם נחוצים: מספיק מכשיר אחד עם גרסה ישנה כדי שריק יגיע לענן. אם אין נתונים מקומיים — מקבלים כרגיל, אין מה להגן עליו.

⚠️ **המסלול שכמעט פספסתי**: אחרי שהגנתי על 5 ה-`if (d.X)` בסנכרון החי, הבדיקה עדיין נפלה עם `Cannot read properties of undefined (reading 'stops')`. הסיבה: **`loadDaysFromSheets` (שורה ~4288) הוא נתיב קריאה שני ובלתי-תלוי** ל-`days_custom`, שקבע `DAYS_STATE = []` **וגם** דרס את `localStorage`. ה-listener לא היה המקום היחיד.

**הרשימה המלאה — 12 מגנים על 8 שדות בשני סוגי מסלולים** (`onSnapshot` חי + `.get()` בטעינה + מסלולי GAS):
`days_custom` ×2 · `packing_list` ×1 (נתיב ה-`.get()` כבר היה מוגן ע"י `isNewFormat` שדורש `length > 0`) · `budget_categories` ×3 · `remindersDone` ×3 · `remindersSchedule` ×1 · `expenses` ×1 (נתיב המיזוג ב-6755 בטוח מעצם היותו מיזוג).

**חור נוסף בצד הדחיפה**: `saveSchedules()` הייתה הקריאה היחידה ב-`_resyncLocalDataToCloud` שנשארה בלי `_hasLocalData` — מכשיר ריק דחף `remindersSchedule: "{}"`, ושעות התזכורות נמחקו לכולם. אחרי התיקון: מכשיר ריק מבצע **0 כתיבות**.

⚠️ **העלות שנרשמה ולא הוכרעה בשקט**: מחיקה מכוונת של **כל** הפריטים במכשיר אחד לא תתפשט למכשירים האחרים. אותה הכרעה כמו ב-"היינו" — עדיף נתון מיותר על נתון אבוד. מחיקה חלקית עוברת רגיל.

**שלמות פונקציות**: 53 handlers ב-HTML נבדקו מול `window` — **0 שבורים** (`remove`/`stopPropagation`/`push`/`click` הם קריאות מתודה, לא גלובליים). 10 טאבים נפתחים ב-0 שגיאות. ארבע חבילות הרגרסיה (#98/#100/#101 + `regress`) עוברות.

#### הסה״כ נגזר מהקטגוריות — סוף שני מספרים שסותרים (2026-08-13)
המשתמש שאל אם הסה״כ נגזר מהקטגוריות. **הוא לא היה.** היו **שני מספרים בלתי-תלויים**:

```js
var catTotal    = budget.reduce(...);   // סכום הקטגוריות
var manualTotal = loadTotalBudget();    // prague_total_budget — שדה נפרד לגמרי
var totalBudget = manualTotal || catTotal;   // ← הידני גובר תמיד
```

כלומר: העלאת קטגוריה **לא הזיזה** את המספר העליון, והתקציב הכללי **לא התחלק** לקטגוריות. גם "% נוצל" ו"נותר לטיול" חושבו מול הידני. שני המספרים יכלו לסתות בשקט — ואצל המשתמש הם אכן סתרו (ידני 15,000 מול קטגוריות 9,500).

**עכשיו הקטגוריות הן מקור האמת היחיד.** `budgetCatTotal()` מחשב, ו-`renderBudget` מציג אותו ישירות.

**מה לא נמחק**: `prague_total_budget`, `loadTotalBudget()`, `saveTotalBudget()` ו-`appdata/main.total_budget` **נשארו כולם**. `_syncDerivedTotal()` ממשיך לעדכן אותם כ**מראה** של הסכום הנגזר, כדי שגיליון ה-GAS וכל קורא ותיק יראו את אותו מספר. הפרמטר `totalVal` נשאר בחתימת `populateBudgetModal` לתאימות עם המסלול של GAS.

**נקודת כתיבה יחידה**: `_syncDerivedTotal` נקרא מתוך `saveBudget()` עצמו — הפונקציה שכל שינוי קטגוריה עובר דרכה (מודל, הוספה, מחיקה, resync). אין מסלול שמדלג.

⚠️ **המגן החוזר**: `_syncDerivedTotal` פותח ב-`if (!_hasLocalData('prague_budget_v1')) return 0;`. בלעדיו זו **בדיוק** אותה תקלה בפעם השלישית — מכשיר ריק מקבל `DEFAULT_BUDGET` מ-`loadBudget`, והיה דוחף **11,500** מעל התקציב האמיתי. הבדיקה מודדת את זה מפורשות (`emptyDevice_pushed: []` מול סכום ברירת מחדל 11,500).

**במודל**: השדה נעשה `readonly` עם הערה "🔒 מחושב אוטומטית", ומתעדכן **תוך כדי הקלדה** (`_refreshModalTotal` על `oninput` של שדות הקטגוריות) — מה שרואים לפני השמירה הוא בדיוק מה שיישמר.

**התכנסות ולא הד**: `_syncDerivedTotal` דוחף רק כאשר `t !== loadTotalBudget()`. שני מכשירים מחשבים את אותו סכום, ולכן ההעברה מתייצבת אחרי דחיפה אחת. נמדד: קריאות חוזרות → `convergence_extraPushes: []`.

**אומת בהרצה** (`derived.mjs`): מכשיר ריק לא דוחף · ידני סותר 15,000 → מוצג 9,500 · הקלדה 4,000→5,000 מזיזה את הסה״כ ל-10,500 **לפני** שמירה · נשמר ונדחף 10,500 · מחיקת קטגוריה → 10,000 מאליו · 0 שגיאות JS. רגרסיות #98 ו-#100 עדיין עוברות.

#### שני נעצים שלא היו במקום — Palladium ו-Café Savoy (2026-08-12)
המשתמש ביקש לוודא שהנעצים שסימן כ"היינו" באמת במקום הנכון. ביקורת על 15 הנעצים מצאה שניים שלא.

| מקום | היה | הפך ל | הפער |
|------|-----|-------|------|
| `Café Savoy` | `[50.0859, 14.4063]` | `[50.0812, 14.4066]` | **523 מ׳ צפונה** |
| `Palladium` | `[50.0882, 14.4334]` | `[50.0892, 14.4294]` | **306 מ׳ מזרחה** |

⚠️ **חתימת טעות ההקלדה**: ב-Café Savoy **קו האורך היה נכון וקו הרוחב סטה**. כששני הערכים שגויים זו כנראה נקודה לא נכונה; כשרק אחד סוטה — זו הקלדה ידנית. שווה לחפש את הדפוס הזה.

⚠️ **`[50.0812, 14.4066]` היא נקודת הרחוב ולא דלת הקפה** — דיוק ±150 מ׳ לאורך רחוב של ~250 מ׳. נרשם בהערה בקוד ולא הוסתר.

**מה שהפך את זה מהשערה לממצא** — ארבע בדיקות שפיות פנימיות בלתי-תלויות, שכולן התכנסו למרחקי ההליכה האמיתיים אחרי התיקון:

```
Palladium  → כיכר העיר העתיקה   903 → 655 מ׳   (מצופה ~650–700)
Palladium  → Koh-i-noor Labuť   227 → 347 מ׳   (מצופה ~400)
Palladium  → Primark                  891 מ׳   (מצופה ~900)
Café Savoy → גשר קארל           377 → 686 מ׳   (מצופה ~700)
```

⚠️ **מוזיאון האשליות לא תוקן** — התברר שיש בפראג **שני** מוזיאוני אשליות: `Illusion Art Museum` ב-`Melantrichova 536/2` (`50.0851, 14.4217`) ו-`Museum of Fantastic Illusions` ב-`Vodičkova 31`. הנעץ `[50.088, 14.421]` לא תואם אף אחד. **תיקון לכיוון הלא נכון גרוע ממצב קיים** — ממתין לתשובה באיזה מהם היו. זו אותה מלכודת כמו שני ה"קוהינור".

#### Aquapalace — שלוש טענות סותרות על אותו נושא (2026-08-12)
המשתמש שאל אם קו 385 מכוסה בחופשי חודשי. הבדיקה חשפה שהאפליקציה אמרה **שלושה דברים שונים** על הגעה לאקוופארק:

| מקום | מה נטען |
|------|----------|
| תחנת היציאה, יום 5 | "אין חיבור מטרו ישיר — Bolt המומלץ ביותר" · ובטיפים: "אין להסתמך על תחבורה ציבורית" |
| כרטיס הקהילה | "אוטובוס 363 **ישר לפתח הפארק**" |
| כרטיס טיפים, טאב ראשי | "אפשר בתחבורה ציבורית בנוחות! תחנה ממש על הפארק" |

**מה נכון**: קו **385** מותג רשמית `Aquapalace Express` ונוסע מ-`Opatov` (מטרו C) ישירות ל-`Čestlice, Aquapark` ב-15–20 דק׳, בלי לעקוף דרך פרוהוניצה. גם **328** עוצר שם. ⚠️ **קו 363 לא עוצר בפארק** — הוא ו-357 עוצרים ב-`Čestlice, V Oblouku`, כלומר ההמלצה הישנה הייתה מורידה אותם במקום הלא נכון.

⚠️ **החופשי חודשי מכסה P/0/B בלבד**, וצ׳סטליצה מחוץ לפראג — צריך כרטיס משלים. **לא אומת אם היא אזור 1 או 2**, ולכן נרשם בטיפים לקנות באפליקציית PID Lítačka או אצל הנהג ולראות את המחיר — במקום לנקוב בסכום שלא אומת.

**הדפוס**: כשעונים על שאלת תחבורה — **לבדוק את כל המקומות שבהם הנושא מוזכר באפליקציה**, לא רק את זה שהמשתמש שאל עליו. שלוש טענות סותרות שרדו כאן חודשים כי אף אחת מהן לא נבדקה מול השנייה. `grep` על שם המקום + מילות מפתח תחבורתיות.

#### שוק בערב — למה רק Manifesto (2026-08-11)
המשתמש ביקש שוק בערב ברביעי (12/8, יום Aquapalace). **כל שווקי האיכרים סוגרים לפני הערב**:

| שוק | שעות |
|------|------|
| `Pražská tržnice` (Hala 22) | ב׳–ו׳ 07:30–**17:00** · שבת עד 14:00 · ראשון סגור |
| `שוק האיכרים Jiřák` | רביעי–שישי 08:00–**18:00** · שבת עד 14:00 |
| `שוק האיכרים Náplavka` | **שבת בלבד** 08:00–14:00 |
| `Havelské tržiště` | כל יום עד 19:00 — אבל שוק מזכרות, לא ערב אמיתי |
| **`Manifesto Market — Anděl`** | **11:00–22:00 בימי חול, עד 23:00 בסופ״ש** ← היחיד שפועל בערב |

שובץ Manifesto ליום 5. אומת שהוא **עדיין פעיל** (היה ספק לגבי סניף Anděl) — פתוח, עם אירועים מתוזמנים ל-2026.

⚠️ **המרחק נרשם בכרטיס ולא הוסתר**: 6.7 ק״מ מהמלון ובכיוון ההפוך, מטרו A→B עם החלפה ב-Můstek, ~30–35 דק׳ לכיוון. אחרי יום שלם בפארק מים זה שיקול אמיתי. **הדפוס**: כשממליצים על משהו עם עלות נסתרת (מרחק, החלפת קו, שעה) — מציגים אותה למשתמש במקום להכריע בשקט לטובת "זה נחמד".

#### Koh-i-noor — עפרונות ביום הקניות (2026-08-11)
המשתמש שאל על "רחוב של חברת עפרונות". התשובה חשפה **בלבול אמיתי** שכדאי שיישאר מתועד: יש **שני** "קוהינור" צ׳כיים.

| | מה זה | איפה |
|---|---|---|
| **Koh-i-noor Hardtmuth** | עפרונות, מ-1790 | צ׳סקה בודיוביצה |
| **Koh-i-noor Waldes** | כפתורי לחיצה וסדקית מתכת, נוסד 1902 | פראג, `Vršovická 478/51` — **ותחנת חשמלית על שמו** |

המפעל שרואים בפראג הוא Waldes ו**מעולם לא ייצר עפרונות**. זו טעות נפוצה מספיק שהעיתונות הצ׳כית כתבה עליה כתבות. האזהרה נרשמה בטיפים של התחנה כדי שלא ייסעו לוורשוביצה בחיפוש עפרונות.

**מה נוסף**: `Koh-i-noor Hardtmuth — Bílá Labuť` — גם כרשומת `type:'shop'` וגם כתחנה מלאה ביום 7 (יום הקניות), מיד אחרי Palladium ובמרחק 227 מ׳ ממנו, **בלי לשנות את סדר התחנות הקיים**.

| מקור | קואורדינטה |
|------|-------------|
| ויקיפדיה | `50°05′25″N 14°26′02″E` → `50.090278, 14.433889` |
| mapaobchodu | `50.090069, 14.434070` |

הפרש **27 מ׳**, וה-DMS מעוגל לשנייה שלמה (±15 מ׳ מובנה) — נלקח האמצע `[50.0902, 14.4340]`. 0 התנגשויות.

**למה דווקא הסניף הזה** מבין ארבעה בפראג: (1) היחיד שהצלחתי לאמת משני מקורות; (2) הגדול בפראג — 117 מ״ר ו-4,700 פריטים מול 52 מ״ר ו-1,000 בסניף Na Příkopě.

⚠️ **סניף `Na Příkopě 860/24` לא נוסף ל-`PLACE_COORDS`** — הוא יושב בדיוק על מסלול ההליכה של יום 7, אבל **לא נמצאה לו קואורדינטה משני מקורות**. הוא מוזכר בטיפים בלבד, ויש הערה מפורשת בקוד לא לנחש לו קואורדינטה. הבדיקה מאמתת שאין לו רשומה. **זה הדפוס הנכון** — מקום שימושי שלא ניתן לאמת נכנס כטקסט, לא כנעץ.

**מדידה**: קניות 8/8 → **9/9**, תחנות 33 → **34** וכולן מועשרות, פופ-אפ 1044 תווים, 0 שגיאות JS, 0 זוגות כפילות חדשים.

#### כשל רשת ≠ קישור שבור — CI (2026-08-11)
שלוש ריצות אדומות ברצף, ובכל אחת נפל אתר **אחר** ותמיד ב-timeout: `prague2go.co.il`, ואז `jewishmuseum.cz` **שעבר בהצלחה שתי דקות קודם**. `main` היה ירוק כל אותו זמן, והשינוי בענף נגע רק לנתוני `app.html`.

הניסיון הראשון — להוסיף את `prague2go` ל-soft list — **הוחזר**: זה משחק חתול-ועכבר שבו כל ריצה מפילה דומיין אחר, והרשימה מתנפחת עד שהבדיקה כבר לא בודקת כלום.

**התיקון**: להבחין בין שני סוגי כשל.
- **כשל קישור** — השרת ענה `404`/`410`/`500`. המשאב באמת לא קיים → **ממשיך להפיל build**, וזו כל מטרת הבדיקה.
- **כשל רשת** — curl לא קיבל שום תשובת HTTP (`000`/ERR) → ניסיון אחרון ארוך (45 שנ׳), ואם גם הוא שותק — **אזהרה בלבד**.

נבדק על 8 מקרים לפני הדחיפה: `200`/`302` עוברים · `404`/`500` מפילים · `ERR`/`000` מזהירים · `ERR`→`200` עובר · `ERR`→`404` מפיל.

⚠️ **`prague2go.co.il` עדיין חי ב-`app.html`** (~שורה 2128, כפתור "🌐 אתר" בכרטיס המדריכים) ולא הגיב בשלוש ריצות. אם לא יחזור — להסיר מהאפליקציה, לא רק להשתיק את ה-CI.

#### עדכון יום 4 למה שקרה בפועל (2026-08-11)
המשפחה עשתה ביום 4 משהו אחר לגמרי ממה שהיה בלוז. הלוז עודכן כדי שהתיעוד — ולוח הסיכום בסוף — ישקפו את המציאות.

| יום | היה | הפך ל |
|-----|-----|--------|
| **4** (11/8) | טירת פראג · Mala Strana · אי קמפה · Čertovka | **הרובע היהודי — Josefov · בית הכנסת ירושלים · Café Savoy** |
| **8** (15/8) | ארוחת בוקר · צ'ק-אאוט (יום כמעט ריק) | + **ארבע תחנות המלכים** בין השתיים |

יום 8 נבחר כי הטיסה ב-23:45 והצ'ק-אאוט ב-12:00 — יש יום שלם פנוי, ואפשר להשאיר מזוודות בלובי. ה-`story` של היום עודכן להסביר בדיוק את זה.

**כל שלוש התחנות החדשות נכתבו מלאות** — `details`, `tips`, `booking`, `google`, `duration`, `who`, `mapUrl` — ולא כתחנות רזות. לכולן יש קואורדינטה, ולכן כל הכפתורים פעילים: נווט · ממני · היינו · ⏰ שעה · ↔ העבר יום.

⚠️ **תחנות המלכים הועברו verbatim** ולא נכתבו מחדש — כך לא אובד תוכן ולא נוצר הבדל בין שתי גרסאות של אותה תחנה.

**מדידה**: 29 → **32 תחנות**, כולן מיוצגות במפה עם תג יום, 0 חסרות פרטים.

#### תצוגת "היום" בטאב ימים (2026-08-11)
- `TRIP_START = new Date(2026, 7, 8)` + `tripDayNumber()` — מחשב את מספר יום הטיול מהתאריך בפועל. ⚠️ **חייב תאריך מקומי ולא UTC**: `new Date('2026-08-08')` הוא חצות UTC, ובפראג זה כבר 03:00 באותו יום — חיסור timestamps היה מחזיר יום שגוי בשעות הקטנות. אומת ב-00:30 מקומי
- `_autoSelectToday()` נקראת מ-`renderDays()` ופותחת את **היום הנוכחי** במקום יום 1. `selectDay()` מדליקה `_daySelectedManually` — אחרי בחירה ידנית, רינדור מחדש (סנכרון ענן, סימון "היינו", חזרה לטאב) **לא** מקפיץ בחזרה להיום
- לפני הטיול → יום 1 · אחרי → היום האחרון
- **תגים בכותרת**: `● היום` (כתום) · `✓ עבר` (אפור) · `בעוד N ימים` (כחול)
- **כדורי הימים**: יום שעבר מציג ✓ במקום המספר, מתעמעם ומאבד רקע לבן; היום הנוכחי מקבל טבעת כתומה גם כשאינו הנבחר
- ⚠️ **עריכה בימים שעברו נשארת פתוחה במלואה** — הוספה, הזזה בין ימים, שינוי שעה. הסימון הוא ויזואלי בלבד ולא נעילה

**באג נסתר שנתפס תוך כדי**: היה `<div id="dayButtons">` **סטטי וריק** ב-HTML בנוסף לזה ש-`renderDays` מייצר בתוך `#dayCard` — **id כפול**. `getElementById` החזיר תמיד את הריק, ולכן המגן ב-`initDaySwipe` ("אל תחליף יום כשגוררים את שורת הכדורים") בדק אלמנט ריק ו**מעולם לא פעל**. הסטטי הוסר.

#### בית הכנסת ירושלים — פער שהתגלה בחיפוש (2026-08-11)
המשתמש חיפש "בית כנסת ירושלים" — **לא היה באפליקציה בכלל**. הסיבה: הוא **לא ברובע היהודי** אלא ב-Nové Město, ולכן לא נכלל לא בששת אתרי Josefov ולא בכרטיס המשולב של Jewish Museum.

- `בית הכנסת ירושלים — Jeruzalémská` — הגדול בפראג (1906, Wilhelm Stiassny), מאורי + ארט נובו, 850 מקומות
- **קואורדינטה אומתה משני מקורות זהים**: ויקיפדיה `50°05′05″N 14°25′55″E` + unesco-czech.cz `50°5'4.96"N 14°25'55.13"E` → `[50.0847, 14.4319]`. מרחק 163 מ׳ מ-`Praha Exchange — Jindřišská` ו-214 מ׳ מ-`מוזיאון החושים` — עקבי עם הכתובת Jeruzalémská 1310/7, ו-~1.1 ק״מ מאתרי הרובע (כלומר בבירור לא כפילות)
- **כרטיס נפרד** 150/100 CZK, אין הזמנה מראש ואין בידוק. פתוח אפריל–אוקטובר 09:00–18:00
- ⚠️ **סגור בשבת ובחגי ישראל** — בטיול הנוכחי 8/8 ו-15/8 הם שבתות, כלומר הביקור אפשרי רק ב-9–14/8

#### שחזור "הרובע היהודי" + ביקורת מה עוד ירד (2026-08-11)
המשתמש חיפש תחנה בשם "הרובע היהודי" ולא מצא. **היא באמת הייתה** — בקומיט `493b113`, כתחנת `DAYS` מלאה. אבל:

> ⚠️ **`493b113` אינו אב-קדמון של `main`** — קו היסטוריה נפרד שמעולם לא מוזג. התוכן נכתב בגרסה מקבילה של האפליקציה ולא "נמחק" מכאן.

**ביקורת מלאה מול הגרסה ההיא** (`git show 493b113:app.html`):

| מערך | ישן | חדש | חסר באמת |
|-------|-----|-----|-----------|
| `DAYS` | 31 | 29 | **1** (`הרובע היהודי`) — 4 קיימים תחת שם אחר, 2 הן פעולות שמכוסות ע"י `צ'ק-אאוט ויציאה לשדה` |
| `ALL_PLACES` | 83 | 114 | **0** — 5 ה"חסרים" הם שמות שאוחדו במכוון ב-#80 |
| `HISTORY_SPOTS` | 12 | 12 | 0 |
| `REMINDERS` | 17 | 16 | 1 (`כרטיסים ל-Majaland Prague`) — **לא שוחזר במכוון**: Majaland קיים בבנק אבל **אינו בלוז**, ותזכורת "לקנות כרטיסים עד תאריך" למשהו לא מתוכנן היא משימה מדומה |

**מה שוחזר**: `הרובע היהודי — Josefov` כרשומת `community`/`אטרקציות` מלאה — תיאור, הזמנה (600/200 CZK), משך, מתאים ל, טיפים ותחבורה. בכך הוא מופיע בבנק, במפה, ב"קרוב אליי", ניתן להוספה ללוז ולסימון "היינו".

⚠️ **הקואורדינטה אינה זו שבקומיט הישן** (`[50.0904, 14.4183]`) — היא כמעט זהה לבית הכנסת הגדול והייתה יוצרת זוג שנראה ככפילות. במקום זה: **מרכז המסה של ששת אתרי הרובע שכבר אומתו במאגר** → `[50.0897, 14.4182]`, במרחק 47–197 מ׳ מכל אחד מהם — סמן של **אזור** ולא של מבנה. 0 התנגשויות קואורדינטה.

**הלקח**: לפני שמסיקים "פיצ'ר נמחק" — לבדוק `git merge-base --is-ancestor`. תוכן שקיים בקומיט **לא אומר** שהוא היה אי פעם ב-`main`.

#### רזולוציית הייצוא (2026-08-11)
תמונה מיוצאת נראתה מטושטשת בהגדלה. **המקור לא היה הטקסט** (הוא וקטורי וחד ממילא) אלא **האריחים**: הם צוירו ב-256px ונמתחו פי 2.

- **`_tripMapGeo(pts, w, h, density)`** — בוחר zoom שנותן `density`× יותר פיקסלי-אריח מגודל הפאנל, ומחזיר `k = 1/density` (יחידות-קנבס לפיקסל-אריח). האריח מצויר בגודל `256*k`, ואחרי `ctx.scale(scale)` זה יוצא **256 פיקסלי-מכשיר — 1:1, בלי מתיחה**
- **scale אדפטיבי**: 3× כשהשטח מתחת ל-~20MP, אחרת 2×. ⚠️ iOS מגביל את **שטח** ה-canvas ולא רק את הצלע, ומחזיר canvas **ריק בלי לזרוק** — לכן התקרה, לא ניסוי וטעייה
- **צפיפות אדפטיבית**: כל הכפלה בצפיפות כמעט מרבעת את מספר האריחים. בצפיפות 3 נמדדו **74 אריחים** — מעל התקרה, וה-`_drawTripMapTiles` פשוט ויתר, כלומר **המפה לא נטענה בכלל**. `_tripTileCount(L)` מחשב מראש, ויורדים בצפיפות עד ≤44

**מדידה**: 10 מקומות → 3240×5514 (17.9MP, 1.28MB, 36 אריחים, zoom 15 במקום 14) · 40 מקומות → 2160×8236 (17.8MP, נפילה מבוקרת ל-2×, 36 אריחים).

#### מפה אמיתית בלוח הסיכום (2026-08-11)
הגרסה הראשונה ציירה נקודות על רקע ריק — נראה כמו scatter plot, לא כמו פראג. עכשיו הרקע הוא **אריחי OpenStreetMap אמיתיים**.

- **`_lon2tileF` / `_lat2tileF`** — Web Mercator, אותה מתמטיקה של Leaflet/OSM. ⚠️ הפרויקציה הקודמת הייתה שטוחה (equirectangular); עם אריחים היא הייתה מציבה את הנעצים במקום הלא נכון
- **`_tripMapGeo`** בוחר zoom שבו כל הנקודות נכנסות לפאנל (עד 16, ברירת מחדל 14 לפראג), וממרכז אותן
- **`_drawTripMapTiles`** מצייר ל-canvas בתוך clip מעוגל. ⚠️ **`crossOrigin='anonymous'` חובה** — בלעדיו ה-canvas נהיה tainted ו-`toBlob` זורק `SecurityError`. `tile.openstreetmap.org` מחזיר `Access-Control-Allow-Origin`
- תקרת 24 אריחים + timeout של 9 שנ׳; **בלי רשת הלוח עדיין נבנה** (אומת: 443KB PNG בלי אריחים)
- **ייחוס OSM** מוצג בתחתית פאנל המפה — חובה לפי תנאי השימוש
- הארכיטקטורה: canvas מצייר רקע → אריחים → **ה-SVG מעל**. ה-SVG מצייר רקע רק **מחוץ** לפאנל, אחרת מלבן אטום היה מוחק את האריחים
- התצוגה על המסך היא ה-canvas עצמו — WYSIWYG, מה שרואים הוא בדיוק מה שמיוצא

⚠️ **באג מספור שנתפס**: מספרי הנעצים נלקחו מהאינדקס בתוך רשימת הנקודות, והשורות מהרשימה המלאה. ברגע שלמקום אחד אין קואורדינטות — כל המספרים במפה מוסטים מול הרשימה. נשמר עכשיו האינדקס המקורי.

#### שם קנוני לכל נקודה — סוף הכפילויות (2026-08-11)
**הבעיה שנשארה**: ב-`PLACE_COORDS` יש 126 מפתחות אבל רק ~98 נקודות פיזיות. **17 קבוצות, 41 שמות** מתארות את אותה נקודה תחת שמות שונים — הכינויים שנוצרו כדי שתחנות `DAYS` יקבלו מיקום. התוצאה שהמשתמש ראה בלוח הסיכום: `Výtopna` ו-`Výtopna — מסעדת הרכבות` כשתי שורות, וכך גם `מצודת פראג` מול `טירת פראג וקתדרלת ויטוס`. וגרוע מזה — סימון "היינו" מכרטיס אחד **לא השתקף** בשני.

- **`_canonMap()`** — מקבץ את כל מפתחות `PLACE_COORDS` לפי קואורדינטה ובוחר שם קנוני לכל נקודה:
  1. שם שקיים ב-`ALL_PLACES` (הרשומה האמיתית) — הקצר מביניהם
  2. אחרת **הארוך ביותר** — בקבוצת המלון כל השמות הם תחנות פעולה (`חזרה למלון ומנוחה`), והארוך הוא `Comfort Hotel Prague City East`, השם היחיד שמתאר מקום. הקצר היה נותן כותרת מגוחכת בלוח
- **`isVisited` / `toggleVisited` עוברים דרך `_canonicalName`** — סימון מכל כינוי מתייחס לאותה נקודה, ומשתקף בכל המשטחים
- **מיגרציה אוטומטית** ב-`getVisitedState()`: מפתחות ישנים מומרים לקנוני, ובהתנגשות נשמר ה-timestamp **המוקדם** ("מתי היינו שם"). נשמר חזרה מיד — בלי מיגרציה ידנית
- ⚠️ **`_normalizeVisited` חייב לרוץ גם על מה שמגיע מהענן**. ה-listener הציב `VISITED_STATE = JSON.parse(d.visited)` ישירות ועקף את המיגרציה — מכשיר אחר ששמר תחת כינוי, הסימון שלו פשוט לא נראה. נתפס ברגרסיה (`remote applied: false`) ותוקן

**מדידה**: 9 שמות מסומנים → **5 מקומות** בלוח. סימון מכינוי ↔ שם קנוני משתקף בשני הכיוונים.

#### כפתור "היינו" (2026-08-10)
- `VISITED_STATE` — אובייקט `{שם מקום: timestamp}`, מקור יחיד לסימון מקומות שכבר ביקרנו בהם
- **ממופתח לפי שם ולא לפי אינדקס** — אינדקסים ב-`ALL_PLACES` זזים בכל הוספת מקום, והסימון היה עובר למקום אחר. ה-timestamp נשמר כדי שאפשר יהיה להציג "מתי היינו" בעתיד בלי מיגרציה
- `isVisited(name)` · `toggleVisited(name)` · `visitedCount()` · `visitedBtnHtml(name, variant)` — `variant:'icon'` לכרטיסי קהילה (כפתור צר ליד נווט/ממני), `'block'` לכרטיסי מסעדות (שורת הפעולות)
- **event delegation** — listener אחד על `document` עם `.closest('.visitedBtn')` ו-`data-vn`, **לא** `onclick` inline: שמות מקומות מכילים גרשים ומרכאות (`מגדל פטז׳ין`, `מ"ר`) וזו בדיוק המלכודת ששברה `onclick` בעבר
- **סנכרון**: `localStorage['prague_visited_v1']` קודם, ואז `appdata/main.visited` — אותו דפוס של `saveDaysState`. יש listener ב-`initRealtimeSync`, והשדה נדחף גם ב-`_resyncLocalDataToCloud` אחרי כניסה
- `refreshVisitedUI()` מרנדר מחדש את הטאב הפעיל (כולל `days`, ש-`refreshScheduleBadges` מדלגת עליו בכוונה)
- **כיסוי מלא — 110/110 כרטיסים**: אטרקציות 56/56 · מסעדות 26/26 · קינוחים 11/11 · צילום 10/10 · קניות 7/7, **וגם** בפופ-אפ פרטי התחנה (`showStopDetail`), בכרטיס התחנה בטאב ימים (תג ✅), ובדיאלוג הנעץ במפה
- **סימון מכל משטח משתקף בכולם**: `refreshVisitedUI()` מעדכן כפתורים במקום (`outerHTML` — תופס גם דיאלוג/פופ-אפ פתוח), מרנדר את הטאב הפעיל, מרנדר את `days` בנפרד, **ומצייר מחדש את נעצי המפה** כדי שהנעץ הירוק יתעדכן בלי מעבר טאב

#### פרטי אטרקציה שנעלמו בפופ-אפ (2026-08-10)
- `showDayTimePicker(emoji, name, desc)` קיבל `desc` כפרמטר ו**מעולם לא הציג אותו** — הפופ-אפ הראה רק emoji+שם (מקוצר ל-26 תווים). בלי תיאור, דירוג, משך או הזמנה, אי אפשר היה להחליט לאיזה יום לשבץ בלי לצאת ולחזור
- חמור יותר: בשמירה הוא דחף תחנה **רזה** — `{emoji, name, time, desc, mapUrl:''}` בלבד — ואיבד `details`/`tips`/`booking`/`google`/`duration`/`who`/`mapUrl`. `saveAddCommunityStop` לעומתו שמר את הרשומה המלאה. תוצאה: כל מקום שנוסף מבנק האטרקציות נפתח אח"כ ב"ימים" ככותרת ריקה
- `showStopDetail` ו-`renderDays` עשו fallback רק ל-`google`/`duration`/`who`, ורק מול `COMMUNITY`/`RESTAURANTS` — לא מול `ALL_PLACES` המלא, ולא ל-`details`/`tips`/`booking`/`mapUrl`
- **התיקון**: `_placeToStopFields(rec)` מנרמל את סכימות ה-type השונות (community: `how`/`booking`/`tips`־string · shop: `hours`/`metro`/`brands` · dessert: `where`/`rating` · photo: `best`/`crowds`/`fee_txt`) לשדות של תחנה. `_stopFallback(name)` מאתר ב-`_uniquePlacesForMap()`, ו-`_withPlaceDetails(stop)` ממלא **רק שדות ריקים** כדי לא לדרוס עריכות של המשתמש
- מוחל בשלושה מקומות: `showStopDetail`, `renderDays`, ובשמירה של `showDayTimePicker`. **ההשלמה בזמן תצוגה מתקנת גם תחנות שכבר נשמרו רזות בענן** — בלי מיגרציה של הנתונים
- הפופ-אפ של בחירת היום מציג עכשיו תיאור מלא + צ׳יפים של ⭐ דירוג / משך / מתאים ל / הזמנה

#### תיקון גלילה במודל "הוסף תחנה" (2026-08-10)
- `#addStopModal` היה ה-bottom sheet **היחיד** בקובץ בלי `max-height`+`overflow-y`. תוכן הגיליון ~730px — גבוה ממסך של רוב הטלפונים — ועם `align-items:flex-end` הכותרת וטאבי הבנק נחתכו **מעל** גבול המסך בלי אפשרות לגלול אליהם (קונטיינר `position:fixed`). נמדד ב-Chromium: 62px חתוכים ב-375×667, 65px ב-390×664, 14px ב-414×715
- תוקן: `max-height:88vh;overflow-y:auto;overscroll-behavior:contain;box-sizing:border-box` על הפאנל
- רשימת הבנק (`#attrBankRow`) הוגדלה מ-220px ל-`min(42vh,300px)`, והסגנון שלה רוכז ב-`BANK_ROW_STYLE` — `buildBankCards` הייתה דורסת את ה-`cssText` בכל החלפת טאב ומוחקת בשקט מאפיינים שהיו ב-HTML, ו-`buildBankRemCards` בכלל לא הציבה סגנון (ירשה את של הטאב הקודם)
- שורת הכפתורים בכרטיס תחנה קיבלה `flex-wrap:wrap` — היא גדלה בכפתור "📍 ממני" ובמסך צר גלשה

#### תמונות + טאב מיקום (2026-07-18)
- **תמונות מסעדות**: כרטיסי מסעדה עם תמונה 150px — gradient+emoji fallback, Wikimedia Commons אם קיים ב-PLACE_IMGS
- `restaurantImgHtml(r)` — helper שמחזיר HTML של תמונה עם onerror fallback
- `PLACE_IMGS` — lookup לפי שם מקום → URL תמונה (Wikimedia Commons)
- `PLACE_COORDS` — קואורדינטות [lat,lng]; **126 מפתחות, כיסוי מלא: 98/98 שמות ב-`ALL_PLACES` + 29/29 שמות תחנות ב-`DAYS`** (2026-08-10). ממופתח לפי **שם מדויק**, ולכן יש בו גם כינויים לשמות תחנות שלא זהים לשם ב-`ALL_PLACES`. תחנות "פעולה" (מנוחה במלון, צ'ק-אאוט) ממופות לעוגן ההגיוני שלהן (המלון). כל קואורדינטה **אומתה משני מקורות בלתי-תלויים לפחות** — אין ניחושים (ראה הסקיל לטבלת המקורות)
- `PLACE_IMGS` — 35+ תמונות Wikimedia Commons לפי שם מקום (~שורה 4494)
- **טאב מיקום** (`location`) — מפת Leaflet+OpenStreetMap, כפתור "📍 אתרו אותי" (Geolocation API)
- **מעקב מיקום חי** (2026-08-10) — הכפתור הוא toggle על `watchPosition`: המרחק/כיוון/מיון מתעדכנים תוך כדי הליכה, נעץ יחיד שזז + עיגול דיוק. throttle לרשימה (3 שנ׳ / 15 מ׳) ועצירה אוטומטית ביציאה מהטאב וב-`visibilitychange` — סוללה
- Pins צבעוניים: כתום=בלוז, כחול=מסעדות, ירוק=אטרקציות, סגול=אתם כאן, צהוב=קניות
- רשימת מקומות קרובים ממוינת לפי מרחק Haversine + פילטר קטגוריה
- **כיוון (מצפן) יחסי למיקום הנוכחי** (2026-08-10) — `bearingTo()` (forward azimuth) + `compassFor()` (8 רוחות, חץ + שם בעברית). מוצג גם בכרטיס ברשימת הקרובים וגם בדיאלוג הנעץ, **רק כשיש GPS פעיל**. החץ מצביע לכיוון גאוגרפי (⬆️=צפון) — לא בוסולה שמתחשבת בכיוון שאליו הטלפון מופנה
- **de-dupe לתצוגה**: `_uniquePlacesForMap()` — 15 מקומות כפולים ב-`ALL_PLACES` ציירו נעצים כפולים; ממוזג בשכבת התצוגה בלבד (לא ב-`ALL_PLACES`, אחרת טאב קהילה נשבר). ראה סקיל לפרטים
- פילטר אטרקציות: `.indexOf('אטרקציות')` על `cat` — לא על `type` — מונע הצגת מסעדות/קינוחים community
- תג "✅ כבר בלוז" גם ברשימת הקרובים

#### Light Theme v7 (2026-07-18)
- **עיצוב בהיר שולט** — לבן (`#ffffff`) בכרטיסים, מודאלים, nav, sidebar
- **body/html**: `#F7F3EE` / `#FAF7F4` gradient (במקום beige כהה)
- **`--c-surface`**: `#F7F3EE` (עדכון טוקן)
- **טיסות**: שני כרטיסי הטיסה (הלוך/חזור) שוכתבו לגמרי — רקע לבן עם border צבעוני (`rgba(244,99,74,0.18)` / `rgba(59,130,246,0.18)`)
- **כדורי ימים (day pills)**: כדורים לא-פעילים עכשיו `#ffffff` עם border דק (במקום `rgba(0,0,0,0.08)`)
- **מודאלים (bottom sheets)**: כולם `#ffffff` (במקום `#F5EFE6` beige)
- **Nav + sidebar**: `rgba(255,255,255,0.95)` backdrop (במקום `rgba(237,228,216,0.97)`)
- **login screen**: `#FAF7F4` (במקום `#EDE4D8`)
- **header**: **נשאר כהה בכוונה** — skyline של פראג בלילה כ-accent עיצובי
- **גבולות**: `rgba(255,255,255,0.14/0.15)` → `rgba(0,0,0,0.10)` (replace_all — גבולות לבנים היו בלתי נראים על רקע לבן)

#### Premium UI — Design System v6 (2026-07-18)
- **Phase 0**: CSS Design System — `:root` tokens (`--c-primary`, `--c-card`, `--r-card`, `--s-card`, `--t-fast` etc.), keyframes (`fadeSlideUp`, `scaleIn`, `skeletonPulse`), DS classes (`.ds-badge-{red,amber,green,blue,gray}`, `.ds-chip`, `.ds-fab`, `.ds-stat`, `.ds-section-hdr`, `.skeleton`, `.btn-primary` gradient, `.btn-ghost`)
- **Phase 1**: Bottom nav — `<button class="nav-item">` with `.nav-icon` / `.nav-label` children; `openTab()` toggles `.active` class (CSS handles color + scale + backdrop-blur pill)
- **Phase 2.1**: ימים — stop cards use DS tokens, numbered circle markers on timeline, time chip in primary color, `.ds-badge-amber` for Google rating, `.btn-primary` for "הוסף תחנה", `.ds-section-hdr` with stop count
- **Phase 2.2**: תזכורות — priority right-border per card (red/amber/blue), urgency chips use `.ds-badge-*`, primary action uses `.btn-primary`, links use `.btn-ghost`
- **Phase 2.3**: הוצאות — removed stray "ביטול" button; summary stats use `.ds-stat`; export button gets 📤 icon
- **Phase 2.4**: ציוד — "איפוס" button moved from header to progress bar row (red-tinted), same ID for JS listener
- **Phase 2.5**: טיסות — two info chips (check-in timing, airport arrival) above flight cards

#### מסד נתונים אחיד לתזמון (2026-07-18)
- **`DAYS_STATE`** = מקור יחיד של אמת לכל תזמון תחנות (קהילה, מסעדות, קניות, צילום, קינוחים)
- **`remindersSchedule`** = נשאר נפרד — תזכורות הן *משימות* לפני הטיול, לא תחנות ב-DAYS
- הוסר `restaurantsSchedule` + `openEditRestaurantSchedule` + כפתור "⏰ קבע זמן" הישן
- `saveSchedules()` שומר רק `remindersSchedule`

#### כניסה — OTP מספרי, בלי magic link (2026-08-07)
- מסך הכניסה שולח קוד בן 6 ספרות למייל (`sendLoginCode()` → GAS `sendOtpCode` דרך JSONP) והמשתמש מקליד אותו (`verifyLoginCode()` → GAS `verifyOtpCode`) — **אין** לחיצה על קישור בכלל
- מוגבל ל-4 מיילי המשפחה — **בצד שרת בלבד** (`FAMILY_EMAILS` ב-`Code.gs`, repo פרטי). הקליינט לא מכיל רשימת מיילים — הוסרה מ-app.html מטעמי אבטחה (הייתה hardcoded ונגישה לכל מי שגולש ל-repo הציבורי); בצד קליינט נבדק רק שהמייל תקין בפורמט
- הקוד עצמו + התוקף (10 דק') מנוהלים ב-GAS backend (`Prague-2026-backend/gas_project/Code.gs`) — לא ב-Firebase
- כניסה מוצלחת (`verifyOtpCode`) מחזירה גם Firebase custom token — הקליינט קורא `signInWithCustomToken()` לפני הצגת האפליקציה. **קריטי**: בלי זה, הכניסה "מצליחה" אבל שום שמירה ל-DB לא עובדת (Firestore דוחה anonymous auth) — ראה `Prague-2026-backend/CLAUDE.md` → "Firebase custom token"
- **`ensureFirebaseAuth()` ממתין ל-`onAuthStateChanged`** ולא קורא `currentUser` סינכרונית (PR #63) — Firebase משחזר session אסינכרונית, אז קריאה סינכרונית מִמְשָה `false` לצמיתות וכל טעינה ראשונית נדחתה (תג "⚠️ מקומי"). אחרי כניסה `_resetAuthAndSync()` מאפסת את ה-memoization ומפעילה realtime sync מחדש
- **`ensureFirebaseAuth()` לא נופל יותר ל-`signInAnonymously()`** (PR #61) — Firestore דוחה anonymous תמיד, אז ה-fallback רק ייצר "הצלחה למראה" שנכשלת בשקט בכל כתיבה. **אל תחזירו אותו.** גם ה-IIFE בטעינת העמוד מוודא `!user.isAnonymous` עם `onAuthStateChanged` לפני שמדלג על מסך הכניסה
- **`_resyncLocalDataToCloud()`** (PR #61) — אחרי כניסה אמיתית, דוחפת לענן את כל מה שנצבר ב-localStorage בזמן שה-session היה שבור (הוצאות/ימים/ציוד/תזכורות/תקציב), דרך פונקציות השמירה הקיימות
- **`_showAppWhenReady()`** (PR #63) — כל הצגת UI מתוך callback אסינכרוני (כמו `onAuthStateChanged`) חייבת לבדוק `document.readyState`; `addEventListener('DOMContentLoaded')` על אירוע שכבר קרה לא יורה לעולם, וזה השאיר את מסך הכניסה תקוע למרות session תקף
- **זכירת מכשיר ל-7 ימים** (PR #62) — `SESSION_KEY` ב-`localStorage` עם timestamp תפוגה (`_setDeviceRemembered`/`_isDeviceRemembered`), במקום `sessionStorage` שנמחק בכל סגירת טאב. הדגל לבדו אף פעם לא מספיק — תמיד מאומת מול Firebase Auth
- **הוחלף**: הגישה הקודמת דרך Firebase Email Link (`sendSignInLinkToEmail`) הוסרה — הייתה שבירה כשהקישור נפתח בדפדפן/מכשיר אחר מזה ששלח את הבקשה
- דורש deploy נפרד ל-GAS backend (`clasp push && clasp deploy`) — ראה `Prague-2026-backend/CLAUDE.md`
- **כפתור "🚪 התנתק מהמכשיר"** (טאב ראשי) → `doLogout()` — מוחק את הדגל ב-localStorage + `signOut()` מ-Firebase ומרענן. לא מוחק נתונים (מקומית או בענן), רק מנתק גישה
- **✅ אימות אבטחה (2026-08-07, נבדק אמפירית מול הפרויקט החי)**: ה-`apiKey` פומבי וספק email/password פעיל, כך שכל אחד **יכול ליצור חשבון** ב-Firebase Auth של הפרויקט. אבל **Firestore Rules חוסמות אותו**: נרשם חשבון עם מייל אקראי לא-משפחתי → קריאה מ-`appdata/main` החזירה `403 PERMISSION_DENIED`, וכתיבה ל-`appdata/` גם `403`. (חשבון הבדיקה נמחק ואומת שנמחק.) **מסקנה: גישה ל-DB רק דרך custom token של ה-GAS, שמונפק רק ל-`FAMILY_EMAILS`.** אל תשנו את ה-Rules ל-`if request.auth != null` — זה יפתח את ה-DB לכל אחד
- **החלטה מודעת (2026-08-07)**: commit `3db2deb` בהיסטוריית `main` הציבורי עדיין מכיל את 4 מיילי המשפחה (הוסרו מה-קוד הנוכחי, לא מההיסטוריה). המשתמש נשאל אם לשכתב היסטוריה (`rebase`+`force-push`) כדי להסיר — **בחר במפורש שלא**, זה לא נתפס כמידע רגיש מספיק כדי להצדיק פעולת git הרסנית. **אל תיזום שכתוב היסטוריה על דעת עצמך** — זו החלטה שכבר התקבלה.

### כללי עבודה
- **לפני כל שינוי — גדול או קטן**: `git tag backup-<תיאור>-$(date +%Y%m%d-%H%M)` + push. אין יוצאים מן הכלל.
- **סדר קבוע**: tag לפני → שינוי → commit + push → עדכן CLAUDE.md → עדכן skill
- **branch**: `claude/unknown-session-xpa0pr` → PR → merge ל-`main`
- **אחרי כל שינוי**: עדכן סעיף זה + skill ב-`.claude/skills/prague-2026.md`
- **ציוני Google**: אימות רק מ-top-rated.online / TripAdvisor — לא להמציא

## איך לערוך את האפליקציה

**אף פעם לא לערוך `index.html` או `gas_project/Code.gs` כאן — הם לא קיימים בריפו הזה בכלל.**

1. ערוך `app.html` כאן.
2. שכפל גם את `arielshish/Prague-2026-backend` **לצד** הריפו הזה (siblings, אותה תיקיית אב).
3. ב-`Prague-2026-backend`, הרץ `python3 sync_gas.py` — זה קורא את `app.html` מכאן ומעדכן את `gas_project/index.html` שם.
4. לפריסת GAS: לך ל-`Prague-2026-backend` — כל הוראות ה-deploy וה-CLAUDE.md המלא נמצאים שם.
